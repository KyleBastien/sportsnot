# Ralph Progress Log
Started: 02/14/2026 08:16:46
---

## Codebase Patterns
- Use `npx nx sync` after adding new packages — it updates TypeScript project references automatically
- Use `{ exact: true }` for position heading selectors (e.g., `getByRole('heading', { name: 'Forward', exact: true })`) to avoid matching "IR Forward"
- Mantine SegmentedControl renders hidden `<input type="radio">` — use `locator('label:has-text("...")')` to click, not `getByRole('radio')`
- DraftPage hooks: all useMemo/useEffect calls must appear BEFORE early returns to avoid React hooks order violations
- DraftPage uses `currentSeason = '20242025'` hardcoded — mock data season doesn't matter since PostgREST mocks ignore query params
- rspack config needs DefinePlugin for `import.meta.env.VITE_*` variables — NxAppRspackPlugin only handles `NX_PUBLIC_*`
- Supabase OTP endpoint is called with `?redirect_to=` query param — use glob `**` suffix in route interceptors
- Page objects use `getByRole`, `getByText`, `getByPlaceholder` — no `data-testid` attributes exist in app
- Import page objects from `../page-objects` — all take a Playwright `Page` in constructor
- App uses Mantine UI — selectors target headings, buttons, tables, badges by role/text
- Playwright config uses `webServer.cwd: '../..'` to reach the monorepo root for `nx serve`
- E2E env vars: `VITE_SUPABASE_URL=http://localhost:54321` and `VITE_SUPABASE_ANON_KEY=mock-anon-key`
- Yarn workspaces auto-resolve `@playwright/test` from root — no need to install separately in e2e
- Auth fixtures: import `{ test, expect }` from `../fixtures/auth.fixture` and use `authenticatedPage`/`unauthenticatedPage`
- Supabase JS stores session in localStorage under `sb-{hostname}-auth-token` — seed via `page.addInitScript()`
- Import `supabasePage` fixture from `../fixtures/supabase-mock.fixture` — use `supabasePage` fixture or `setupSupabaseMocks(page, overrides)` directly
- PostgREST `.single()` uses `Accept: application/vnd.pgrst.object+json` header — mock handlers should check for it
- Use `mockTableList(data)`, `mockTableData(list, single)`, `mockTableError(status, msg)` helpers for route overrides
- E2E tsconfig needs `rootDir: "../.."` and types source in `include` to import from `@sportsnot/types`
- Data factories: import from `../fixtures/data-factories` — all accept `Partial<T>` overrides
- PostgREST join queries (e.g., `leagues` with `league_members!inner(...)`) return nested arrays — mock data must include the joined table as an array property
- Mantine CopyButton + Tooltip wraps ActionIcon — select copy button by emoji content (e.g., `📋`), not tooltip label
- LeagueDashboardPage `.single()` join query includes nested `users` object inside `league_members` — mock must match shape
- Commissioner status: `league.commissioner_id === user.id` — use `mockUser.id` as commissioner_id for commissioner tests
- JoinLeaguePage lookup uses `.single()` with `league_members(id)` join — mock needs nested `league_members` array with `{ id }` entries only
- Settings page query key is `league-settings` — separate invalidation from dashboard `league` key
- Settings page `canModify = isCommissioner && league.status === 'setup'` — controls are disabled outside setup phase
- Draft lobby queries leagues with `league_members(id, user_id, team_name, users(display_name))` join — mock must include nested `users` object
- Draft lobby uses `current_round + 1` as nextRound — set `current_round: 0` for Round 1 display
- "Test User" display_name appears in both nav bar and member list — scope assertions to `getByRole('list')` to avoid strict mode violations
- Use `{ exact: true }` for number text assertions (e.g., `getByText('3', { exact: true })`) to avoid matching substrings like `33`
- Compare tray has `data-testid="compare-tray"` — scope text assertions to avoid matching both table rows and compare tray
- Card DOM structure: heading → Group → Card — use `locator('../..')` to scope from heading to the Card containing the table
- IR activation RPC: `supabase.rpc('activate_ir_player', ...)` → mock at `SUPABASE_URL/rest/v1/rpc/activate_ir_player`
- Use `page.unrouteAll({ behavior: 'ignoreErrors' })` to swap mock data mid-test for post-mutation state
- Round transition page sorts standings desc (best-first) but re-draft picks use worst-first `sortedMembers.length - index` — verify both orderings
- Use `{ exact: true }` for "Round 1" text to avoid matching "Round 1 Complete!" and "Round 1 Final Standings"

## 2026-02-14T16:16 - US-001
- Scaffolded Playwright E2E project in packages/e2e
- Files changed: packages/e2e/* (package.json, playwright.config.ts, project.json, tsconfig.json, tests/smoke.spec.ts), .gitignore, tsconfig.json, yarn.lock, package.json
- **Learnings for future iterations:**
  - `nx sync` must be run after adding a new package to sync TypeScript project references
  - Playwright `--list` flag verifies config without needing the dev server running
  - Git checkout on Windows may fail to delete directories — answer `n` to skip and continue
- Auth fixture uses `page.route()` to intercept Supabase HTTP endpoints + `page.addInitScript()` to seed localStorage
- Mock session storage key follows Supabase convention: `sb-{hostname}-auth-token`
---

## 2026-02-14T16:23 - US-002
- Created `packages/e2e/fixtures/auth.fixture.ts` with Playwright `test.extend()` fixtures
- `authenticatedPage` fixture intercepts all Supabase auth endpoints and returns valid session with mock user
- `unauthenticatedPage` fixture intercepts all Supabase auth endpoints and returns 401/no session
- Mock user includes id, email, display_name, avatar_url fields via user_metadata
- Auth token refresh, OTP, signout endpoints all mocked to prevent hanging requests
- Fixtures export custom `test` and `expect` objects for other test files to import
- Files changed: packages/e2e/fixtures/auth.fixture.ts
- **Learnings for future iterations:**
  - Supabase JS stores session in localStorage under key `sb-{hostname}-auth-token`
  - Must mock both `/auth/v1/token?grant_type=refresh_token` and `/auth/v1/user` for session to work
  - `page.addInitScript()` runs before any app code, perfect for seeding localStorage
  - Supabase auth uses `/auth/v1/otp` for magic links, `/auth/v1/signout` for logout
---

## 2026-02-14T16:26 - US-003
- Created `packages/e2e/fixtures/supabase-mock.fixture.ts` with PostgREST and Realtime mocks
- `setupSupabaseMocks(page, overrides?)` intercepts all known table endpoints with empty defaults
- Default GET returns `[]` for list queries and 406 for `.single()` queries (PostgREST shape)
- POST returns 201, PATCH returns 200, DELETE returns 204
- `page.routeWebSocket()` stubs Supabase Realtime to prevent hanging connections
- Composable via `RouteOverrides` map — tests override specific tables while keeping defaults
- Helper functions: `mockTableList()`, `mockTableData()`, `mockTableError()` for test-specific data
- Extended `authTest` with `supabasePage` fixture combining auth + REST + Realtime mocks
- Tables mocked: leagues, league_members, drafts, draft_picks, rosters, player_stats_cache, team_stats_cache
- Files changed: packages/e2e/fixtures/supabase-mock.fixture.ts
- **Learnings for future iterations:**
  - PostgREST `.single()` sends `Accept: application/vnd.pgrst.object+json` header — use it to distinguish list vs single queries
  - PostgREST error shape: `{ message, details, hint, code }` — code `PGRST116` for no rows on .single()
  - `page.routeWebSocket()` accepts glob patterns — use `${SUPABASE_URL}/realtime/**` for Realtime
  - Composable overrides pattern: pass a `Record<tableName, handler>` to replace specific table mocks
  - Import `supabasePage` fixture from `../fixtures/supabase-mock.fixture` for tests needing data mocks
---

## 2026-02-14T16:28 - US-004
- Created `packages/e2e/fixtures/data-factories.ts` with factory functions for all entity types
- Factories: createMockUser, createMockLeague, createMockLeagueMember, createMockDraft, createMockDraftPick, createMockRosterSlot, createMockRoster (full team), createMockPlayerStats, createMockTeamStats
- All factories accept `Partial<T>` overrides and import types from `@sportsnot/types`
- Uses realistic NHL player names (McDavid, Matthews, Crosby, etc.) and real team IDs
- `createMockRoster()` generates a full team: 5F + 3D + 1G active + 1 IR_F + 1 IR_D
- Updated `packages/e2e/tsconfig.json` to include types source files and set rootDir to monorepo root
- Files changed: packages/e2e/fixtures/data-factories.ts, packages/e2e/tsconfig.json
- **Learnings for future iterations:**
  - E2E tsconfig needs `rootDir: "../.."` and `../../packages/types/src/**/*.ts` in `include` to import from @sportsnot/types
  - `composite: true` enforces rootDir containment — must widen rootDir when importing cross-package types
  - Web app tsconfig pattern: sets `composite: false` and includes source files directly — e2e keeps composite but widens rootDir
---

## 2026-02-14T16:32 - US-005
- Created page object models in packages/e2e/page-objects/ for DashboardPage, DraftPage, RosterPage, StandingsPage
- Files changed: packages/e2e/page-objects/DashboardPage.ts, DraftPage.ts, RosterPage.ts, StandingsPage.ts, index.ts
- **Learnings for future iterations:**
  - No data-testid attributes in the web app — all selectors use getByRole, getByText, getByPlaceholder
  - App uses Mantine UI components (Card, Table, Badge, SegmentedControl, Modal)
  - Draft position filter uses SegmentedControl with radio role — use `getByRole('radio', { name })` to select
  - Current user in standings is marked with "You" badge text — filter rows by hasText: /You/
  - StandingsPage has no CSV export button currently — exportCSV() method uses fallback pattern match
  - Page objects export barrel via index.ts — import from `../page-objects`
---

## 2026-02-14T16:35 - US-006
- Created `packages/e2e/tests/auth.spec.ts` with 6 authentication flow E2E tests
- Tests: unauthenticated redirect to /auth/login, login page renders magic link form, submitting email shows "check your email", auth callback with hash params redirects to dashboard, sign out redirects to login, expired session redirects to login
- Fixed `packages/e2e/fixtures/auth.fixture.ts`: OTP route pattern `${SUPABASE_URL}/auth/v1/otp**` to match query params
- Fixed `packages/web/rspack.config.js`: Added DefinePlugin to inject `import.meta.env.VITE_*` variables from `process.env`
- Files changed: packages/e2e/tests/auth.spec.ts (new), packages/e2e/fixtures/auth.fixture.ts, packages/web/rspack.config.js
- **Learnings for future iterations:**
  - rspack does NOT inject `import.meta.env` by default — NxAppRspackPlugin only defines `NX_PUBLIC_*` vars via its own DefinePlugin
  - Must add `DefinePlugin` with `import.meta.env.VITE_SUPABASE_URL` etc. AFTER Nx plugins to avoid being overridden
  - Supabase `signInWithOtp()` sends POST to `/auth/v1/otp?redirect_to=...` — route interceptor must use glob `**` suffix
  - Auth callback page listens for `SIGNED_IN` event — simulate magic link by navigating to `/auth/callback#access_token=...&type=magiclink`
  - Mantine Menu requires clicking the `Menu.Target` (UnstyledButton with avatar) before `Menu.Item` is visible
  - Use `getByRole('menuitem', { name: /sign out/i })` for Mantine Menu dropdown items
  - For navigation assertions, use `{ timeout: 15000 }` — app can take time to boot and render after Supabase auth resolves
---

## 2026-02-14T16:57 - US-007
- Created packages/e2e/tests/dashboard.spec.ts with 6 dashboard E2E tests (5 passed, 1 skipped)
- Tests: authenticated user sees heading, league list with 3 leagues, empty state, live games widget (skipped), clicking league card navigates to /leagues/:id, Create/Join League CTAs navigate correctly
- Built createLeagueWithMembers() helper to create mock league data matching the PostgREST join shape
- Used mockTableList() override for the leagues table to inject test-specific data
- Files changed: packages/e2e/tests/dashboard.spec.ts (new)
- **Learnings for future iterations:**
  - Dashboard queries leagues with league_members!inner(...) join -- mock data must include league_members array in response
  - PostgREST join responses nest related table data as arrays inside the parent object
  - League fields in PostgREST responses use snake_case while TypeScript types use camelCase
  - Live games widget does not exist in the dashboard yet -- test skipped with test.skip()
  - DashboardPage page object navigateToLeague(name) clicks the heading text -- works for Card-based layouts
---

## 2026-02-14T17:00 - US-008
- Created `packages/e2e/tests/league-create.spec.ts` with 6 league creation flow E2E tests
- Tests: form renders on /leagues/create, required fields validation, max participants range, successful POST + navigation to dashboard, invite code and commissioner controls shown, copy button interaction with clipboard verification
- Mocked POST to leagues and league_members tables, and GET for league dashboard with join query shape
- Files changed: packages/e2e/tests/league-create.spec.ts (new)
- **Learnings for future iterations:**
  - Mantine CopyButton wraps ActionIcon — Tooltip label is NOT an accessible name on the button; use emoji content `📋` as the button name selector
  - League creation does two inserts: POST to `leagues` (with `.select().single()`) then POST to `league_members`
  - LeagueDashboardPage fetches via `.select('*, league_members(id, user_id, team_name, total_points, users(display_name, avatar_url))')` — mock must include nested `users` object inside `league_members` array
  - Commissioner is determined by `league.commissioner_id === user.id` — use `mockUser.id` for commissioner tests
  - Start Draft button is disabled when `members.length < 2` — test can verify disabled state for single-member league
  - Mantine NumberInput renders as `role="textbox"` not `role="spinbutton"` — use `getByRole('textbox', { name: ... })`
---

## 2026-02-14T17:04 - US-009
- Created `packages/e2e/tests/league-join.spec.ts` with 6 join league flow E2E tests
- Tests: invite code input renders, invalid code shows error, valid code shows league preview + team name prompt, successful join navigates to league dashboard, full league shows error, already-joined shows member error
- Two-step flow: Step 1 enters invite code and clicks "Find League" → Step 2 enters team name and clicks "Join League"
- Mocked PostgREST `.single()` with `league_members(id)` join for member count, unique constraint error for duplicate join
- Files changed: packages/e2e/tests/league-join.spec.ts (new)
- **Learnings for future iterations:**
  - JoinLeaguePage is a two-step form: lookup by invite code first, then show preview + team name input
  - Invite code lookup uses `.select('id, name, max_participants, league_members(id)').eq('invite_code', ...).single()` — mock needs nested `league_members` array with `id` only
  - Full league check: `league_members.length >= max_participants` — mock member count equal to max to trigger error
  - Already-a-member detection: checks if error message `.includes('unique')` — mock 409 with unique constraint message
  - Invite code is auto-uppercased by the input — mock data comparison is case-insensitive
---

## 2026-02-14T17:07 - US-010
- Created `packages/e2e/tests/league-dashboard.spec.ts` with 6 league dashboard and settings E2E tests
- Tests: dashboard shows league name/status/members/standings, non-commissioner sees no settings, commissioner navigates to settings, commissioner edits league name via PATCH, commissioner regenerates invite code, Start Draft button visible in setup status
- Used two user contexts: commissioner (mockUser.id matches commissioner_id) and non-commissioner (different commissioner_id)
- Mocked PATCH requests for settings save and invite code regeneration with request tracking
- Files changed: packages/e2e/tests/league-dashboard.spec.ts (new)
- **Learnings for future iterations:**
  - Settings page uses `league-settings` query key (not `league`) — separate from dashboard query
  - Settings page shows "Access Denied" alert when non-commissioner visits — no redirect
  - `canModify` check: `isCommissioner && league.status === 'setup'` — both conditions required for edit controls
  - Regenerate Code button uses `generateInviteCode()` from `@sportsnot/utils` — PATCH updates `invite_code` field
  - Settings page CopyButton uses `<Button>` with text "Copy" — different from dashboard ActionIcon with emoji
  - Members table in settings shows 👑 emoji next to commissioner name
---

## 2026-02-14T17:10 - US-011
- Created `packages/e2e/tests/draft-lobby.spec.ts` with 4 draft lobby E2E tests
- Tests: member list with badges (Commissioner, You), snake draft info card (format, participants, total picks, roster), commissioner Start Draft button, non-commissioner waiting state
- Mocked leagues table with `.single()` join query returning nested `league_members` with `users(display_name)`
- Files changed: packages/e2e/tests/draft-lobby.spec.ts (new)
- **Learnings for future iterations:**
  - Draft lobby queries leagues with `league_members(id, user_id, team_name, users(display_name))` join — different from dashboard which also includes `total_points`
  - `current_round` is 0 initially — lobby shows `current_round + 1` as the upcoming round number
  - "Test User" display_name appears in both the nav bar and the member list — use `getByRole('list')` to scope assertions to the participants list
  - Use `{ exact: true }` for number assertions like `getByText('3', { exact: true })` to avoid matching `33` or `3D`
  - Lobby also polls `drafts` table for active draft with `.maybeSingle()` — default empty mock is sufficient since no active draft exists in lobby state
---

## 2026-02-14T17:14 - US-012
- Created `packages/e2e/tests/draft-board.spec.ts` with 8 draft board E2E tests
- Fixed React hooks order violation in `packages/web/src/app/routes/draft/DraftPage.tsx`: moved `useMemo` and `useEffect` before early returns
- Tests: available players list renders, position filtering (F/D/G/All), search by name, stat column headers, eliminated/injured player filtering, draft confirmation modal, pick confirmation with history update, draft history with position badges
- Mocked active draft with current user's turn, 12 forwards + 5 defensemen + 1 injured forward, 5 teams + 1 eliminated team
- Files changed: packages/e2e/tests/draft-board.spec.ts (new), packages/web/src/app/routes/draft/DraftPage.tsx (hooks fix)
- **Learnings for future iterations:**
  - DraftPage had useMemo calls AFTER early returns (loading/no-draft states), causing "Rendered more hooks than during the previous render" crash — all hooks must be before any early return
  - Mantine SegmentedControl renders `<input type="radio">` as hidden (0-size) elements — `getByRole('radio')` finds them but they're not clickable; use `locator('label:has-text("...")')` instead
  - Draft queries: `useDraft` does `.from('drafts').select('*, draft_picks(*, league_members(team_name, user_id))').single()` with 3s polling
  - `usePlayoffPlayers` queries `player_stats_cache` with `nhl_season` and `playoff_round` filters, ordered by goals desc
  - `usePlayoffTeams` queries `team_stats_cache` with same filters, ordered by wins desc
  - Injured players (`is_injured: true`) and eliminated teams (`is_eliminated: true`) are filtered out entirely — not greyed out
  - No interactive sort buttons exist in the draft UI — players are pre-sorted by the query
  - Draft confirmation modal title is "Confirm Draft Pick" with position SegmentedControl (Forward/IR Forward, Defense/IR Defense, or Goalie)
  - Use `getByRole('columnheader', { name: 'G', exact: true })` to match single-letter column headers without ambiguity
---

## 2026-02-14T17:25 - US-013
- Implemented compare tray feature in DraftPage.tsx: state management, toggle compare buttons on skater rows, compare tray with side-by-side stats
- Created `packages/e2e/tests/player-compare.spec.ts` with 5 E2E tests all passing
- Tests: adding player shows compare bar, up to 4 players max, stat comparison table, removing players, persistence across filtering
- Files changed: packages/web/src/app/routes/draft/DraftPage.tsx, packages/e2e/tests/player-compare.spec.ts (new)
- **Learnings for future iterations:**
  - Compare tray uses `data-testid="compare-tray"` for E2E test targeting
  - `getByText('Connor McDavid')` matches both the available players table AND the compare tray -- scope assertions to specific sections when asserting filter-hidden state
  - ComparePlayer interface stores stats (goals, assists, points) at add-time -- no re-fetch needed
  - Compare button toggles between 'Compare' (outline) and 'Compared' (filled) states; disabled when tray is full (4 players)
  - Compare tray state lives in React useState -- persists across filter/search changes within the same page render
---

## 2026-02-14T17:32 - US-014
- Created `packages/e2e/tests/roster.spec.ts` with 5 roster management E2E tests
- Tests: all active slots (5F, 3D, 1G) with player details and count badges, IR slots (IR_F, IR_D) with Inactive badges and Activate IR buttons, round stats and earned points per player, total active points calculated correctly, roster accessible via league dashboard "My Roster" button
- Mocked league_members (single for current user), leagues (single for current_round), rosters (list of 11 slots: 5F+3D+1G active, 1 IR_F+1 IR_D inactive)
- RosterPage displays players as "Player #ID" or "Team #ID" — no player name resolution at the roster level
- Files changed: packages/e2e/tests/roster.spec.ts (new)
- **Learnings for future iterations:**
  - RosterPage `useMyRoster` hook does 3 sequential queries: league_members.single() → leagues.single() → rosters.select() list
  - Position group headings: "Forward", "Defenseman", "Goalie", "IR Forward", "IR Defenseman" — use `{ exact: true }` for "Forward"/"Defenseman" to avoid matching IR variants
  - Active slots have green "Active" badge, IR slots have gray "Inactive" badge
  - Activate IR button only appears on IR slots when there are active candidates at the matching base position
  - Total points sums only `is_active: true` slots' `points_earned` values
  - League dashboard "My Roster" button only visible when `league.status === 'active'`
---

## 2026-02-14T17:37 - US-015
- Created `packages/e2e/tests/ir-activation.spec.ts` with 6 IR activation flow E2E tests
- Tests: IR activation button visible on IR slots, clicking opens modal, modal shows same-position replacement players, modal displays point warning/differential, confirming activation calls RPC and shows "From IR" badge, position mismatch (no active Forwards) hides Activate IR button
- Mocked `activate_ir_player` RPC endpoint, used `unrouteAll` + re-mock pattern to swap roster data after activation
- Files changed: packages/e2e/tests/ir-activation.spec.ts (new)
- **Learnings for future iterations:**
  - Card DOM structure: heading → Group → Card — use `locator('../..')` (two levels up) to scope from heading to the full Card containing the table
  - IR activation calls `supabase.rpc('activate_ir_player', ...)` → mocked at `SUPABASE_URL/rest/v1/rpc/activate_ir_player`
  - IR slot "Activate IR" button only appears when: `isIrSlot && !activated_from_ir && injuredCandidates.length > 0`
  - After activation, `activated_from_ir: true` triggers "From IR" orange badge on the slot
  - Use `page.unrouteAll({ behavior: 'ignoreErrors' })` to swap mock data mid-test for post-mutation state
- StandingsPage queries: `leagues.select('name, current_round').single()` + `league_members.select('id, user_id, team_name, total_points, users(display_name)').order('total_points', desc)`
- StandingsPage has Rank/Team/Manager/Points columns — no breakdown columns or CSV export yet
- Scoring history page does not exist — no route or component; use `test.skip()` for future tests
---

## 2026-02-14T17:42 - US-016
- Created `packages/e2e/tests/standings.spec.ts` with 5 standings E2E tests (2 passed, 3 skipped)
- Created `packages/e2e/tests/scoring-history.spec.ts` with 2 scoring history E2E tests (0 passed, 2 skipped)
- Tests passing: members ranked by total points with medal badges (1st/2nd/3rd), current user row highlighted with "You" badge
- Tests skipped: points breakdown columns (not implemented), round-by-round columns (not implemented), CSV export (not implemented), scoring history page (not implemented - no route or component exists)
- Files changed: packages/e2e/tests/standings.spec.ts (new), packages/e2e/tests/scoring-history.spec.ts (new)
- **Learnings for future iterations:**
  - StandingsPage queries `leagues.select('name, current_round').single()` + `league_members.select('id, user_id, team_name, total_points, users(display_name)').order('total_points', { ascending: false })`
  - StandingsPage only has 4 columns: Rank, Team, Manager, Points - no breakdown or round-by-round columns
  - Medal badges: 1st (gold), 2nd (gray), 3rd (orange) - 4th+ show plain number
  - Current user identified by `member.user_id === user?.id` - gets bold text, blue background, and "You" badge
  - No CSV export button exists on StandingsPage
  - Scoring history page has no route, no component, and empty directory at `packages/web/src/app/routes/scoring/`
  - Use `test.skip()` for features not yet implemented - keeps test count accurate for tracking
---

## 2026-02-14T17:45 - US-017
- Created `packages/e2e/tests/round-transition.spec.ts` with 5 round transition E2E tests (all passing)
- Tests: previous round final standings with ranked members table, re-draft pick order (worst-to-best snake), draft history with completed rounds, commissioner Start Re-Draft button with POST verification, non-commissioner waiting state
- Mocked leagues table with `.single()` join (league_members + users), drafts table with completed status filter
- 4 members with varying points: Other(40), Delta(32), Alpha(25), Gamma(15) — verifies ranking and pick order
- Files changed: packages/e2e/tests/round-transition.spec.ts (new)
- **Learnings for future iterations:**
  - Round transition page sorts standings desc (best-first) but re-draft picks calc is `sortedMembers.length - index` (worst gets pick #1)
  - `getByText('Round 1')` is ambiguous on this page — matches heading, subheading, and draft history text; use `{ exact: true }`
  - Drafts query filters by `status=eq.completed` in URL — use `url.includes('status=eq.completed')` to distinguish from POST
  - Commissioner action inserts new draft via POST to `drafts` table then PATCHes leagues status to 'drafting'
  - Back to League button uses `variant="subtle"` — still matches `getByRole('button')`
---

## 2026-02-14T17:49 - US-018
- Created `packages/e2e/tests/navigation.spec.ts` with 5 navigation and routing E2E tests (all passing)
- Tests: main nav links (logo, user menu Dashboard/Profile), browser back/forward navigation, deep-linking to all 12 protected routes redirects unauthenticated users, deep-linking when authenticated loads correct pages, unknown routes render app shell without crashing
- Used DashboardPage page object for consistent heading assertions
- Files changed: packages/e2e/tests/navigation.spec.ts (new)
- **Learnings for future iterations:**
  - Dashboard heading is "Dashboard" (Title order={2}), not "Welcome Back" — "Welcome Back" is a subheading Text element
  - Use DashboardPage page object `dashboard.heading` for reliable dashboard load assertions
  - No 404 catch-all route exists — unknown routes render empty AppShell.Main with just the header
  - UserMenu items are `menuitem` role: Dashboard, Profile, Sign Out — accessed via `getByRole('menuitem')`
  - `goto()` + `goBack()` + `goForward()` work reliably for history navigation tests in Playwright
---

## 2026-02-14T17:53 - US-019
- Created `packages/e2e/tests/mobile.spec.ts` with 5 mobile viewport E2E tests (all passing)
- Tests: responsive nav (user name hidden on mobile, avatar menu works), draft page (filtering, searching, picking, modal), roster page (position groups, total points, player entries), league dashboard (league name, members, nav buttons), modals on mobile (draft confirmation modal visible and interactive)
- All tests use `page.setViewportSize({ width: 375, height: 667 })` for iPhone SE viewport
- Reused mock patterns from draft-board, roster, and league-dashboard tests
- Files changed: packages/e2e/tests/mobile.spec.ts (new)
- **Learnings for future iterations:**
  - App uses Mantine `visibleFrom="sm"` on user display name in header -- hidden on mobile, only avatar initial shows
  - No bottom navigation or sidebar exists -- app uses header-only layout with user menu dropdown
  - League dashboard members are in a table, not a `role="list"` -- use `getByText()` directly, not scoped to `getByRole('list')`
  - Mantine modals render as overlay dialogs on all viewports -- no special bottom sheet behavior on mobile
  - `setViewportSize()` should be called before `setupSupabaseMocks()` and `goto()` for consistent rendering
- DashboardPage does not destructure `isError` from React Query — API errors fall through to empty state, not ErrorBoundary
- React Query `retry: 1` config — tests for error states should account for retry delay
- Use `mockTableError(status, message)` to simulate PostgREST errors in route handlers
---

## 2026-02-14T17:58 - US-020
- Created `packages/e2e/tests/error-states.spec.ts` with 5 error state and edge case E2E tests (all passing)
- Tests: network failure fallback (500 error → empty state), 401 triggers login redirect, server-side form validation errors shown, loading states with delayed responses, empty states for no data
- Used `mockTableError()` for 500 error simulation, delayed `route.fulfill()` for loading states, localStorage clearing for auth invalidation
- Files changed: packages/e2e/tests/error-states.spec.ts (new)
- **Learnings for future iterations:**
  - DashboardPage swallows API errors — only destructures `isLoading` from `useMyLeagues()`, not `isError`; falls through to empty state
  - React Query default config: `retry: 1`, `staleTime: 5min` — errors retry once before settling
  - To test loading states: use `await new Promise(resolve => setTimeout(resolve, ms))` inside route handler before `route.fulfill()`
  - Mantine `Loader` component has class `.mantine-Loader-root` — use CSS selector to detect loading spinners
  - To simulate expired auth: override `/auth/v1/user` to return 401 AND clear localStorage `sb-*` keys via `addInitScript()`
  - CreateLeaguePage properly shows server error messages via red Alert with `{error}` text content
---

## 2026-02-14T18:04 - US-021
- Created dedicated .github/workflows/e2e.yml CI workflow for Playwright E2E tests
- Workflow triggers on PRs and pushes to main branch
- CI installs Playwright browsers via 
px playwright install --with-deps chromium with caching
- Builds web app with 
x build @sportsnot/web using mock Supabase env vars
- Playwright runs against built static output via 
px serve (not dev server)
- HTML report always uploaded; trace files uploaded on failure as CI artifacts
- Updated playwright.config.ts to use static file server in CI, dev server locally
- Playwright browser cache at ~/.cache/ms-playwright keyed by yarn.lock hash
- Files changed: .github/workflows/e2e.yml (new), packages/e2e/playwright.config.ts
- **Learnings for future iterations:**
  - @nx/web:file-server executor provides serve-static target but triggers rebuild — use 
px serve for pre-built output
  - Playwright CI config: orbidOnly: true, etries: 2, workers: 1 — already in config via process.env.CI
  - 
px serve --single enables SPA fallback routing (equivalent to 404.html redirect)
  - Separate browser install (--with-deps) from system deps (install-deps) for cache optimization
  - Set VITE_* env vars at workflow level so both build and test steps have access
---
