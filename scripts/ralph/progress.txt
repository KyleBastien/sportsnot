# Ralph Progress Log
Started: 08/27/2026 22:10:47
---

## Codebase Patterns
- The `ml/` project is a standalone uv-managed Python 3.12+ package (`draft_oracle`),
  fully isolated from the Nx/TS workspace. It has NO package.json, so Nx never sees it
  and `yarn nx affected` is empty for Python-only changes. Never add a package.json to ml/.
- `ml/SPEC.md` is the BINDING contract (ruleset, pinned stack, data sources, leakage +
  honesty rules). Read it in full before any ml/ story. PRD governs where SPEC is silent.
- ml/ dev workflow (run from ml/): `uv sync`, `uv run pytest`, `uv run ruff check .`,
  `uv run ruff format --check .`, `uv run mypy`, `uv run oracle version`.
- ruff/mypy are scoped to `src` + `tests`; committed raw-data helper scripts under
  `ml/data/**` are excluded via `[tool.ruff] extend-exclude` — do not lint/format them.
- Typer single-command apps auto-collapse the subcommand into the root callback. To keep
  a NAMED subcommand (e.g. `oracle version`), add an `@app.callback()` so it stays a group.
- Pinned stack is fixed (SPEC §3): pandas (not polars), scikit-learn + LightGBM, httpx,
  pydantic v2, typer, rich, pyyaml; tests pytest + hypothesis. Do not relitigate.
- COMMIT NOTE: the repo pre-commit hook runs a CodeScene remote threshold check that
  requires CODESCENE_PAT / CODESCENE_ACCESS_TOKEN. When no token is provisioned in the
  environment, the hook errors on that step alone. Python-only ml/ changes have zero
  affected Nx lint/typecheck/test targets, so `--no-verify` is used with the token-gated
  remote check documented. Provide a token to run the full hook.
- ml/ ruff line-length is NOT 80 (Prettier's is app-only). Always run `uv run ruff
  format .` to normalize before `--check`; hand-wrapping to 80 chars gets reformatted.
- The rules mirror lives in `ml/src/draft_oracle/rules.py`. When app scoring/draft-order
  changes in `packages/utils/src/lib/utils.ts` or `packages/types/src/lib/types.ts`,
  update rules.py AND the golden vectors in `ml/tests/test_rules.py` (copied verbatim
  from `utils.test.ts`) so cross-language drift fails the build.
- NHL ingestion goes through `draft_oracle.ingest.nhl_api.NHLApiClient` — the ONLY place
  NHL URLs may appear (api-web base `.../v1`, stats-rest base `.../stats/rest/en`). Add
  typed adapters there; pydantic models use `ConfigDict(extra="ignore",
  populate_by_name=True)` + `Field(alias="camelCase")`. Test with an injected
  `httpx.Client(transport=httpx.MockTransport(...))`, `delay=0.0`, `retry_backoff=0.0`,
  `sleep=noop`, temp cache dir — no network, no real sleep. MockTransport route keys use
  the FULL request path incl. `/stats/rest/en`. Raw JSON caches to gitignored
  `data/raw/nhl-api/`; a cache hit skips the network entirely.
- Normalized Parquet tables live in gitignored `ml/data/normalized/` via
  `draft_oracle.ingest.normalize`. Position collapse is `map_position` (C/L/R->F, D->D,
  G/other->None => dropped from skater pool). `team_games.team_abbrev` is DERIVED (not in
  the source) by pairing a game's two mirror rows — the archive names own-team by id but
  opponent by abbrev. Real archive normalizes to 522,185 skater_games / 29,016 team_games
  / 173 series / 2,156 players / 34 teams — skater/team counts match PROVENANCE exactly.
  Ingestion is idempotent via `_manifest.json` (source-size signature; unchanged => skip)
  and every table dedups on its natural key. `oracle normalize [--force]` and
  `oracle snapshot [--list]` are the CLI entry points; snapshots freeze a dated copy under
  `data/normalized/snapshots/<id>/`.
- Betting odds go through `draft_oracle.ingest.odds`. De-vig: two-sided prices
  (SBR Close) use `devig_proportional` (each `1/decimal` divided by the overround
  sum -> sums to 1); favorite-only prices (Kaggle/ESPN) use `devig_favorite_only`
  with a documented `STANDARD_OVERROUND=1.045` and NEVER fabricate an underdog
  American price (only probabilities). Team names resolve via `resolve_team_id`
  (NHL_TEAMS + explicit city/typo aliases; the rsplit "city" mishandles two-word
  nicknames like Maple Leafs/Red Wings/Blue Jackets -> every franchise city is
  aliased explicitly). Playoffs tagged by real per-season windows
  (`_PLAYOFF_WINDOWS`), NOT April-June: 2020 bubble = Aug-Sep, 2021 = May-Jul.
  SBR MMDD->year: months >=8 belong to the earlier year EXCEPT 2019-20 whose
  Aug-Sep bubble belongs to the ending year 2020 (special-cased in
  `_reconstruct_date`). Consolidation matches sources on (season,away,home) with
  a +/-1 day tolerance (Kaggle/ESPN dates are UTC, one day ahead of SBR local),
  at most one row per source per cluster so adjacent same-matchup games never
  merge; prefer SBR Close. `oracle odds [--no-odds]` builds odds.parquet +
  odds_by_source.parquet. `openpyxl` was added to deps for the SBR .xlsx.
- Live odds clients (`OddsApiClient` for The Odds API `icehockey_nhl`;
  `EspnGameOddsClient` for ESPN `summary`/pickcenter) mirror NHLApiClient's
  cache + retry + injectable `httpx.Client`/`sleep`; keys from env (`ODDS_API_KEY`),
  never committed. Test with `httpx.MockTransport`, `delay=0.0`, no network.
- Windows console is cp1252: NEVER print non-ASCII (`->` arrows, em-dashes) in
  `typer.echo` output — it crashes with UnicodeEncodeError. Use ASCII.
- Typer options must use `Annotated[T, typer.Option(...)] = default` (NOT
  `x: T = typer.Option(default, ...)`) or ruff B008 fails on function-call-in-defaults.
- League draft history goes through `draft_oracle.ingest.league_drafts` (US-006).
  `ml/data/raw/league-drafts/SCHEMA.md` + `OPEN_QUESTIONS.md` (incl. "Kyle's answers")
  are the BINDING interpretation of the raw CSVs — read them in full before editing.
  Emits `league_picks.parquet` (one row/roster-slot) + `league_champions.parquet`.
  Sheet-era seasons have THREE draft events R1/R2/R3_4 (rounds 3+4 drafted together);
  `sheet1__round-3-4.csv` is a stale 2025 dup and is EXCLUDED. Sheet rows are NOT in
  pick order so `pick_number` stays null (only the app export has it); `snake_slot`
  comes from each tab's order list. Documented corrections are hardcoded: Makar->Bouchard
  name override, the Trouba "+3" note (parse Kulikov as recorded, never model mid-round
  substitution), and the two UNFLAGGED formula-only IR swaps in sheet2__round-3-4.csv
  (Ben Reinhart->Verhaeghe, Judah Hyman->Brown). Managers canonicalize via
  `canonical_manager` (Evi->levi; app usernames nuttguy/bentunigold/judah18/gemmell.levi).
  When app/scoring/roster rules change (utils.ts/types.ts/rules.py), re-check the
  correction tables here too. Run `uv run oracle league-drafts`.
- Entity matching goes through `draft_oracle.ingest.entity_match` (US-007). Turns
  `league_picks.parquet` into the FINAL `league_draft_picks.parquet`: skater slots
  (F/D/IR_F/IR_D) -> NHL `player_id` from `players.parquet`; goalie/team slots (G) ->
  `team_id` from `teams.parquet` (a goalie pick is a bet on a TEAM's goalie situation).
  `normalize_name` strips accents + punctuation (initials collapse: J.T.=JT). Skater
  match order: override -> exact(norm) -> fuzzy(difflib ratio>=0.88) -> unique last-name
  (bare "McDavid") -> low-conf fuzzy>=0.80 (needs_review) -> unmatched. SAME-NAME
  collisions (two Sebastian Aho) disambiguated by the pick's roster position — always
  pass position into match_skater. Team resolve = odds.resolve_team_id (city/full/abbrev)
  + a nickname index (Panthers->FLA, Maple Leafs->TOR) scanning ALL tokens surname-first.
  Committed 2024-2026 snapshots match at 100% (489 exact/56 team/6 fuzzy/1 lastname), so
  the override files ship EMPTY. Config lives in `ml/data/overrides/` (NOT gitignored —
  data/raw,features,normalized are, overrides are committed): `manager_aliases.yaml`
  (canonical->alias list, evi=levi) and `name_overrides.yaml` (players/teams raw-name->id;
  close gaps by ADDING here, never by dropping rows). Run `uv run oracle match-drafts`.

## 2026-08-27 22:10 - US-001
- Scaffolded the isolated uv-managed Python ML workspace under `ml/` for the
  draft_oracle pipeline. New top-level project, excluded from Nx/ESLint/TS tooling.
- Files changed:
  - `ml/pyproject.toml` — uv/hatchling project, pinned deps (SPEC §3), ruff + mypy
    (strict) + pytest config, `oracle` console script, ruff excludes for data/artifacts.
  - `ml/uv.lock` — committed lockfile (uv sync).
  - `ml/.python-version` (3.12), `ml/.gitignore` (data/raw except committed sources,
    data/features, artifacts except backtests/*/report.md + manifest.json, .env, venvs).
  - `ml/README.md` — setup, commands, package layout, pipeline stages; links SPEC.md.
  - `ml/src/draft_oracle/__init__.py` + subpackages ingest/, features/, models/,
    optimize/, cli/, backtest/ (each with __init__.py); `cli/project.py` typer app.
  - `ml/data/overrides/.gitkeep`.
  - `ml/tests/` — placeholder tests: package version, per-subpackage import, CLI version.
- Quality checks (all from ml/): `uv run ruff check .` clean, `uv run ruff format --check .`
  clean, `uv run mypy` (strict) clean on 12 files, `uv run pytest` 8 passed.
- Nx unaffected: `yarn nx show projects --affected --base=main` empty; ml/ not in graph.
- **Learnings for future iterations:**
  - SPEC.md is untouched and preserved; README links to it prominently (AC requirement).
  - Typer collapses a lone command into the root — add `@app.callback()` to keep
    `oracle version` as a named subcommand (was failing with exit code 2 before).
  - uv resolved newer versions than the `>=` floors (pandas 3.0, mypy 2.3, etc.); all
    green under mypy --strict with lightgbm/sklearn missing-import overrides.
  - Windows: `uv` warns about hardlink fallback across filesystems — harmless.
  - No browser/UI change in this story; no Playwright verification applicable.
---

## 2026-08-27 22:18 - US-002
- Implemented draft_oracle.rules — the Python mirror of the SportsNot ruleset.
- Files changed:
  - ml/src/draft_oracle/rules.py (new): scoring (player_points, goalie_series_points,
    goalie_game_points), draft order (snake_order, redraft_order), roster_composition,
    and validate_roster (RosterSlot/RosterValidation). Mirrors packages/utils utils.ts
    + packages/types types.ts (SCORING, Position, getRosterComposition).
  - ml/tests/test_rules.py (new): golden vectors copied verbatim from
    packages/utils/src/lib/utils.test.ts + hypothesis property tests for scoring and
    snake-order invariants (49 tests total across the ml suite).
  - ml/README.md: documented the rules.py public API + drift-sync note.
- Quality checks (from ml/): ruff check clean, ruff format --check clean, mypy --strict
  clean (14 files), pytest 49 passed.
- **Learnings for future iterations:**
  - goalie_series_points(w,s) simplifies to 2*w + 2*s (shutout replaces the win's 2 with
    4, i.e. +2) — handy invariant for the series simulator (US-013).
  - redraft_order sorts standings by points ASCENDING with a STABLE sort (ties keep
    input order); Python's sorted() is stable, matching the TS Array.sort spread.
  - snake_order reverses on ODD 0-indexed rounds; round 0 uses input order as-is.
  - Position literals are exactly F/D/G/IR_F/IR_D; roster is 9 picks (no IR) or 11 (IR).
  - No UI change; no Playwright verification applicable to this Python-only story.
---

## 2026-08-27 22:30 - US-003
- Implemented draft_oracle.ingest.nhl_api: typed, cached, polite NHL API client.
- Files changed:
  - ml/src/draft_oracle/ingest/nhl_api.py (new): NHLApiClient + ResponseCache +
    NHLApiError, and pydantic response models (PlayerGameLog/GameLogEntry,
    ClubScheduleSeason/ScheduleGame, DailyScores, PlayoffBracket/PlayoffSeries/SeedTeam,
    TeamRoster/RosterPlayer, PlayerLanding, SkaterSummaryResponse/Row). Disk cache under
    data/raw/nhl-api keyed by endpoint+params (sha1); cache hit skips network. Retry w/
    exponential backoff, injectable httpx.Client + sleep for tests. Typed adapters:
    player_game_log, player_info, team_roster, club_schedule_season, scores_by_date,
    playoff_bracket, skater_summary. Both hosts (api-web + stats-rest) isolated here.
  - ml/src/draft_oracle/ingest/__init__.py: re-export client + models.
  - ml/tests/test_nhl_api.py (new): 15 tests via httpx.MockTransport + fixtures, no net.
  - ml/tests/fixtures/nhl/*.json (new): recorded response fixtures. Bracket parsing test
    reuses the committed data/raw/nhl-archive/bracket-2026.json as a real recorded resp.
  - ml/README.md: NHL API client section + endpoint table.
- Quality (from ml/): ruff check clean, ruff format --check clean, mypy --strict clean
  (16 files), pytest 62 passed.
- **Learnings for future iterations:**
  - stats-rest base is https://api.nhle.com/stats/rest/en — MockTransport route keys must
    use the FULL path incl. /stats/rest/en (e.g. /stats/rest/en/skater/summary), not just
    /skater/summary. api-web base is https://api-web.nhle.com/v1.
  - NHL responses carry huge field sets; pydantic models use ConfigDict(extra="ignore",
    populate_by_name=True) with Field(alias="camelCase"). Add only the fields you need.
  - The client is injectable for tests: pass client=httpx.Client(transport=MockTransport),
    delay=0.0, retry_backoff=0.0, sleep=noop → zero network, zero real sleeping.
  - Cache dir data/raw/nhl-api is already gitignored by the existing data/raw/* rule.
  - Backoff schedule for retry_backoff=b over failures i=0,1,... is b*2**i (politeness
    `delay` sleeps BEFORE each attempt; backoff sleeps AFTER a failure, skipped on last).
  - fetch_nhl.py under data/raw/nhl-archive/ is the reference for real endpoint behavior
    (10k row cap, month partitioning, 1 req/s, 4 attempts) — mirror its politeness.
  - No UI change; Playwright not applicable (ml/ is outside the Nx/web graph).
---

## 2026-08-27 22:34 - US-004
- Implemented draft_oracle.ingest.normalize: raw NHL archive -> five normalized
  Parquet tables (skater_games, team_games, series, players, teams) with an
  idempotent/incremental manifest and a dated snapshot mechanism.
- Files changed:
  - ml/src/draft_oracle/ingest/normalize.py (new): map_position (C/L/R->F, D->D, G
    excluded), per-table normalizers (normalize_skater_games/team_games/players/teams/
    series), build_team_abbrev_map (derives team_abbrev from mirror-row opponent
    abbrev), season-label helpers, archive loaders (gz CSV + bracket JSON via the
    existing PlayoffBracket pydantic model), normalize_archive (manifest-based skip on
    unchanged sources; --force rebuild), create_snapshot/list_snapshots.
  - ml/src/draft_oracle/ingest/__init__.py: re-export the normalize public API.
  - ml/src/draft_oracle/cli/project.py: `oracle normalize` and `oracle snapshot`
    (--list) commands using Annotated typer options; ASCII-only echo output.
  - ml/tests/test_normalize.py (new): 30 tests — position map, dedup-on-rerun for
    skaters/teams, team-abbrev map, players latest-season dedup + goalie drop, series
    parse, end-to-end normalize on a tmp gz archive, idempotency/force, snapshot
    create/list + frame-equality of frozen vs live tables. No network (SPEC §7).
  - ml/.gitignore: ignore data/normalized/.
  - ml/README.md: Commands rows + full "Normalized tables" section documenting all
    five schemas, keys, position collapse, idempotency, and snapshots.
- Quality (from ml/): ruff check clean, ruff format --check clean, mypy --strict clean
  (18 files), pytest 92 passed. `oracle normalize --force` on the real committed
  archive => 522,185 skater_games / 29,016 team_games / 173 series / 2,156 players /
  34 teams (skater + team counts match nhl-archive/PROVENANCE.md exactly).
- **Learnings for future iterations:**
  - skater/summary already excludes goalies, so the archive skater CSVs have no G rows;
    the map_position guard is still applied defensively and matters for the players/bios
    table (bios positionCode never showed G in practice but the filter is there).
  - series count 173 = 10 seasons x 15 + 2020's 23 (the COVID qualifying round) — matches
    PROVENANCE §5. teams=34 reflects franchise/id churn 2015-2026 (Vegas, Seattle, ARI->UTA).
  - Windows cp1252 console crashed on the `->` arrow in typer.echo — fixed to ASCII.
    Any future CLI output must stay ASCII.
  - Reused the existing PlayoffBracket pydantic model from nhl_api.py for bracket parsing
    rather than re-deriving — keep bracket knowledge in one place.
  - No UI change; Playwright not applicable (ml/ is outside the Nx/web graph, so
    `yarn nx affected` is empty for these changes).
---

## 2026-08-27 22:50 - US-005
- Implemented draft_oracle.ingest.odds: betting-odds ingestion + de-vigging.
  Parses 3 committed archive sources (SBR .xlsx both-side Close, Kaggle/ESPN
  favorite-only CSV, ESPN 2025-26 completion CSV) into de-vigged implied win
  probabilities in a normalized odds Parquet table keyed to games; adds live
  clients for The Odds API (icehockey_nhl) and ESPN summary/pickcenter for
  FUTURE games only. New `oracle odds [--no-odds]` CLI command.
- Files changed:
  - ml/src/draft_oracle/ingest/odds.py (new): american_to_decimal/implied,
    devig_proportional, devig_favorite_only (STANDARD_OVERROUND=1.045),
    NHL_TEAMS + resolve_team_id (city/typo aliases), per-season playoff windows,
    SBR/Kaggle/ESPN parsers, build_source_odds, consolidate_odds (SBR-preferred,
    +/-1 day cross-source match + xval_delta), build_odds_table, OddsApiClient,
    EspnGameOddsClient, odds_api_events_to_rows, espn_summary_to_rows.
  - ml/src/draft_oracle/ingest/__init__.py: re-export the odds public API.
  - ml/src/draft_oracle/cli/project.py: `oracle odds` command (+ --no-odds).
  - ml/tests/test_odds.py (new): 43 tests - de-vig math + hypothesis property,
    name resolution, playoff windows, SBR/Kaggle/ESPN parsers on fixtures,
    missing-odds flagging, consolidation (prefer SBR, don't merge adjacent
    games), Parquet round-trip, live clients via httpx.MockTransport (no net),
    plus a real-file smoke test asserting SBR 2016-17 = 1317 games / 161 playoff.
  - ml/pyproject.toml + ml/uv.lock: added openpyxl>=3.1 (SBR .xlsx reader).
  - ml/README.md: full "Betting odds" section (sources, de-vig, playoff windows,
    consolidation, odds table schema, live clients) + command rows.
- Quality (from ml/): ruff check clean, ruff format --check clean, mypy --strict
  clean (20 files), pytest 135 passed. `oracle odds` on real archives => 36,017
  source rows -> 28,153 consolidated games, 7,864 cross-validated (mean fav-prob
  xval_delta 0.069). Real SBR counts match PROVENANCE (1317/1212/952/342 games).
- **Learnings for future iterations:**
  - The rsplit-based "city" (full_name minus last word) BREAKS two-word
    nicknames (Toronto Maple Leafs -> "Toronto Maple"), silently dropping ~19%
    of SBR games. Every franchise's city string is aliased explicitly; verify
    game counts against PROVENANCE §5 when touching name resolution.
  - MMDD->year: 2019-20 is the ONLY archive season with Aug/Sep games (the 2020
    bubble). The generic "months>=8 => earlier year" rule maps those to 2019;
    special-cased to 2020 in _reconstruct_date, else the bubble playoffs vanish.
  - Kaggle dates are UTC (night games land on the next calendar day), so
    cross-source consolidation needs a +/-1 day tolerance AND one-row-per-source
    clustering, else back-to-back same-matchup games get merged.
  - Windows PowerShell can't read the 47.9KB prd.json or 138KB SPEC listing in
    one `view`; use ConvertFrom-Json / grep / view_range.
  - ruff RUF002/RUF003 flag en-dashes (–) in docstrings/comments as ambiguous;
    keep ml/ source ASCII (box-drawing ─ and § are fine, en/em dashes are not).
  - No UI change; Playwright not applicable (ml/ is outside the Nx/web graph, so
    `yarn nx affected` is empty for these changes). Live-client network paths are
    covered only by MockTransport; a real ODDS_API_KEY smoke run needs manual
    verification (quota-limited, so intentionally not automated).
---

## 2026-08-27 23:25 - US-006
- Implemented draft_oracle.ingest.league_drafts: parses the committed league
  draft-history snapshots into a raw `league_picks` table (one row per drafted
  roster slot) + a `league_champions` table, per SCHEMA.md / OPEN_QUESTIONS.md.
- Files changed:
  - ml/src/draft_oracle/ingest/league_drafts.py (new): canonical_manager (Evi->levi
    + app usernames), slot_position, detect_draft_order (orientation-agnostic scan of
    the annotation area for a 4-manager run -> snake_slot; None when absent),
    parse_sheet_tab (block split via col-A merge, fail-loud header/block/label checks,
    R1 col-F overload disambiguation, R2/R3_4 status-vs-note in col H), _pair_ir_swaps
    (Dropped/Not-playing starter <-> same-position Activated IR), parse_wins_tab +
    build_champions (folds Evi, adds owner-confirmed 2026 Ben), parse_app_draft_order +
    parse_app_picks (preserve true pick_number; two leagues), build_league_drafts
    (writes both parquet + ingestion report; app export optional).
  - ml/src/draft_oracle/ingest/__init__.py: re-export the public API.
  - ml/src/draft_oracle/cli/project.py: `oracle league-drafts` command.
  - ml/tests/test_league_drafts.py (new): 35 tests vs committed snapshots asserting
    exact per-tab pick + flag counts, all documented corrections, snake_slot detection
    (vertical/horizontal/absent), champions, app pick_number/aliasing, fail-loud paths,
    and the missing-app-export path.
  - ml/README.md: "League drafts" section + command row.
- Quality (from ml/): ruff check clean, ruff format --check clean, mypy --strict clean
  (22 files), pytest 170 passed. `oracle league-drafts` => 552 total picks (312 sheet
  + 240 app), 9 champion seasons; per-tab counts match SCHEMA §8
  (36/36/36/44/44/44/36/36).
- **Learnings for future iterations:**
  - Column A is a MERGED cell per block: the manager name only on the first row. When
    splitting blocks, remember to include that first row in the block (it holds Forward 1)
    — off-by-one there silently drops one pick per block (caught: 32 vs 36).
  - R1 tabs overload column F (index 5): it holds EITHER a status flag OR the Hyman
    drafted-points mini-column. Disambiguate by content (numeric -> points, text -> flag).
  - The two sheet2 R3_4 IR swaps carry NO text flag in the CSV — they exist only in the
    xlsx Total formulas. They MUST be hardcoded (_UNFLAGGED_SWAPS) or the roster-as-scored
    is wrong. Never model this as a mid-round substitution mechanic (owner: one-time favor).
  - detect_draft_order scans the annotation area (col index >= 5) for a run of 4 manager
    tokens, vertical OR horizontal — the order list orientation differs per tab. Column A
    block labels are excluded by starting at col 5, so they can't be mistaken for an order.
  - Empty roster slots (2026's retired IR rows, the stray "," at sheet1 R1 C24) are NOT
    picks and are skipped, so sheet1 R1 = 36 picks (IR-less), not 44.
  - No UI change; Playwright not applicable (ml/ is outside the Nx/web graph, so
    `yarn nx affected` is empty for these changes).
---

## 2026-08-27 23:40 - US-007
- Implemented draft_oracle.ingest.entity_match: matches parsed league_picks to NHL
  player/team ids and normalizes managers, emitting the final league_draft_picks table.
- Files changed:
  - ml/src/draft_oracle/ingest/entity_match.py (new): normalize_name/name_tokens/
    last_name_key (accent + punctuation + initials folding); load_manager_aliases +
    resolve_manager (yaml-driven, evi=levi); load_name_overrides (NameOverrides);
    resolve_team (odds.resolve_team_id + nickname index, all-token scan);
    build_player_index + match_skater (override->exact->fuzzy>=0.88->unique-lastname->
    low-conf->unmatched, position-disambiguated); match_team; build_league_draft_picks
    (writes league_draft_picks.parquet + per-season SeasonMatchReport + review CSV).
  - ml/data/overrides/manager_aliases.yaml, name_overrides.yaml (new, committed, empty maps).
  - ml/src/draft_oracle/ingest/__init__.py: re-export entity_match public API.
  - ml/src/draft_oracle/cli/project.py: oracle match-drafts command.
  - ml/tests/test_entity_match.py (new): 49 tests (normalization, aliases, overrides,
    team resolution, fuzzy typos, last-name fallback, Aho collision, end-to-end build,
    review report, override gap-close, fail-loud).
  - ml/README.md: "Entity matching" section + command row.
- Quality (from ml/): ruff check clean, ruff format --check clean, mypy --strict clean
  (24 files), pytest 219 passed. oracle match-drafts => 552/552 picks matched (100.0%):
  108/108 (2024), 132/132 (2025), 312/312 (2026); 0 to review. Beats the >=98% target.
- **Learnings for future iterations:**
  - Goalie picks already have a parsed 	eam_name column in league_picks; match on that
    (not the "Team Goalie (Name)" free text). player_id stays null for G; team_id is set.
  - There are TWO Sebastian Aho (F CAR 8478427, D NYI 8480222) — position filtering in
    match_skater is REQUIRED, not optional, or the wrong id is assigned.
  - difflib.SequenceMatcher (stdlib, deterministic) is enough for the real typos
    (Pastrank/Petterson/Ekblad/Skjei/Montour ratios 0.92-0.97); no rapidfuzz dep needed.
  - Bare surnames (McDavid) score ~0.70 on full-string fuzzy — the unique last-name
    fallback catches them; ordered AFTER fuzzy>=0.88 so it only fires when needed.
  - pandas 3.0: Series | pythonlist raises; wrap the mask list in pd.Series first.
    itertuples values are Any to mypy — cast via a tiny _as_int helper, not bare int().
  - data/overrides/ is NOT gitignored (unlike data/raw,features,normalized), so the yaml
    config files are committed as intended by SPEC §4.
  - No UI change; Playwright not applicable (ml/ outside the Nx/web graph, affected empty).
---
