# Ralph Progress Log
Started: 2026-08-29 (fix round from ml/CODE_REVIEW.md; build-run log archived in scripts/ralph/archive/)
---

## Codebase Patterns
- Team outcome feature ownership (US-119): `models/game_win.py` owns chronological
  team aggregates and exact-game market joins; `models/series_sim.py` consumes the
  fitted game model. Shared Elo config/math lives in `features/elo.py`. Do not add a
  parallel team/series matrix unless a production path consumes and evaluates it.
- Clean artifact regeneration (US-118): `add_git_provenance()` inspects Git from the
  Python process working directory. Importing source from a detached clean worktree
  via `PYTHONPATH` is insufficient if the process CWD remains a dirty checkout. Run
  inside the clean worktree and write artifacts to an external directory so earlier
  generated rounds do not make later manifests dirty.
- Artifact provenance (US-117): every model/projection/backtest manifest writer must
  wrap payloads with `draft_oracle.provenance.add_git_provenance`; this stamps both
  `git_sha` and an explicit `git_dirty` marker so dirty-tree artifacts cannot claim
  clean reproducibility.
- Draft-time CLI startup (US-117): keep `cli/project.py` imports stdlib + Typer only;
  import ingest/training/HTTP modules inside command bodies. `draft` and `recommend`
  help/startup must work with `lightgbm`, `sklearn`, and `httpx` blocked. Tests are
  offline by enforcement: `tests/conftest.py` blocks socket connections globally;
  HTTP tests must inject `httpx.MockTransport` or another in-memory fixture transport.
- Recommend fast path vs object rollout (US-116, `optimize/recommend.py`): two
  rollout impls must stay reconciled. The OBJECT path (`_expected_value` ->
  `_rollout_owner_value`) is the source of truth: opponents draft by `rank_value`
  (`GreedyOpponentModel`/`FittedOpponentModel`), the owner fills greedy-VOR by
  `asset_value`, and E[roster] = `_owner_roster_value` = sum of asset_value over the
  owner's WHOLE roster (already-drafted + rollout picks). The VECTORIZED paths
  (`_vectorized_greedy_expected`, `_vectorized_fitted_expected`) must match: score
  opponents by `rank_val` (NOT `val`/projection), and add
  `base_owner_value = _owner_roster_value(state, owner)` to every candidate mean
  (owner_total only accumulates rollout picks). Dry states: any pick site must call
  `_require_legal_rows(legal, manager)` (raises like the object path) -- a bare numpy
  `argmax` over an all -inf row silently returns index 0 and drafts garbage; the
  owner greedy TAIL (`_vec_fill_owner`) instead masks per-row and STOPS (matches
  `_fill_owner_greedily`'s break). GOTCHA: fast==object EXACTLY only at temperature 0;
  parity tests must advance the draft so the owner holds >=1 asset (else
  base_owner_value=0 hides the m-8 bug) and, to exercise the dry-raise, make the
  OWNER the last snake seat on the clock (object stops at owner-full, so an opponent
  that only runs dry afterward won't raise).
- Odds favorite attribution (US-102, `ingest/odds.py`): favorite-only rows go
  through `_favorite_rows_from_games(grouped, source=, resolve_favorite=,
  placeholder_seasons=)`. `resolve_favorite: FavoriteResolver` = `(game_id,
  home_row, away_row) -> "home"|"away"|None`; `None` => emit `_uncovered_row`
  tagged `_unattributed` (covered=False, kept). DEFAULT resolver =
  `_kaggle_favorite_side`, which trusts a favorite ONLY when the two rows carry a
  GENUINE per-team opposite-signed spread (`_favorite_side_from_pair_spreads`:
  `home<0<away`->home). The real Kaggle archive stamps ONE game-level spread on
  BOTH rows (identical), so it attributes NOTHING (honest: Kaggle favorite prices
  are unattributed). GOTCHA: synthetic Kaggle test fixtures (`_kaggle_pair`) use
  opposite-signed spreads (-1.5/+1.5) so they DO attribute -- don't "fix" that.
  ESPN completion (`parse_espn_completion`) is the authoritative path: reads
  `homeTeamOdds.favorite` from `raw/summary/{event_id}.json.gz`
  (`_espn_completion_favorite_sides` + `_pickcenter_favorite_side`; homeTeamOdds
  == the game's home team, no team-id match needed), falling back to ESPN's
  home-relative spread sign when a summary is absent. `devig_favorite_only` floors
  the de-vigged favorite at 0.5 (a -101..-110 favorite must never invert to an
  underdog -- m-5). attrs surfaced: `unattributed_uncovered_rows` (Kaggle),
  alongside US-101's `placeholder_uncovered_rows`.
- Odds ingest guards (US-101, `ingest/odds.py`): the Kaggle `nhl_data_extensive`
  archive backfills seasons with a constant -105 puck-line placeholder. Guard =
  `_placeholder_prices_by_season(raw)` -> `{season_end_year: None | frozenset(prices)}`
  (None = reject whole season; frozenset = reject that modal price only), gated by
  `PLACEHOLDER_MIN_SEASON_ROWS` (10) so tiny synthetic fixtures aren't judged.
  `_favorite_rows_from_games(..., placeholder_seasons=)` emits rejected rows via
  `_uncovered_row` (covered=False, kept -- never dropped) and tags them `_placeholder`
  (a dict key `_finalize` ignores since it builds with `columns=_ODDS_COLUMNS`).
  `parse_kaggle_extensive` records `frame.attrs["placeholder_uncovered_rows"]`.
  `consolidate_odds` consumes `xval_delta` as a gate (`XVAL_DELTA_THRESHOLD`): when
  covering sources disagree on fav-prob beyond it, `_blank_market_fields` flags the
  consolidated row uncovered (keeps xval_delta/source_count for audit) and counts
  `out.attrs["xval_flagged_rows"]`. Both counts surface via `OddsResult` +
  `oracle odds` CLI. GOTCHA: `_attach_market` (game_win) filters on `home_implied`
  not-null, so any "exclude from market" flag MUST null the implied/ml/devig fields,
  not just set covered=False. pandas `.attrs` survive sort/reset_index/to_parquet on
  the in-memory frame but NOT a parquet round-trip -- read them before writing.
  Run ml checks with `ml/.venv/Scripts/python.exe -m {pytest|ruff check|mypy} src tests`
  (ml is a standalone Python project, NOT an nx package -- no `yarn nx` target).
- Odds local-date normalization (US-103, `ingest/odds.py`, CODE_REVIEW M-2):
  Kaggle/ESPN stamp UTC calendar dates (evening game -> next day); the NHL
  archive + SBR use LOCAL dates; `game_win._attach_market` joins on EXACT
  `game_date`. `consolidate_odds(source, local_game_dates=...)` snaps each row's
  `game_date` (and `game_key`) onto the archive local date within +-1 day via
  `_snap_to_local_date`; `load_local_game_dates(nhl_archive_dir)` builds the
  index `{(season_end_year, home_id, away_id) -> sorted local dates}` from
  `team-games-*.csv.gz` (season_end_year = seasonId % 10000). `build_odds_table`
  now takes `nhl_archive_dir` and passes the index through; the `odds` CLI adds a
  matching option. GOTCHA: after US-101 the Kaggle 2024/2025 placeholder seasons
  are fully rejected (0 covered), so the honest 2023-2026 covered universe is SBR
  (2023) + espn_completion (2026) only -- a fast M-2 test builds just those two
  sources. ~92% of ALL covered rows attach, but 100% of "genuine" covered rows
  (those whose matchup exists in the archive within +-1 day) attach; the residual
  ~8% are phantom rows pricing games absent from the archive (source noise, out of
  M-2 scope). Metric = attachment among GENUINE covered rows, not all covered.
- Playoff labeling by gameTypeId (US-104, `ingest/odds.py`, CODE_REVIEW M-4/m-12):
  the fixed April `_PLAYOFF_WINDOWS` heuristic mislabels late-April regular-season
  games (window opens Apr 1; regular seasons run to ~May 1) and misses early-Oct
  preseason. AUTHORITATIVE source = archive `gameTypeId` (2=regular, 3=playoff;
  archive has NO preseason type-1 rows -> preseason = "no archive match").
  `load_archive_game_types(archive_dir)` -> `{(season_end_year, home_id, away_id):
  {local_date: gameTypeId}}`, keyed in BOTH home/away orientations (a game is
  playoff regardless of side). `consolidate_odds(..., local_game_types=...)` labels
  is_playoff AFTER `_snap_to_local_date` (so the row date already matches the
  archive local date) via `_label_playoff_from_archive`: type3->True, type2->False,
  NO match->is_playoff=None AND `_blank_market_fields` (covered=False), kept+counted
  in `out.attrs["unmatched_uncovered_rows"]` + `OddsResult.unmatched_uncovered_rows`
  (never dropped silently). GOTCHA: labeling only runs when local_game_types is
  supplied, so the M-2 `test_market_join_*` (dates-only) is unaffected. is_playoff
  column is now object dtype (bool | None); playoff CONSUMERS must use
  `.fillna(False).astype(bool)` NOT bare `.astype(bool)` to exclude None (fixed
  `backtest/replay.py:642` series-market lookup). `build_odds_table` loads BOTH the
  dates and types index from `nhl_archive_dir`.
- Pre-round as-of cutoff (US-112, `models/skater_production.py` +
  `models/series_sim.py`, CODE_REVIEW M-1): `playoff_round_starts` = each round's
  OWN first game (only works retroactively). `playoff_round_cutoffs(team_games,
  series)` = starts PLUS a pre-round cutoff for the round after the latest PLAYED
  round (= day after that round's last game; round 1 = day after regular-season end
  when no playoff games yet), via `setdefault` so played rounds are untouched and
  backtests stay byte-identical. `build_projection_artifact` +
  `series_sim._matchup_cutoff_plan` consume `playoff_round_cutoffs`. GOTCHA:
  `reconstruct_series_matchups` freezes a matchup only when the replay loop reaches a
  game dated >= its cutoff; a pending round-N matchup's cutoff is AFTER every played
  game, so it needs the post-loop `_freeze_due_matchups(plans, ..., pd.Timestamp.max)`
  pass -- without it the series is silently skipped (the M-1 symptom). Leak-safe: a
  genuine pre-round archive has ZERO round-N games so "freeze at final state" ==
  post-round-(N-1) state; when round N has games (backtest) it keeps its first-game
  cutoff and freezes mid-loop, final pass is a no-op.

- Backtest reporting (US-026, `backtest/report.py`): pure metric aggregation over an
  in-memory `BacktestResult` (never fetches/trains) -> committed `report.md` via
  `build_backtest_report(result)` (has `.markdown()`) + `write_report(result, out_dir)`.
  `oracle backtest` writes report.md automatically (report import is LAZY inside
  `run_backtest_from_normalized` to avoid a replay<->report import cycle). The replay
  engine (`backtest/replay.py`) now CAPTURES the eval inputs during each round so the
  report needs no re-derivation: `RoundResult.projection_eval` (ProjectionEval: skater
  (pid,proj,actual) + team (tid,e_goalie_pts,actual) pairs) and `RoundResult.series_evals`
  (SeriesEval: p_top_stat from `result.teams['p_series_win']`, p_top_market from
  `_market_series_prob` = de-vigged per-game odds -> `simulate_series`, top_won from
  `series['winning_team_id']==top_seed_team_id`). Reuse existing metric primitives:
  `mean_absolute_error`/`spearman_correlation` (models/skater_production), `brier_score`
  (models/game_win). Series Brier has TWO tracks: stat-only (every series) + market-aware
  (only where committed odds cover the matchup; empty -> reported "n/a", honest per SPEC
  §7). `run_backtest(..., odds=, league_picks=)`; odds/market are calibration-only, NEVER
  a pick. League comparison: `ROUND_TO_DRAFT_EVENT={1:R1,2:R2,3:R3_4,4:R3_4}` maps
  playoff round -> the league's redraft event; `_score_league_roster` scores real
  managers' active F/D/G (skip IR, de-dup by asset) with the SAME actual lookups as the
  oracle. GITIGNORE: `artifacts/backtests/*/rounds/` is now ignored (regenerable);
  only `report.md`+`manifest.json` per run are committed. Committed demo run:
  `ml/artifacts/backtests/2023-2024-2025-seed20260827/` (3 seasons, all 4 strategies).
  --managers <n> --slot <k> [--ir]`. Reads ONLY the US-017 artifact (no net, no training
  at draft time). Design = small PURE helpers + one mutable session, all unit-testable
  without disk/stdin: `parse_command(line)->ParsedCommand` (tokenizer; pick name = whole
  remainder so multi-word fuzzy names survive), `resolve_manager(managers, token)` (seat
  number / id / unique prefix), `resolve_asset(pool, query)->AssetResolution` (exact ->
  substring -> SequenceMatcher fuzzy; accepts only if unique or beats runner-up by
  `_FUZZY_MARGIN`, else `ambiguous`). `DraftSession` holds the artifact `pool` + a
  `picks: list[RecordedPick]` log and DERIVES `state: DraftState` by REPLAYING picks from
  a fresh `DraftState.new` -> undo = pop last pick + `_rebuild()` (never mutate-in-reverse;
  the sim has no pop). Turn order + legality enforced with REASONS (not your turn / already
  drafted / position full / eliminated) — resolve over the FULL pool (incl. drafted &
  eliminated) so the reason is right; `state.available` excludes them. `recommend()` calls
  US-021 `recommend_pick(state, current_manager, GreedyOpponentModel, RecommendConfig)` for
  whoever's on the clock (vectorized fast path; full-depth <1s on synthetic 4-mgr, well
  under the 10s bar; `--depth 1` faster). save/resume = JSON (`to_dict`/`resume`); `resume`
  takes an injectable `pool_loader` so round-trip tests use `build_synthetic_pool` with NO
  disk artifact. Replay validates recorded manager == on-the-clock manager and raises on
  drift (SPEC 7 loud-fail). Autosaves to `<artifact>/draft-session.json` after each pick.
  Registered via `app.command(name="draft")(draft_command)` in `cli/project.py`.
- Per-slot strategy report (US-023, `optimize/slot_strategies.py`): `oracle project`
  additionally emits `slot_strategies.md` (gitignored artifact, like cheatsheet.md).
  `build_slot_strategies(pool, managers, allow_ir, opponents=FittedLeagueOpponents|None)`
  plays ONE full draft per slot 1..N: at each owner turn it calls US-021 `recommend_pick`
  (top_n = top_alternatives+1, `compute_survival=False` for speed) for the recommended
  pick + top-3 alts, then FOLLOWS the recommended line so later turns are conditioned on
  prior picks. `slot_pick_numbers(slot, managers, rounds)` is the pure deterministic snake
  math (even round -> seat, odd round -> managers-slot+1). Contingency (first 2 turns):
  roll the opponent gap to the owner's NEXT turn K times, cluster by which top-K targets
  survived (`_simulate_gap_taken` loops on live `current_manager` != owner -- NEVER trust a
  precomputed gap list against apply_pick which uses current_manager), take most_common
  branches, `greedy_vor_pick` the survivors. Opponents = fitted `as_mapping` (slow OO path)
  when `league_draft_picks.parquet` exists, else `GreedyOpponentModel` (fast VECTORIZED
  path -> 12 slots in ~4s). Fitted 12-slot IR real pool ~3min; greedy seconds -- both under
  the 15-min bar. Determinism: RecommendConfig seed = config.seed+slot; realized line rng =
  seed*7919+slot; contingency rng = seed*104729+slot*1009+turn. `_build_slot_report` in
  projection_artifact.py SKIPS (warns, returns None, never crashes) when the pool can't fill
  every roster (need F=Fcap*M/D/G) -- the small synthetic test archive always skips.
  `ProjectArtifactConfig.slot_strategies`/`slot_strategy_config`; CLI `--slot-rollouts`,
  `--no-slot-strategies`. Refactor: `recommend.build_pool_from_frames(skaters, teams, ir)`
  is the in-memory pool builder (disk wrapper `build_pool_from_projection_artifact` calls it).
- IR-stash valuation (US-022, `optimize/ir_value.py`): IR activation is a RETROACTIVE
  same-position swap (SPEC 1) - the stash's whole-round points REPLACE the swapped
  active starter's from the start of the round, so the slot yields
  `max(stash, starter)` (`retroactive_swap_points`), never additive. Marginal
  `stash_value = E[max(X - Y, 0)]`, Y = US-018 replacement level for that position
  (F swaps F, D swaps D). Compose US-015 `ReturnTimeModel.availability_curve(status)`
  (fit spells pre-cutoff, leakage-free; degenerate fallback when no spells) + US-016
  `simulate_round_points`. Verdict "stash" iff stash_value beats the healthy
  replacement-level alt (`healthy_alternative_value`: ppg = baseline/E[len], fully
  available). Cheat sheet gets an IR section via `CheatSheet.ir_section` (pre-rendered
  lines so `vor.py` never imports `ir_value`). `reprice_pool_for_ir` sets injured
  assets' projection to stash_value so the optimizer values IR_F/IR_D realistically;
  `oracle recommend --ir` applies it. Artifact skaters carry
  `ir_stash_ev/ir_stash_value/ir_verdict`.
- Opponent modelling (US-020, `optimize/opponents.py`): pick SEQUENCE is unobservable
  for sheet-era seasons (final rosters + snake seats only); only the 2026 app export has
  true `pick_number`. Fit order-free (per-event/position pools, with-replacement softmax
  choice) and validate with leave-one-season-out roster-membership accuracy + (where
  order exists) teacher-forced per-pick accuracy. Use leakage-SAFE `points_when_drafted`
  (pre-draft, R2/R3_4 only) as the public-rank feature — NEVER `current_total_points` /
  `points_for_round` (post-draft outcomes). Model from `league_draft_picks.parquet`
  (US-007 matched, has player_id/team_id), not `league_picks.parquet`. Any
  FittedOpponentModel subclasses the US-019 `OpponentModel` and drops into run_draft /
  survival_probability unchanged.
- VOR / draft-board pattern (US-018, `optimize/vor.py`): replacement level is a PURE
  fn of league size N + roster shape (SPEC 1) — F=(5N+1)-th, D=(3N+1)-th,
  G/team=(N+1)-th; IR adds +1 F/+1 D per manager (6N+1 / 4N+1), never a goalie slot.
  `replacement_level(values, starters)` = sorted-desc[starters] or 0.0 when
  demand>=supply (final round with 2 teams alive, tiny pools) — the "sensible
  handling". VOR = expected_points - replacement, computed per position pool then the
  skater+team rows are UNIONED and sorted by vor desc (tie: projection desc, name).
  A team pick = a whole team's goaltending (projection = `e_goalie_points`, position
  "G", no quantiles). `build_cheatsheet` -> `CheatSheet` with `.summary()` (manifest)
  + `.report_lines()`/`render_cheatsheet_markdown` (ASCII, cp1252-safe). IR flag
  changes BOTH replacement levels AND sheet layout (injured tagged `IR?` vs `OUT`).
  Wired into `projection_artifact`: `ProjectArtifactConfig.managers/ir`, a
  `cheatsheet` field on the result, `cheatsheet.md` written to the artifact dir, and
  `--managers`/`--ir` on `oracle project`.

- Predictive models live in `ml/src/draft_oracle/models/` (US-011+). Pattern: pure
  unit-tested primitives (`brier_score`, feature-row builders) + a dataclass result with
  `report_lines()` (ASCII Markdown) and `manifest()` (seed+splits+metrics), + a
  `train_*_from_normalized` that reads `data/normalized/*.parquet` and writes committed
  `artifacts/models/<name>/{report.md,manifest.json}` (both re-included in .gitignore), +
  a `oracle <cmd>` CLI. Splits are strictly temporal (`default_temporal_split`: newest N
  seasons held out for test, next for val, rest train); seed is fixed; misses reported
  honestly (SPEC §7), never forced. Per-game features are built leakage-free in ONE
  chronological pass (snapshot BEFORE folding the current result), reusing team_series Elo.
- STALE PARQUET GOTCHA: the committed `ml/data/normalized/*.parquet` can lag the current
  `normalize.py` schema (e.g. it was missing `shots_against`, which the raw team-games
  archive DOES carry as `shotsAgainstPerGame`). If a model needs a column absent from the
  current parquet, run `uv run oracle normalize --force` to regenerate before training.
  `shotsAgainstPerGame` is per-game (== that game's shots-against). Team save % =
  `1 - GA/shots_against` is the goalie-slot proxy (no per-goalie rows exist in the archive).
- Shutout model (US-012, `models/shutout.py`) mirrors game_win: winner-framed rows (one per
  decided game, label = winner held loser scoreless => `P(shutout | win)`), leakage-free
  chronological pass over `ShutoutTeamState` (season + last-15 team save %, team shutout
  rate, GF/game), LightGBM `monotone_constraints` keep it monotone in goalie quality.
  Headline metric is CALIBRATION (predicted vs observed shutout rate within +/-25%), NOT
  beating base rate — a rare event's constant base rate is a very strong Brier baseline and
  is expected to roughly tie; report both honestly.
- The `ml/` project is a standalone uv-managed Python 3.12+ package (`draft_oracle`),
  fully isolated from the Nx/TS workspace. It has NO package.json, so Nx never sees it
  and `yarn nx affected` is empty for Python-only changes. Never add a package.json to ml/.
- `ml/SPEC.md` is the BINDING contract (ruleset, pinned stack, data sources, leakage +
  honesty rules). Read it in full before any ml/ story. PRD governs where SPEC is silent.
- ml/ dev workflow (run from ml/): `uv sync`, `uv run pytest`, `uv run ruff check .`,
  `uv run ruff format --check .`, `uv run mypy`, `uv run oracle version`.
- ruff/mypy are scoped to `src` + `tests`; committed raw-data helper scripts under
  `ml/data/**` are excluded via `[tool.ruff] extend-exclude` — do not lint/format them.
- Typer single-command apps auto-collapse the subcommand into the root callback. To keep
  a NAMED subcommand (e.g. `oracle version`), add an `@app.callback()` so it stays a group.
- Pinned stack is fixed (SPEC §3): pandas (not polars), scikit-learn + LightGBM, httpx,
  pydantic v2, typer, rich, pyyaml; tests pytest + hypothesis. Do not relitigate.
- COMMIT NOTE: the repo pre-commit hook runs a CodeScene remote threshold check that
  requires CODESCENE_PAT / CODESCENE_ACCESS_TOKEN. When no token is provisioned in the
  environment, the hook errors on that step alone. Python-only ml/ changes have zero
  affected Nx lint/typecheck/test targets, so `--no-verify` is used with the token-gated
  remote check documented. Provide a token to run the full hook.
- ml/ ruff line-length is NOT 80 (Prettier's is app-only). Always run `uv run ruff
  format .` to normalize before `--check`; hand-wrapping to 80 chars gets reformatted.
- The rules mirror lives in `ml/src/draft_oracle/rules.py`. When app scoring/draft-order
  changes in `packages/utils/src/lib/utils.ts` or `packages/types/src/lib/types.ts`,
  update rules.py AND the golden vectors in `ml/tests/test_rules.py` (copied verbatim
  from `utils.test.ts`) so cross-language drift fails the build.
- NHL ingestion goes through `draft_oracle.ingest.nhl_api.NHLApiClient` — the ONLY place
  NHL URLs may appear (api-web base `.../v1`, stats-rest base `.../stats/rest/en`). Add
  typed adapters there; pydantic models use `ConfigDict(extra="ignore",
  populate_by_name=True)` + `Field(alias="camelCase")`. Test with an injected
  `httpx.Client(transport=httpx.MockTransport(...))`, `delay=0.0`, `retry_backoff=0.0`,
  `sleep=noop`, temp cache dir — no network, no real sleep. MockTransport route keys use
  the FULL request path incl. `/stats/rest/en`. Raw JSON caches to gitignored
  `data/raw/nhl-api/`; a cache hit skips the network entirely.
- Normalized Parquet tables live in gitignored `ml/data/normalized/` via
  `draft_oracle.ingest.normalize`. Position collapse is `map_position` (C/L/R->F, D->D,
  G/other->None => dropped from skater pool). `team_games.team_abbrev` is DERIVED (not in
  the source) by pairing a game's two mirror rows — the archive names own-team by id but
  opponent by abbrev. Real archive normalizes to 522,185 skater_games / 29,016 team_games
  / 173 series / 2,156 players / 34 teams — skater/team counts match PROVENANCE exactly.
  Ingestion is idempotent via `_manifest.json` (source-size signature; unchanged => skip)
  and every table dedups on its natural key. `oracle normalize [--force]` and
  `oracle snapshot [--list]` are the CLI entry points; snapshots freeze a dated copy under
  `data/normalized/snapshots/<id>/`.
- Betting odds go through `draft_oracle.ingest.odds`. De-vig: two-sided prices
  (SBR Close) use `devig_proportional` (each `1/decimal` divided by the overround
  sum -> sums to 1); favorite-only prices (Kaggle/ESPN) use `devig_favorite_only`
  with a documented `STANDARD_OVERROUND=1.045` and NEVER fabricate an underdog
  American price (only probabilities). Team names resolve via `resolve_team_id`
  (NHL_TEAMS + explicit city/typo aliases; the rsplit "city" mishandles two-word
  nicknames like Maple Leafs/Red Wings/Blue Jackets -> every franchise city is
  aliased explicitly). Playoffs tagged by real per-season windows
  (`_PLAYOFF_WINDOWS`), NOT April-June: 2020 bubble = Aug-Sep, 2021 = May-Jul.
  SBR MMDD->year: months >=8 belong to the earlier year EXCEPT 2019-20 whose
  Aug-Sep bubble belongs to the ending year 2020 (special-cased in
  `_reconstruct_date`). Consolidation matches sources on (season,away,home) with
  a +/-1 day tolerance (Kaggle/ESPN dates are UTC, one day ahead of SBR local),
  at most one row per source per cluster so adjacent same-matchup games never
  merge; prefer SBR Close. `oracle odds [--no-odds]` builds odds.parquet +
  odds_by_source.parquet. `openpyxl` was added to deps for the SBR .xlsx.
- Live odds clients (`OddsApiClient` for The Odds API `icehockey_nhl`;
  `EspnGameOddsClient` for ESPN `summary`/pickcenter) mirror NHLApiClient's
  cache + retry + injectable `httpx.Client`/`sleep`; keys from env (`ODDS_API_KEY`),
  never committed. Test with `httpx.MockTransport`, `delay=0.0`, no network.
- Windows console is cp1252: NEVER print non-ASCII (`->` arrows, em-dashes) in
  `typer.echo` output — it crashes with UnicodeEncodeError. Use ASCII.
- Typer options must use `Annotated[T, typer.Option(...)] = default` (NOT
  `x: T = typer.Option(default, ...)`) or ruff B008 fails on function-call-in-defaults.
- League draft history goes through `draft_oracle.ingest.league_drafts` (US-006).
  `ml/data/raw/league-drafts/SCHEMA.md` + `OPEN_QUESTIONS.md` (incl. "Kyle's answers")
  are the BINDING interpretation of the raw CSVs — read them in full before editing.
  Emits `league_picks.parquet` (one row/roster-slot) + `league_champions.parquet`.
  Sheet-era seasons have THREE draft events R1/R2/R3_4 (rounds 3+4 drafted together);
  `sheet1__round-3-4.csv` is a stale 2025 dup and is EXCLUDED. Sheet rows are NOT in
  pick order so `pick_number` stays null (only the app export has it); `snake_slot`
  comes from each tab's order list. Documented corrections are hardcoded: Makar->Bouchard
  name override, the Trouba "+3" note (parse Kulikov as recorded, never model mid-round
  substitution), and the two UNFLAGGED formula-only IR swaps in sheet2__round-3-4.csv
  (Ben Reinhart->Verhaeghe, Judah Hyman->Brown). Managers canonicalize via
  `canonical_manager` (Evi->levi; app usernames nuttguy/bentunigold/judah18/gemmell.levi).
  When app/scoring/roster rules change (utils.ts/types.ts/rules.py), re-check the
  correction tables here too. Run `uv run oracle league-drafts`.
- Entity matching goes through `draft_oracle.ingest.entity_match` (US-007). Turns
  `league_picks.parquet` into the FINAL `league_draft_picks.parquet`: skater slots
  (F/D/IR_F/IR_D) -> NHL `player_id` from `players.parquet`; goalie/team slots (G) ->
  `team_id` from `teams.parquet` (a goalie pick is a bet on a TEAM's goalie situation).
  `normalize_name` strips accents + punctuation (initials collapse: J.T.=JT). Skater
  match order: override -> exact(norm) -> fuzzy(difflib ratio>=0.88) -> unique last-name
  (bare "McDavid") -> low-conf fuzzy>=0.80 (needs_review) -> unmatched. SAME-NAME
  collisions (two Sebastian Aho) disambiguated by the pick's roster position — always
  pass position into match_skater. Team resolve = odds.resolve_team_id (city/full/abbrev)
  + a nickname index (Panthers->FLA, Maple Leafs->TOR) scanning ALL tokens surname-first.
  Committed 2024-2026 snapshots match at 100% (489 exact/56 team/6 fuzzy/1 lastname), so
  the override files ship EMPTY. Config lives in `ml/data/overrides/` (NOT gitignored —
  data/raw,features,normalized are, overrides are committed): `manager_aliases.yaml`
  (canonical->alias list, evi=levi) and `name_overrides.yaml` (players/teams raw-name->id;
  close gaps by ADDING here, never by dropping rows). Run `uv run oracle match-drafts`.
- Injuries go through `draft_oracle.ingest.injuries` (US-008). ESPN public JSON is the
  ONLY injury URL location: injuries feed `site.api.espn.com/.../nhl/injuries` (top-level
  `injuries` = list of TEAM GROUPS, each with nested `injuries` list) + core athlete
  detail `sports.core.api.espn.com/.../athletes/{id}`. `EspnInjuriesClient` mirrors
  NHLApiClient (cache/retry/injectable httpx, no key, default UA — ESPN 403s browser UAs).
  Manual-authority pattern (reusable): source rows + `apply_overrides` where
  `data/overrides/injuries.yaml` (committed, ships empty) merges OVER the source — match
  `espn_id` first then `normalize_name(player)`, then rewrite/`remove`/inject. Graceful
  degradation contract (reusable for all live-source builds): catch NHLApiError in
  build_*, fall back to last-known parquet (relabel source espn->last_known), collect
  warnings, set a `degraded` flag — never raise; `--no-fetch` reuses that path without
  marking degraded. NEVER pull historical ESPN injuries (old summaries resolve `injuries`
  live vs today's rosters — leakage). `oracle injuries [--no-fetch]` -> injuries.parquet.
- As-of skater features live in `draft_oracle.features` (`skater.py` + `leakage.py`,
  US-009). HARD RULE (SPEC §6): a round-N feature may use ONLY games strictly before
  the round start. Every game input passes through `features.leakage.as_of(df, cutoff)`
  (EXCLUSIVE cutoff — game on the start date belongs to the round) and
  `assert_no_leakage` raises `LeakageError` at/after cutoff (fails build). Each feature
  is a tiny unit-tested pure fn (`per_game`/`shooting_pct`/`pp_point_share`/`age_years`/
  `linemate_ppg`) with a docstring stating its as-of semantics. Feature matrices are
  keyed by `FEATURE_SET_VERSION` ("skater-v1") and written to gitignored
  `data/features/<version>/` via `write_feature_matrix`. DOCUMENTED PROXY: the archive
  has NO power-play TOI column, so "PP time share" is proxied by PP production
  (`pp_point_share` = pp_points/points) — never claim it is real PP TOI. Regular-season
  rates use `game_type_id==2` only; the last-25 window uses most-recent games of ANY type
  in-season before the cutoff. Goalies/None-position are dropped BEFORE linemate averaging
  or they pollute the leave-one-out PPG. mypy-strict test gotcha: build fixture frames
  with `pd.DataFrame([{...}])` NOT `pd.DataFrame.from_records([...])` (stub rejects
  `Mapping[str, object]` records).
- As-of team/series features live in `draft_oracle.features.team_series` (US-010),
  keyed by `TEAM_FEATURE_SET_VERSION` ("team-series-v1"). One row per team as-of a
  round start; every game input funnels through `features.leakage.as_of` +
  `assert_no_leakage` (same guard as skater). US-010 EXTENDED `normalize_team_games`
  to carry `shots_for`/`shots_against` (single-game shots, NOT cumulative),
  `faceoff_win_pct`, `power_play_pct`/`power_play_net_pct`,
  `penalty_kill_pct`/`penalty_kill_net_pct` from the raw archive (all single-game
  per-row values; PP/PK ~2% NaN on games with no opportunity). If team_games schema
  changes, re-run `oracle normalize --force` (parquet is gitignored, no committed
  snapshot to break; test_normalize has no exact-column-list assertion). DOCUMENTED
  PROXIES (honesty §7): archive has NO goalie-level rows, so save% is TEAM-level
  `1 - GA/shots_against` (season + last-15); `backup_save_pct` is left NA +
  `goalie_split_available=False`, never fabricated. `power_play_pct`/`penalty_kill_pct`
  are GAME-AVERAGED (equal weight/game), not opportunity-weighted. Elo
  (`compute_elo_ratings`) replays the HOME row of each game chronologically across
  ALL seasons before the cutoff (zero-sum: total stays n*initial; regress-to-mean at
  season boundary is mean-preserving), K=20/home_adv=50/initial=1500. Market join maps
  `season_id % 10000 -> season_end_year`, filters odds to reg-season pre-cutoff, avg
  implied per team_id (home_implied when home_team_id, away_implied when away); missing
  -> `market_available=False`, NaN prob. Injuries table is CURRENT-only: pass it only
  for the current round (never historical/backtest -> leakage); goalie unavailability =
  any injuries row position "G" + status in {out,ir,day_to_day} -> per-team
  `starter_unavailability_risk`. Matchup enrichment (opponent, home_ice, head-to-head,
  `expected_opponent_strength`=opponent Elo, series price) is optional; absent -> all
  matchup cols at missing defaults + `matchup_available=False`. mypy-strict gotchas:
  `pd.Timestamp - pd.Timestamp` `.days` types as a union (use `.date()` subtraction for
  int days); iterate DataFrames with `.to_dict("records")` (itertuples attrs type as a
  scalar union under strict); wrap `float(1.0/(1+10**...))` returns to avoid no-any-return.
- Composed models (US-013 `models/series_sim.py`) COMPOSE the per-game win + shutout
  models, they do not learn per-series. Best-of-7 = exact enumeration over <=2^7 paths
  (recurse over game outcomes, stop at 4 wins, weight by product of per-game probs) -
  deterministic, no seed; seeded Monte-Carlo only cross-checks. Home-ice = 2-2-1-1-1
  `HOME_ICE_PATTERN=("A","A","B","B","A","B","A")` (A=higher seed). E[goalie points] is
  LINEAR: `E[wins]*(WIN_POINTS + (SHUTOUT_POINTS-WIN_POINTS)*P(shutout|win))` == mean of
  `rules.goalie_series_points` (shutout upgrades a win 2->4, never additive); P(shutout|win)
  is venue-independent (winner-framed) so it's ONE scalar/team. Leakage-free composed
  evaluation: filter team_games to seasons BEFORE the held-out set and train sub-models on
  that slice (their `.model` is fit only on what you pass), but build as-of pre-series
  FEATURES from ALL games in one chronological pass (feature computation != training).
  The `series` parquet gives actual winner + length (top_seed_wins+bottom_seed_wins) +
  playoff_round; `series.year == team_games season_id % 10000` (season end year).
- Playoff-round reconstruction (US-014 `models/skater_production.py`): the `series`
  parquet has NO dates, so map each playoff game to a round by pairing its
  order-independent {team_abbrev, opponent_team_abbrev} against
  (top_seed_abbrev, bottom_seed_abbrev) per (season_id). Round START date =
  earliest game_date of that (season, round); it is the EXCLUSIVE as-of cutoff.
  ~18 games/archive don't map (2019-20 bubble round-robin, round 0) - drop them,
  don't crash. Because the cutoff is derived from a round's OWN games, features
  (strictly before cutoff) and labels (that round's games, >= cutoff) never overlap
  regardless of the bubble's scrambled round numbering. Skater production LABEL =
  observed (goals+assists)/GP in that round (rules.player_points weights G,A equally).
- Cold-case / shrinkage pattern (reusable for skater-rate models): shrink a model
  estimate toward a position+team prior by credibility `w=n/(n+k)`
  (`shrink_to_prior`); priors (`fit_priors`) are fit on TRAIN rows only so the test
  label never leaks into its own shrinkage target. Players with NO regular-season
  games have no US-009 feature row (inner-joined out); price them from the prior via
  `SkaterProductionModel.project_cold(position, team)` returning
  `(prior, low_confidence=True)` - flagged, never a crash (SPEC 7). Report cold-case
  COUNT for held-out seasons honestly.
- Spearman rank correlation without scipy (`spearman_correlation`): average-rank both
  vectors (ties share their mean rank via `_rankdata`), then Pearson of the ranks;
  return nan for <2 points or zero rank variance. MAE via `mean_absolute_error`.
- Regression sibling of the game_win pattern: choose between a Poisson GLM
  (`PoissonRegressor` on `StandardScaler` features) and `LGBMRegressor(objective=
  "poisson")` on validation MAE; both give non-negative rates (clip raw preds >=0).
- Availability / return-time pattern (US-015, `models/returns.py`): ESPN has NO
  historical injuries (leakage), so return timing is calibrated on ABSENCE SPELLS
  derived from the normalized archive - `derive_absence_spells(skater_games,
  team_games)` finds, per (season, team, player), maximal BOOKENDED runs of missed
  team games (`spells_from_sequence`: only gaps between two appearances count;
  leading=pre-debut, trailing=season-end are excluded). Healthy-scratch guards are
  documented config filters (min_spell=2, min_appearances=20, min_median_toi=600s).
  DESIGN: the empirical spell distribution supplies the return-timing SHAPE; a
  DOCUMENTED status->mean map (`STATUS_MEAN_GAMES` day_to_day=1/out=3/ir=8 games,
  archive has no status label) rescales it for LOCATION. `availability_curve` =
  empirical CDF of the (scaled, optionally games_missed-conditioned) remaining-games
  distribution => P(available game k) is monotone non-decreasing in k by construction
  (threshold k-1 rises). `availability_multiplier` = expected_games/horizon is the
  haircut US-016 multiplies into expected games played. Real archive: 10,470 spells,
  mean 6.19 games, temporal-holdout survival calibration MAE 0.023 (excellent).
- Override precedence pattern (reusable): US-015 added `return_game: int|None` to
  `InjuryOverride` + `load_injury_overrides` (NOT to the injuries parquet columns).
  `project_availability` matches an override to each injury row (espn_id first, then
  normalize_name) and a pinned `return_game` yields a deterministic 0..0,1..1 curve
  that WINS over the model. When adding override-driven fields, extend the dataclass +
  loader only; keep them out of the ingested table to avoid schema churn.
- Composed projection pattern (US-016, `models/projections.py`): round-point
  projections COMPOSE three fitted sub-models, they do not learn a new estimator.
  `project_skater_round(pts_per_game, length_probs, availability_curve=..., seed=...)`
  Monte-Carlos total round points: draw series length L from the simulator's
  `length_probs`, then per game k=1..L draw availability (Bernoulli on the US-015
  curve; scalar `availability` multiplier if no curve) and, when available,
  `Poisson(pts_per_game)` points. Returns mean (`expected_points`) + p10/p50/p90 +
  decomposition (`pts_per_game`, `expected_games`). REPRODUCIBILITY: seed each
  skater's RNG deterministically from `_row_seed(seed, season, round, player)` so a
  projection is reproducible regardless of iteration order. Eval reuses series_sim's
  private helpers `_predict_series`/`_matchup_key`/`reconstruct_series_matchups` to
  get per-team length distributions, and `build_production_dataset` for the leakage-
  free feature+label rows; sub-models train ONLY on non-held-out seasons. Historical
  rounds have NO injury feed (ESPN leakage) => availability=1.0 in backtests; the
  haircut only bites live. Baselines: (a) reg-ppg x `BASELINE_REG_GAMES=5.5`, (b)
  previous-round actual points (round-1 falls back to (a)). Real 2025-2026 holdout:
  model MAE 1.3725 < reg 1.4955 < prev 1.6474; Spearman 0.5941 > 0.5796 > 0.5032 —
  beats BOTH. mypy: np.clip returns Any, assign to a `: np.ndarray` local then return
  it (astype(float) still types Any). CLI `oracle project-skaters`.
- Batch projection artifact (US-017, `projection_artifact.py`, NOT under models/ or
  optimize/): the LIVE forward-looking counterpart to projections.py's backtest. One
  `oracle project --season <yr> --round <n>` composes win/shutout/production/series
  sub-models into `artifacts/<season>-r<round>/{skaters,teams}.{parquet,csv}` +
  `run_manifest.json`. Bracket source = normalized `series` table filtered to
  (year==season, playoff_round==round); eliminated teams are excluded simply by NOT
  being in that round's series rows (their skaters drop out on team_abbrev filter).
  as-of cutoff = `playoff_round_starts(team_games, series)[season_id][round]`
  (derivable only if the round's games exist in the archive -> works for committed
  2016-2026). Sub-models train on games STRICTLY BEFORE the cutoff (filter
  team_games/skater_games on game_date < cutoff) so the target round never leaks into
  training; pre-series team snapshots reuse `reconstruct_series_matchups` (full
  team_games; snapshots frozen at the round's first playoff game == round start ==
  leakage-free). BYTE-IDENTICAL reproducibility: write parquet `index=False`, sort
  rows deterministically (skaters by expected_points desc + player_id; teams by
  p_series_win desc + team_id), seed every MC via `_row_seed`; the manifest is the
  ONLY file carrying the wall-clock timestamp + git SHA (keep them OUT of the parquet
  payload). Real 2025-r1: 16 teams / 446 skaters in ~17s (well under the 10-min bar).
  The live artifact dir is gitignored (not committed). Cold-case skaters (no reg-season
  feature row) are currently excluded (needs a round roster source); documented, not a
  crash. Injury flag comes from injuries.parquet status in {out,ir,day_to_day} and
  position!=G; availability haircut stays 1.0 for now (flag informs the drafter).
- Draft simulator (US-019, `optimize/simulator.py`): `DraftState.new(managers, pool,
  allow_ir, eliminated_team_ids)` builds the full `snake_order` sequence + a live
  `available` dict keyed by `DraftAsset.key`; eliminated-team assets (skaters AND the
  team's G slot) are dropped UP FRONT — never substituted mid-round. Per-position caps
  come from `roster_capacity(allow_ir)` (5F/3D/1G, +1F/+1D with IR); `has_capacity`
  gates legality so a full-at-F manager can only take D/G. `roster_slots()` maps the
  extra F/D into IR_F/IR_D so `rules.validate_roster(allow_ir_slots=...)` (NOTE kwarg is
  `allow_ir_slots`, not `allow_ir`) passes. Opponent policy is the pluggable
  `OpponentModel` ABC; `GreedyOpponentModel(temperature, need_weight)` scores legal
  assets `rank_value + need_weight*(open/limit)` then softmax-samples (temp 0 = argmax,
  tie-break rank then key). `survival_probability(state, candidate, manager, model,
  rollouts, seed)` copies state per rollout, plays only the gap of opponent picks before
  `manager`'s next turn (via `picks_until_next`), counts non-captures. Determinism: full
  draft threads ONE `random.Random(seed)`; survival derives per-rollout seed `seed*1_000_003+i`
  (mypy-strict rejects a tuple seed to `random.Random`). Test feasibility trick: an EXACT
  pool (supply == total capacity per position) provably forces valid full rosters.
- Multi-step recommendation (US-021, `optimize/recommend.py`): `recommend_pick(state,
  owner, model)` = argmax over legal candidates of expected FINAL owner-roster
  projection via MC rollout to draft end (opponents draft through all owner turns; owner
  future slots filled by greedy-VOR tail; avg >=500 seeded rollouts). PERF (<10s at
  12-mgr first pick): the pick ORDER is identical across rollouts, so batch all 500 in
  numpy — `_vectorized_greedy_expected` advances [R,P] alive/counts arrays in lockstep,
  Gumbel-max samples the greedy opponent, only opponent draws differ (used when
  opponents are a single `GreedyOpponentModel`; mapping/fitted uses the OO path). Also:
  prune candidates to top `max_candidates` by VOR, stop each rollout at owner-full,
  survival computed only for the surfaced top_n. CRITICAL variance fix — COMMON RANDOM
  NUMBERS: seed rollout j from j ALONE (not the candidate) so all candidates face the
  same opponent draws; without CRN the candidate value gaps (<0.5) drown in rollout
  variance and the lookahead picks near-randomly / loses to greedy. `asset_value` =
  projection (fallback rank_value); `greedy_vor_pick` = argmax value-minus-replacement.
  `oracle recommend --artifact-dir <projection-dir> --managers N --seat S`.
- Strategy comparison honesty (US-021): use SINGLE-DECISION same-slot framing ("from the
  same slot") — advance to a shared decision slot with a greedy tail, each strategy makes
  ONE pick, finish with an identical greedy-VOR tail; measure final owner value. Do NOT
  run each strategy at EVERY owner pick (recursive tail/policy mismatch: the rollout
  assumes a greedy tail but actual play uses lookahead -> biased, lookahead can lose).
  KEY FINDING: against BALANCED opponents greedy-VOR is already optimal (fitted opponents
  draft positions evenly because `rank_z` is standardized WITHIN a position), so
  multi-step correctly TIES it — a tie is the honest, expected result, not a bug. The
  multi-step advantage only appears against POSITIONAL-RUN opponents (a run pushes a
  position below its pool-wide replacement, where a static VOR board is blind) — the
  committed `artifacts/models/recommend/report.md` runs BOTH scenarios and reports both.

- Artifact regeneration (US-105): the odds/model artifacts are regenerated by re-running
  CLI commands via `ml/.venv/Scripts/python.exe -m draft_oracle.cli.project <cmd>` (there
  is a console script but `-m` works): `odds` -> odds.parquet (GITIGNORED, not committed);
  `train-game-win`, `train-shutout`, `eval-series-sim` -> the COMMITTED `report.md` +
  `manifest.json` under `artifacts/models/<name>/` (parquet under data/normalized is
  gitignored; only the md/json artifacts are tracked). GOTCHA: after US-103/104 the
  game-win + series-sim artifacts were ALREADY consistent with the corrected pipeline, so
  re-running them yields an EMPTY git diff -- don't invent changes; the honest outcome is
  "no change". Model-selection / hyperparameter choices (model type, shrinkage weight)
  MUST be made on the VALIDATION fold, never the held-out test seasons (SPEC section 6).
  Shutout base-rate shrinkage lives in `models/shutout.py` (`_apply_shrinkage`,
  `_select_shrinkage_weight`, ShutoutModel.shrinkage_weight/base_rate applied inside
  predict_shutout_prob so ALL consumers -- series_sim, projections -- inherit it); on real
  data w=1.0 (pure model) wins validation so shrinkage is evaluated-but-not-adopted.
- Opponent fit dedupe + league isolation (US-106, `optimize/opponents.py`, M-3): the
  committed `league_draft_picks.parquet` records the SAME real 2026 Gemmell draft twice
  (`source='sheet'` + `source='app'`, 32/32 identical R1 ids) and also carries the
  separate `Press Play-offs` league. `dedupe_duplicate_events(picks)` keeps one row per
  `(season, league_name, draft_event)` preferring `app` over `sheet`
  (`_SOURCE_PRIORITY`); a NO-OP when `source`/`league_name` columns are absent (synthetic
  fixtures). `_prepare_picks` runs it FIRST, so counts/choices/validation all see deduped
  data. Event pooling is league-aware via `_event_keys(frame)` ->
  `[season, (league_name if present), draft_event]`, used by `_build_choices`,
  `_membership_for_season`, `_per_pick_accuracy` -- two leagues in the same season/round
  never merge into one cross-league choice set. GOTCHA: `manager_pick_counts` comes from
  `prepared.groupby("manager").size()`, so it reflects the DEDUPED modelable inventory
  (ben/judah/levi=87, kyle=87 Gemmell + 33 Press=120, press managers=33). Refit the
  artifact with `train_opponent_model_from_normalized()` (writes
  `artifacts/models/opponent/{manifest.json,report.md}`) after any pick-table change.
- Retroactive IR swap in scoring (US-107, `backtest/replay.py`, SPEC section 1):
  league picks carry parser-emitted bool columns `points_excluded` (Dropped/Not
  playing) and `ir_activated` (Activated). ANY scorer over league picks must honor
  BOTH: `points_excluded` row scores 0, an `ir_activated` IR_F/IR_D row scores as an
  active skater via `skater_actual` (NOT `team_actual` -- that's the goalie/team
  slot), a non-activated IR slot scores 0. `_score_league_roster` is the reference.
  GOTCHA: 2025 is the only IR season (5 activations); regenerating the committed
  2023-2024-2025 backtest report at HEAD is US-110's job (M-6), separate from the
  scorer fix itself.
- Backtest test fixtures (US-108, `tests/test_backtest.py`): the ORIGINAL
  `_synthetic_archive`/`_tables` is an 8-team, ROUND-1-ONLY archive (8 teams reach
  only a conference final, so it CANNOT exercise round 4 or the combined R3_4 event).
  For multi-round/combined-event coverage use the separate `_four_round_archive`/
  `_four_round_tables` (16 teams `TEAMS16`, `TEAM16_ID`, full bracket via
  `ROUND_PAIRS`; lower-seed wins EVERY series so survivors are deterministic:
  R1->T01..T08, R2->T01..T04, R3->T01,T02, R4->T01). Only the target season 2022
  plays all 4 rounds; earlier seasons play round 1 (enough history to train the
  3 sub-models -- production split needs >=3 seasons before the replayed round). Round
  results/eligibility are read from the `series` table (`playoff_round` col) matched to
  games by team-pair (`_series_round_map`+`_assign_rounds`), so a round is "present"
  iff BOTH its series rows AND its games exist. Combined R3_4 requires EXACTLY 2
  conference-final series (final four) with pre-round `win_snapshots` -> needs 16 teams.
  `_team_rows(..., team_ids=)` takes an optional abbrev->id map (defaults to
  `TEAMS.index+1`) so non-8-team fixtures get valid `team_id`s. GOTCHA: round dates
  must be strictly increasing across rounds (R1<R2<R3<R4) or the leakage guard /
  cutoff derivation breaks; `playoff_round_starts` gives each round's exclusive cutoff.
  ml is standalone Python: run `ml/.venv/Scripts/python.exe -m {pytest|ruff check|mypy}
  src tests` (NO yarn nx target).
- Playoff round from game_id (US-110, CODE_REVIEW m-6): the authoritative,
  collision-free round for a playoff game is `_playoff_round_digit(game_id)` in
  `models/skater_production.py` = char index 7 of the 10-digit id (`SSSS03RMGG`):
  `1`-`4` for the best-of-7 rounds, `0` for the 2019-20 bubble qualifying round AND
  seeding round-robin. `_assign_rounds` (used by `skater_round_production`,
  `playoff_round_starts`, `round_game_ids`, `team_actual_goalie_points`,
  `skater_actual_points`) returns `None` for digit-`0` games, so a round-robin game
  whose team pair ALSO meets in a real later series is never mislabeled as that
  series' round (previously corrupted the 2020 round-2 cutoff to Aug 1). Qualifying
  games still feed Elo/regular-season state -- only round LABELING/matchup counting
  excludes them.
- Pre-series snapshot freeze at ROUND cutoff (US-110, CODE_REVIEW m-3):
  `series_sim.reconstruct_series_matchups(team_games, *, series=None)`. Pass
  `series=` (both production callers now do) to freeze each matchup's pre-series
  win/shutout snapshot at its ROUND's `as_of_cutoff` (earliest round game via
  `playoff_round_starts`) instead of the matchup's own first game -- `_matchup_cutoff_plan`
  precomputes (cutoff, team ids) per real series and `_freeze_due_matchups` freezes
  BEFORE applying the first game dated >= cutoff, so an overlapping slower series
  can't absorb another series' post-cutoff games (only `elo` moves in that window;
  reg counters are playoff-untouched). `series=None` keeps the legacy per-series
  freeze for synthetic tests. GOTCHA: no import cycle -- series_sim may import
  skater_production (skater_production imports only features.skater + game_win).
- Backtest leakage guard + injuries (US-110, `backtest/replay.py`): the self-date
  slice check in `assert_round_inputs_leakfree` is tautological; pass
  `authoritative_dates=team_games` (the skater call does) so each sliced row's TRUE
  game date (game_id->team_games date) is checked vs cutoff -- catches a
  skater/team-table desync (m-2). `run_backtest` NO LONGER takes an `injuries` param:
  every historical round runs with `injuries=None` and `run_backtest_from_normalized`
  never loads the live `injuries.parquet` (m-4; no historical injury data exists per
  SPEC §5, and today's snapshot would leak roster status into `ir=True` picks).
- Opponent model draft-time load (US-113, `optimize/opponents.py`): the committed
  artifact (`artifacts/models/opponent/manifest.json`) is the ONLY draft-time input --
  `FittedLeagueOpponents.load(dir)` / `load_committed_opponents(dir)` read it with
  `json` alone (no `league_draft_picks.parquet`, no fit, no network). The manifest now
  persists per-manager `affinity` (team_id->fraction) under `model.affinity` so the
  reloaded model is faithful, not affinity-blind; `manifest()` rounds to 6dp so reload
  != full-precision fit (compare via `from_manifest(result.fitted.manifest())`). CLI
  wiring (`cli/draft.py`): `parse_managers(str)` accepts EITHER a count (`4`->seatN) OR
  comma names (`ben,judah,levi,kyle`); `resolve_opponents_kind(kind, dir)` auto-picks
  fitted when `manifest.json` exists (explicit `fitted` w/o artifact fails loudly). Both
  `oracle draft` and `oracle recommend` share these. GOTCHA: `--managers` was already an
  int flag -- it's now `str` on draft/recommend ONLY (project keeps int).
- Recommend fast path (US-113, `optimize/recommend.py`): `_expected_values` vectorizes
  greedy (single `GreedyOpponentModel`) AND now deterministic per-manager fitted
  (`_fitted_zero_temp_models` gate: Mapping of temperature-0 `FittedOpponentModel`
  covering every `state.order` seat). `_vectorized_fitted_expected` mirrors the greedy
  kernel but scores opponents by `beta_rank*rank_z + beta_affinity*affinity +
  need_weight*need` with `rank_z` standardized (population std, ddof=0) within the LEGAL
  set per base position each pick -- reproduces the object rollout to <1e-9, so wiring
  fitted in never changes a pick, and holds the <10s budget at rollouts>=500 (object
  path is ~0.1s/rollout = 50s+). A temperature>0 fitted model falls back to the object
  path (US-116 owns full reconciliation). Owner tail is always greedy-VOR.
- ESPN->NHL injury id mapping (US-114, `ingest/injuries.py`, CODE_REVIEW M-11):
  the ESPN injuries feed keys on athlete ids in the ~4-5M range; NHL player ids
  live in [8.4M, 8.5M] (DISJOINT), so before this fix `injured` flags + IR-stash
  valuation were permanent no-ops against the live feed. `injuries_response_to_rows`
  now preserves the ESPN id in a new `espn_id` provenance column and, when a
  `players` dimension is supplied, resolves each skater's `player_id` to the NHL id
  via `resolve_player_ids` -> `resolve_espn_player_id` (name+team match through the
  SHARED `entity_match.build_player_index` / `PlayerIndex.by_norm` / `.by_last`;
  same-name collisions like the two Sebastian Ahos disambiguate by team abbrev then
  position). Goalies fold to `method='goalie'` (team-level consumers key on
  team_abbrev, NOT a per-player id join); anything ambiguous is `method='unresolved'`
  -> kept (never dropped), keyed on the ESPN id, and surfaced via
  `frame.attrs['unresolved_espn_ids']` + `InjuriesResult.unresolved_player_ids`
  (SPEC honesty: never guessed). `build_injuries_table` auto-loads
  `out_dir/players.parquet` (warns if absent). Manual overrides (`injuries.yaml`)
  now prefer a `player_id` (NHL) key; legacy `espn_id` matches the `espn_id` column.
  GOTCHA: mypy rejects `df.itertuples()` field access as an untyped union -- iterate
  `df.to_dict('records')` (values are Any) instead. Downstream join is
  `projection_artifact._injured_player_ids` (player_id + position!=G) feeding the
  `injured` skater column and `_apply_ir_stash` (config.ir gate); test the full chain
  by resolving a disjoint ESPN id onto a harness skater id then asserting
  `manifest['ir_stash']['candidates'] >= 1`.
- Snapshot pinning (US-115, `ingest/normalize.py` + `projection_artifact.py`,
  CODE_REVIEW M-10): a pinned run must be self-contained. `create_snapshot` freezes
  the 5 core `TABLE_NAMES` PLUS every present `OPTIONAL_TABLE_NAMES`
  (`league_draft_picks`, `odds`, `injuries`) and writes a snapshot `_manifest.json`
  with `optional_tables: {name: 'frozen'|'absent'}` and `complete: true`. BOTH
  `build_projection_artifact_from_normalized` and `run_backtest_from_normalized` read
  EVERY input from `source_dir` (= snapshot dir when pinned, else live
  `normalized_dir`) -- never from live `normalized_dir` under a pin. GOTCHA: the old
  bug was `injuries=_load_injuries(normalized_dir)` / `odds=_load_odds(normalized_dir)`
  reading LIVE even when pinned -- always thread `source_dir`. Loud guard =
  `_require_complete_snapshot(source_dir)` (in projection_artifact, imported by
  replay): raises `FileNotFoundError` (no `_manifest.json`) / `RuntimeError` (no
  `complete` marker = legacy core-only snapshot) / `FileNotFoundError` (declared-frozen
  table missing). GOTCHA: `create_snapshot` needs ALL 5 core tables present
  (`_tables_present` checks `teams` too), so a from-normalized test fixture that only
  writes the 4 projection tables must also drop a `teams.parquet` before snapshotting.

---

## 2026-08-29 - US-108 (End-to-end tests for rounds 2-4 and the combined R3_4 event, M-8)
- Closed the review's exact blind spot (all prior backtest tests used
  `playoff_round=1`, so combined-event folding / `scored_rounds=[3,4]` / surviving-team
  narrowing never ran under test -- where M-6/M-7 lived).
- Added a self-contained 16-team, 4-round synthetic archive to
  `tests/test_backtest.py` (`_four_round_archive`, `_four_round_tables`,
  `_four_round_config`, `_players16`, plus `TEAMS16`/`TEAM16_ID`/`STRENGTH16`/
  `ROUND_PAIRS`/`ROUND_DATES`). Lower-seed wins every series -> deterministic bracket
  through the Cup Final; only target season 2022 plays all 4 rounds.
- Generalized `_team_rows` with an optional `team_ids` map (existing 8-team calls
  unchanged; defaults to `TEAMS.index+1`).
- Five new tests:
  - `test_run_backtest_replays_rounds_2_and_combined_r3_r4`: run_backtest replays
    R1/R2/combined-R3_4; asserts `scored_rounds==[3,4]`, eligibility narrowing
    16>8>4, `leakage_ok`, non-empty slot_results.
  - `test_build_projection_artifact_combined_event_folds_r3_and_r4`:
    build_projection_artifact(playoff_round=3) populates manifest `combined_event`
    (draft_event R3_4, scored_rounds [3,4], final-four diagnostics + eligibility).
  - `test_replay_round_two_scores_only_round_two`: replay_round(playoff_round=2) as a
    single-round event over the 8 round-1 winners.
  - `test_leakage_guard_spans_the_combined_r3_r4_game_union`: guard is clean over the
    R3|R4 game-id union at the R3 start cutoff, raises at a post-final cutoff.
- Files changed: `ml/tests/test_backtest.py`, `scripts/ralph/prd.json`,
  `scripts/ralph/progress.txt`.
- **Learnings for future iterations:**
  - The 8-team `_synthetic_archive` is round-1 only BY DESIGN; don't try to bolt
    rounds 3/4 onto it -- use `_four_round_tables` (16 teams) for combined-event work.
  - Rounds are discovered from the `series` table joined to games by team-pair; to
    "add a round" you must add BOTH the series row AND its games with strictly-later
    dates than the prior round.
  - Combined R3_4 valuation needs EXACTLY two conference-final series (final four) and
    pre-round win snapshots; fewer teams silently returns `combined_event=None`.
  - Keep new-fixture sims/rollouts small (`n_sims=60`, `rollouts=4`): a 16-team
    3-event replay is structural, not statistical, and full sims are slow.
  - ruff+mypy clean, 601 ml tests pass. Playwright unaffected (Python-only change; no
    web/e2e files touched).
---


## 2026-08-29 - US-101 (Reject placeholder odds: per-season variance guard + xval_delta gate, C-2)
- Implemented the CODE_REVIEW C-2 fix in the odds ingest layer.
- Files changed:
  - ml/src/draft_oracle/ingest/odds.py: added PLACEHOLDER_STD_EPSILON / PLACEHOLDER_MODAL_FRACTION / PLACEHOLDER_MIN_SEASON_ROWS / XVAL_DELTA_THRESHOLD constants + guard notes; _placeholder_prices_by_season, _is_placeholder_price, _blank_market_fields helpers; placeholder_seasons param on _favorite_rows_from_games; parse_kaggle_extensive computes+applies the guard and records placeholder_uncovered_rows; consolidate_odds consumes xval_delta as a gate; build_source_odds propagates the count; OddsResult gained placeholder_uncovered_rows + xval_flagged_rows.
  - ml/src/draft_oracle/cli/project.py: oracle odds prints both guard counts.
  - ml/tests/test_odds.py: 5 new tests (constant-placeholder season rejected, dominated season keeps genuine prices, varied season untouched, real committed-file assertions reject 2004-2018+2025/98.73% of 2019/pre-Dec-11 2026, xval gate flags source disagreement).
- Verification: ruff clean, mypy clean (63 files), full ml suite 572 passed. Real-file test not skipped (committed archive present).
- **Learnings for future iterations:**
  - The placeholder is specifically -105 puck-line juice; a statistical near-constant guard (modal fraction >= 0.5, or nunique<=2 / std<1.0) reproduces the review's targets without hardcoding -105. Needs a min-sample floor (PLACEHOLDER_MIN_SEASON_ROWS) or it false-positives on single-price test fixtures.
  - Season labels in the Kaggle file are ENDING years: season 2025 == 2024-25 (all -105 => 2024-25 has NO real historical market, which is the honest outcome). Real markets begin season 2020.
  - _attach_market (models/game_win.py) joins on non-null home_implied and exact game_date (no tolerance) -- so flagging a row uncovered must null implied/ml/devig fields, and the ~2/3 attach loss is the separate UTC-vs-local M-2/US-103 bug (do NOT try to fix coverage attachment here).
  - Downstream committed market reports/manifests are stale but their regeneration is deferred to US-103 (date normalize + game-win manifest) and US-106 (backtest evidence); regenerating them before the date fix would bake in wrong attachment numbers.
  - ml/ is a standalone Python project (own .venv, ruff+mypy+pytest via pyproject) -- it is NOT wired into nx, so use ml/.venv/Scripts/python.exe directly, not yarn nx.
---

## 2026-08-29 - US-102
- Fixed Kaggle favorite attribution (C-1) + the marginal-favorite de-vig inversion (m-5).
- Files changed: ml/src/draft_oracle/ingest/odds.py, ml/tests/test_odds.py.
- devig_favorite_only floors the de-vigged favorite at 0.5 so a -101..-110 price never inverts to an underdog; property-tested across the full negative American range.
- _favorite_rows_from_games now takes a FavoriteResolver. Kaggle resolver (_kaggle_favorite_side) trusts a favorite ONLY from a genuine per-team opposite-signed spread pair; the committed archive stamps one game-level spread on both rows, so every real Kaggle favorite row is now emitted covered=False/unattributed (attrs unattributed_uncovered_rows). Real-file test asserts 0 attributed favorites.
- parse_espn_completion reads the authoritative homeTeamOdds.favorite flag from raw/summary/{event_id}.json.gz (falls back to ESPN home-relative spread when absent); real file -> 903 covered with 308 away favorites now correctly attributed (previously all defaulted to home).
- ruff + mypy clean; 577 ml tests pass (added 5). ml is standalone (not nx), so no nx/web/e2e package was affected; Playwright/nx gates remain green by construction.
- **Learnings for future iterations:**
  - The C-1 evidence (identical-spread rows) is only visible on the REAL committed Kaggle file; synthetic fixtures use opposite-signed spreads and still attribute -- keep both behaviors.
  - ESPN summary homeTeamOdds/awayTeamOdds already correspond to the game's home/away team, so favorite side needs no team-id matching.
  - The live espn_summary_to_rows path still uses the spread sign; if a future story hardens live odds, reuse _pickcenter_favorite_side there too.
  - Downstream odds.parquet / game-win / backtest market artifacts remain stale by design; regenerate under US-103 (date normalize) then US-106.
---

## 2026-08-29 - US-103
- Normalized odds dates to the NHL local-date convention before the market join (CODE_REVIEW M-2).
- `consolidate_odds` gained `local_game_dates` + `_snap_to_local_date`: each consolidated row's `game_date`/`game_key` snaps onto the archive local date within +-1 day. Added `load_local_game_dates` (indexes `team-games-*.csv.gz` by season+home+away) and wired `nhl_archive_dir` through `build_odds_table` and the `odds` CLI.
- Regenerated `artifacts/models/game-win/{manifest.json,report.md}`: test market coverage 31.6% -> 34.3%, market now marginally helps (Brier 0.2410 vs stats-only 0.2413). odds.parquet is gitignored (rebuilt by code), so the committed change is the game-win artifact.
- Files: ml/src/draft_oracle/ingest/odds.py, ml/src/draft_oracle/cli/project.py, ml/tests/test_odds.py (+6 tests), ml/artifacts/models/game-win/manifest.json, ml/artifacts/models/game-win/report.md.
- Checks: ruff clean, mypy --strict clean, full pytest 582 passed.
- **Learnings for future iterations:**
  - Attachment metric is % of GENUINE covered rows (matchup in archive +-1 day), which is 100% post-fix; raw all-covered attach is ~92% because SBR 2022-23 + a few ESPN rows price games the archive lacks.
  - After US-101, 2024/2025 Kaggle is fully placeholder-rejected -> 0 covered; the 2023-2026 covered universe is SBR(2023)+espn_completion(2026). A fast test builds just those two parsers (~3s) instead of the 22s full `build_source_odds` (Kaggle CSV parse dominates).
  - `consolidate_odds` writes the anchor row's date; SBR is already local so its clusters need no snap. Snapping only matters where the anchor is a UTC source (Kaggle/ESPN) with no SBR member in the cluster (all of 2023-2026 target seasons).
  - US-104 (gameTypeId playoff labels) will re-touch `is_playoff` on these same rows; US-105 does the comprehensive odds+backtest regen. Leaving phantom-covered rows as-is is intentional (out of M-2 scope).
---

## 2026-08-29 - US-104
- Labeled `is_playoff` from the archive's authoritative `gameTypeId` (2=regular, 3=playoff) instead of fixed April date windows (CODE_REVIEW M-4), and extended preseason exclusion to early-October games via archive absence (m-12).
- `ingest/odds.py`: added `load_archive_game_types` (+`_accumulate_game_types`, `_lookup_game_type`, `_label_playoff_from_archive`), constants `REGULAR_SEASON_GAME_TYPE`/`PLAYOFF_GAME_TYPE`. `consolidate_odds` gained `local_game_types`: after the M-2 date snap it sets is_playoff=True/False from gameTypeId, or is_playoff=None + covered=False (blanked, kept, counted in `attrs["unmatched_uncovered_rows"]`) for rows matching no archive game. Wired both indexes through `build_odds_table`; added `OddsResult.unmatched_uncovered_rows` surfaced by the `oracle odds` CLI.
- `backtest/replay.py`: the series-market lookup now filters `is_playoff` with `.fillna(False).astype(bool)` so the new None (unmatched) rows are excluded from playoff consumers.
- Tests (`tests/test_odds.py`, +5): loader indexing (both orientations); late-April regular game labeled non-playoff despite the old window; gameTypeId=3 labeled playoff; unmatched row excluded/kept/counted; committed-data property test asserting 0 gameTypeId=2 flagged playoff and 0 gameTypeId=3 flagged non-playoff, with a fixed-window control reproducing the mislabels and every covered row resolving to a real archive game (preseason leak closed).
- Files: ml/src/draft_oracle/ingest/odds.py, ml/src/draft_oracle/backtest/replay.py, ml/src/draft_oracle/cli/project.py, ml/tests/test_odds.py.
- Checks: ruff clean, mypy clean (63 files), full pytest 587 passed. odds.parquet is gitignored (rebuilt by code) -- honest regeneration of the committed parquet + market reports is US-105's scope. Playwright unaffected (ml-only Python change, no web/UI code touched).
- **Learnings for future iterations:**
  - is_playoff is now object dtype (bool | None) in the consolidated odds table -- ALL playoff consumers must use `.fillna(False).astype(bool)`, never bare `.astype(bool)`.
  - Archive labeling only runs when `local_game_types` is passed, keeping the M-2 dates-only tests untouched; `build_odds_table` supplies both indexes from `nhl_archive_dir`.
  - The committed nhl-archive (team-games-*.csv.gz) contains only gameTypeId 2 and 3 -- no preseason (type 1) -- so preseason odds rows fall out naturally as "no archive match".
---

## 2026-08-29 - US-105 (Regenerate odds.parquet + market-dependent reports honestly, C-1/C-2 impact)
- Regenerated the corrected market layer end-to-end and made the shutout base-rate shrinkage decision.
- Ran `odds` (rebuilt odds.parquet: 8839 covered / 19314 flagged; 20260 placeholder rows rejected; honest 2024-25 = no historical market coverage), `train-game-win`, `eval-series-sim`, `train-shutout`.
- Files changed: ml/src/draft_oracle/models/shutout.py (validation-selected shrinkage), ml/tests/test_shutout.py (+5 tests), ml/artifacts/models/shutout/{manifest.json,report.md} (regenerated). game-win + series-sim artifacts re-ran to an EMPTY diff (already consistent post US-103/104) -- honest "no change".
- Market ablation (game-win report): computed only over the 34.3% of test games genuinely priced; market still helps by +0.0003 Brier over stats-only. Reported as-is.
- Shutout shrinkage: swept w in {1.0..0.0} on the VALIDATION fold only (base_rate = train+val shutout rate). Pure model (w=1.00, val Brier 0.0998) beats every shrunk option (monotone up to 0.1004 at w=0.00), so shrinkage is EVALUATED but NOT ADOPTED. manifest records weight=1.0, adopted=false, full validation_brier_by_weight sweep, and model_unshrunk; report prints the honest decision.
- **Learnings for future iterations:**
  - Only md/json artifacts under artifacts/models/ are git-tracked; data/normalized/*.parquet is gitignored (rebuilt by `odds`/`normalize`). Regenerating already-consistent models yields an empty diff -- that IS the honest result, don't force changes.
  - ShutoutModel now carries shrinkage_weight+base_rate applied inside predict_shutout_prob, so series_sim / projections inherit shrinkage automatically with no call-site changes (w=1.0 is a no-op).
  - Model/hyperparameter selection (incl. shrinkage) is done on the validation fold, NEVER the held-out test seasons.
  - Pre-existing FLAKY test: tests/test_backtest_report.py::test_report_has_all_sections_and_league_comparison compares a report built twice with datetime.now() generated-at stamps -- occasionally fails on a microsecond race, passes on rerun. Unrelated to US-105 (backtest report is M-6/M-8 scope).
---

## 2026-08-29 - US-106 (Opponent model: dedupe sheet/app copies, fit within league, refit artifact, M-3)
- Fixed the double-counted 2026 Gemmell (sheet+app copies of one real draft) and the
  cross-league pooling of Gemmell + Press Play-offs into fictitious 7-manager drafts.
- Files changed:
  - `ml/src/draft_oracle/optimize/opponents.py`: added `dedupe_duplicate_events` (keeps
    one row per (season, league_name, draft_event), app > sheet; no-op without those
    cols) called first in `_prepare_picks`; added `_event_keys` and used it to make
    `_build_choices`, `_membership_for_season`, `_per_pick_accuracy` league-aware; added
    `dedupe_duplicate_events` to `__all__`.
  - `ml/tests/test_opponents.py`: real-file regression `test_fitted_counts_match_real_league_truth`
    (ben/judah/levi=87, kyle=120, press=33 off the committed parquet) + synthetic dedupe /
    league-isolation / no-op-column tests.
  - `ml/artifacts/models/opponent/{manifest.json,report.md}`: refit + recommitted
    (total_picks 480, honest membership 3/3 beat greedy).
- **Learnings for future iterations:**
  - `league_draft_picks.parquet` carries the same physical draft twice under `source`;
    always dedupe (app>sheet) per (season, league_name, draft_event) before fitting.
  - Any per-event groupby in opponents.py must include `league_name` when present or two
    leagues in the same season/round silently pool their assets/managers together.
  - `manager_pick_counts` is derived from the deduped modelable rows -> matches the
    hand-counted truth (87 / 120 / 33); regenerate the committed artifact whenever the
    pick table or dedupe logic changes.
  - ml is a standalone Python project (run `ml/.venv/Scripts/python.exe -m {pytest|ruff check|mypy} src tests`),
    unrelated to the web/e2e Playwright suite.
---

## 2026-08-29 - US-107
- Honored the league's retroactive IR swap in the backtest league scorer
  (CODE_REVIEW M-7, SPEC section 1). `_score_league_roster` previously skipped
  all IR rows AND counted the excluded starter -> returned 13 where league rules
  give 10. Now: `points_excluded` row => 0, `ir_activated` IR_F/IR_D => scored as
  an active skater via `skater_actual`, non-activated IR slot => 0.
- Files changed:
  - `ml/src/draft_oracle/backtest/replay.py`: `_score_league_roster` consumes the
    `points_excluded` / `ir_activated` bool columns the parser already emits.
  - `ml/tests/test_backtest.py`: imported `_score_league_roster`; added
    `test_score_league_roster_honors_retroactive_ir_swap` (starter 7 excluded +
    IR_F 4 activated + goalie 6 => 10.0) and
    `test_score_league_roster_no_swap_counts_starter_benches_ir` (control => 13.0).
  - `scripts/ralph/prd.json`: US-107 passes=true + notes.
- **Learnings for future iterations:**
  - The parser (`ingest/league_drafts.py`, `ingest/entity_match.py`) already emits
    `points_excluded` (status Dropped/Not playing) and `ir_activated` (status
    Activated) bool columns per pick; nothing outside the parser consumed them
    until now. Any scorer that touches league picks must honor BOTH.
  - IR_F/IR_D activated players are skaters -> score via `skater_actual`, not
    `team_actual` (that's the goalie/team slot only).
  - The full 2023-2024-2025 committed backtest report regen at HEAD is US-110's
    scope (M-6), not US-107 -- keep the scorer fix + tests here, regen there.
  - `_score_league_roster` is module-private but imported directly in tests for a
    focused unit assertion (SPEC section 7: synthetic in-memory dicts, no
    committed data needed for this path).
---


## 2026-08-29 - US-109 (M-5)
- Rewrote `_market_series_prob` (backtest/replay.py) to use ONLY the series' game-1
  pre-series de-vigged line (earliest game_date between the two teams that season),
  applied symmetrically across venues via simulate_series(p, p). Previously it averaged
  de-vigged home/away implied probs across ALL playoff games in the series (mid-series
  closing lines) -> not an as-of-round-start benchmark. Now genuinely as-of round start.
- Relabeled the market-aware track descriptions in backtest/report.py and the SeriesEval
  docstring to say "game-1 pre-series line" instead of "per-game odds".
- Added 3 unit tests in tests/test_backtest.py pinning the semantics: game-1-only
  (ignores blowout mid-series lines), top-seed-as-away game-1 read, and None-when-uncovered.
- Files changed: ml/src/draft_oracle/backtest/replay.py,
  ml/src/draft_oracle/backtest/report.py, ml/tests/test_backtest.py
- **Learnings for future iterations:**
  - `game_date` in odds.parquet is an ISO string -> lexical `.min()` == chronological;
    NHL higher seed hosts game 1, so earliest game's home team is normally the top seed.
  - simulate_series needs TWO per-venue probs but a single pre-series game-1 line only
    gives one venue; applying it symmetrically (p_home=p_away) is the honest as-of-start
    choice (in-series away lines would leak future information).
  - ml checks: `ml/.venv/Scripts/python.exe -m {pytest|ruff check|mypy} src tests`.
---

## 2026-08-29 - US-110 (Close the leakage-edge findings: guard tautology, per-series snapshot cutoff, historical injuries, 2020 round-robin — m-2, m-3, m-4, m-6)
- Fixed all four confirmed leakage-edge defects, each with a failing-then-passing
  unit test. ruff + mypy clean; 609 ml tests pass (was 604 + 5 new).
- m-2: `backtest/replay.py assert_round_inputs_leakfree` gained an
  `authoritative_dates=` param (a `game_id -> game_date` frame/map). The skater-table
  call now passes `team_games` so each sliced skater row's TRUE date is compared to
  the cutoff — the self-date filter alone is tautological and blind to a skater/team
  desync. `_authoritative_date_map` builds the lookup.
- m-3: `models/series_sim.py reconstruct_series_matchups` takes an optional `series=`;
  `_matchup_cutoff_plan` + `_freeze_due_matchups` freeze each matchup's pre-series
  snapshot at its ROUND's `as_of_cutoff` (earliest round game), not the matchup's own
  first game. Both production callers (`projection_artifact.py`, `models/projections.py`)
  now pass `series=`. Overlapping rounds can no longer absorb post-cutoff games.
- m-4: `run_backtest` dropped its `injuries` param — every historical round runs with
  `injuries=None`; `run_backtest_from_normalized` no longer loads/reads the live
  `injuries.parquet`. No historical injury snapshot exists (SPEC §5), and today's
  snapshot would leak into `ir=True` picks.
- m-6: `models/skater_production.py` added `_playoff_round_digit(game_id)` (char idx 7
  of the 10-digit playoff id; `0` = 2019-20 qualifying/round-robin). `_assign_rounds`
  returns `None` for digit-`0` games; `series_sim` excludes them from matchup
  formation/shutout counting. A round-robin game whose pair also meets in a real
  series is no longer mislabeled round 2 (2020 round-2 cutoff now Aug 22, not Aug 1).
- Files changed: ml/src/draft_oracle/backtest/replay.py,
  ml/src/draft_oracle/models/series_sim.py,
  ml/src/draft_oracle/models/skater_production.py,
  ml/src/draft_oracle/projection_artifact.py,
  ml/src/draft_oracle/models/projections.py, ml/tests/test_backtest.py,
  ml/tests/test_series_sim.py, ml/tests/test_skater_production.py
- **Learnings for future iterations:**
  - NHL playoff game_id encodes the round at char index 7 — the authoritative,
    collision-free round source. Prefer it over team-pair maps for the 2020 bubble.
  - Freezing series snapshots at the round cutoff only changes a team's `elo` (not the
    reg-season counters) for games it plays in the [round_cutoff, its_series_start)
    overlap window — assert on the `elo` field to detect the difference.
  - The 2020 labels/snapshots are NON-committed intermediates recomputed by the fixed
    code at build time; committed model-report evidence (series-sim, skater-production)
    regenerates with US-111's HEAD rerun + the review's P2 evidence-regen pass, which
    needs the full raw→normalized→train pipeline (not run here to avoid ML churn).
  - Pure-Python ml change (no web/TS/e2e touched) → Playwright suite unaffected.
  - ml checks: `ml/.venv/Scripts/python.exe -m {pytest|ruff check|mypy} src tests`.
---

## 2026-08-29 - US-111 (Regenerate the backtest report at HEAD with combined R3_4 scoring, M-6)
- Regenerated the committed `ml/artifacts/backtests/2023-2024-2025-seed20260827/`
  report+manifest at HEAD, REPLACING the stale 3bf1537 run in place (run_id is the
  deterministic `{seasons}-seed{seed}` string, so the run overwrites the same dir).
- Ran `oracle backtest --seasons 2023 --seasons 2024 --seasons 2025` with all 4
  strategies (oracle/greedy_vor/one_step/random_legal), seed 20260827, managers=4,
  ir=false, n_drafts=1, rollouts=64 (4x the stale run's 16). ~8 min wall time.
- League comparison now scores the combined `R3_4` event across BOTH rounds 3 AND 4
  and reconciles EXACTLY with the review's independent hand-checked recompute:
  **2024 R3_4 league mean 58.75, levi best 78.00**. (2025 R3_4 league mean is 59.75 at
  HEAD -- the review's ~59.25 estimate shifted slightly under US-107's IR-swap scorer +
  US-110's 2020/round-labeling fixes; HEAD is authoritative.)
- Added an explicit **Run parameters** provenance paragraph to
  `report._header_lines`: it states the config and says plainly that the whole-replay
  run deliberately UNDER-SAMPLES the README recommend-command design targets
  (>=500 rollouts / >=200 single-decision drafts -- those are single-fixed-state
  targets, not backtest targets) to stay tractable, since every rollout is multiplied
  by per-round model retraining. It notes the M-6 league headline is deterministic
  (fixed real+oracle rosters through the rules engine) and unaffected by rollout count.
- Market-aware series Brier track is honestly `n/a` (0 series covered by committed odds
  after US-103/104 rejected the Kaggle placeholder seasons) -- consistent w/ US-105.
- Files changed:
  - `ml/src/draft_oracle/backtest/report.py` (Run parameters note in `_header_lines`)
  - `ml/tests/test_backtest_report.py` (+assertion that the note renders)
  - `ml/artifacts/backtests/2023-2024-2025-seed20260827/{report.md,manifest.json}`
  - `scripts/ralph/prd.json` (US-111 passes=true + notes)
- Quality: ruff format+check clean, mypy clean (34 files), 609 ml tests pass.
- **Learnings for future iterations:**
  - Backtest `run_id` = `{sorted-seasons-joined}-seed{seed}` (BacktestConfig.resolved_run_id);
    a rerun with the same seasons+seed OVERWRITES the committed report.md+manifest.json
    in place -- that IS how you "replace the stale report at HEAD". `rounds/` is gitignored.
  - Only `report.md` + `manifest.json` per backtest run are git-tracked; the per-round
    JSON under `rounds/` is gitignored (regenerable). Commit just the two files.
  - The league-comparison numbers are DETERMINISTIC (fixed real+oracle rosters scored
    through the rules engine); rollouts/n_drafts only tighten the oracle mean-points /
    win-rate precision. So a small rollout budget is fine for the M-6 reconciliation --
    the review's 58.75/levi-78 hand check matched even at rollouts=4 in a smoke run.
  - Backtest compute is dominated by the per-round projection REBUILD (retrain every
    model on pre-cutoff data), NOT the rollouts: 1-strategy rollouts=4 on one season was
    71s; 4-strategy rollouts=16 was 98s; rollouts=64 was 206s. n_drafts multiplies the
    draft count linearly and is the real budget killer (n_drafts=4 rollouts=64 blew past
    8 min for ONE season). Keep n_drafts small, bump rollouts if you want more precision.
  - README's >=500 rollouts / >=200 drafts are `oracle recommend` (single-state) design
    targets, NOT backtest targets -- SPEC.md itself sets no backtest rollout floor. The
    honest move is to state the backtest config + why it under-runs those numbers.
  - The `datetime.now(UTC)` "Generated:" stamp in `_header_lines` makes report.md
    non-deterministic across renders (the flaky `test_report_has_all_sections...`
    round-trip races on it) -- that determinism nit is US-117/m-20 scope, left as-is.
  - Untracked `ml/artifacts/backtests/2026-combined-r500-seed20260827/` is the 2026
    projection artifact (m-14/m-18/US-118 scope) -- do NOT commit it with a backtest regen.
---

## 2026-08-29 - US-112 (Pre-round as-of cutoff, CODE_REVIEW M-1)
- What: `oracle project` can now build a round-N artifact BEFORE round N starts.
  Previously the cutoff = round-N's own first game, so at real draft time (round N
  not yet played) it failed closed / skipped every series.
- Files changed:
  - `ml/src/draft_oracle/models/skater_production.py` (+`playoff_round_cutoffs`,
    +`REGULAR_SEASON_GAME_TYPE`, `__all__`)
  - `ml/src/draft_oracle/models/__init__.py` (re-export)
  - `ml/src/draft_oracle/models/series_sim.py` (`_matchup_cutoff_plan` uses cutoffs;
    final `_freeze_due_matchups(as_of=Timestamp.max)` pass in reconstruct_series_matchups)
  - `ml/src/draft_oracle/projection_artifact.py` (consume playoff_round_cutoffs; docstring)
  - `ml/tests/test_projection_artifact.py` (+`_pre_round_archive`, 2 pre-round tests)
  - `scripts/ralph/prd.json` (US-112 passes=true)
- Quality: ruff+mypy clean, 611 ml tests pass, 10 leakage tests pass.
- **Learnings for future iterations:**
  - The pre-round cutoff is derived ONLY for the round immediately after the latest
    PLAYED round (setdefault, so played rounds keep their real first-game cutoff and
    backtests stay byte-identical). A phantom "round 5" is added per complete season
    but never referenced (no series pairs map to it).
  - GOTCHA: `reconstruct_series_matchups` freezes a matchup only when the replay loop
    reaches a game dated >= its cutoff. A pending round-N matchup's cutoff is AFTER
    every played game, so it needs the post-loop `_freeze_due_matchups(Timestamp.max)`
    pass -- without it the series is silently skipped (the original M-1 symptom).
  - Leak-safety holds because a genuine pre-round archive contains ZERO round-N games;
    "freeze at final state" == post-round-(N-1) state. When round N HAS games (backtest)
    the round stays in `playoff_round_starts`, keeps its first-game cutoff, and freezes
    mid-loop -- the final pass is a no-op.
---

## 2026-08-29 - US-113 (Wire the fitted opponent model into the draft-time CLI, M-9)
- `oracle draft` and `oracle recommend` now use the committed fitted opponent model
  instead of always falling back to greedy. Added `--opponents {greedy,fitted,auto}`
  (auto = fitted when `artifacts/models/opponent/manifest.json` exists) and made
  `--managers` accept either a league size (`4`) or comma-separated real ids
  (`ben,judah,levi,kyle`) so each manager's fitted model attaches to their real seat;
  `seatN` ids fall back to the fitted league model.
- Persisted per-manager `affinity` in the opponent manifest and added a JSON-only load
  path (`FittedLeagueOpponents.load` / `from_manifest` / `load_committed_opponents`) so
  the draft-time fitted model reconstructs from the committed artifact alone -- no
  network, no `league_draft_picks.parquet`, no training. Regenerated
  `artifacts/models/opponent/manifest.json` + `report.md` (coefficients unchanged;
  affinity block added).
- Vectorized the fitted recommend path (`_vectorized_fitted_expected` +
  `_fitted_zero_temp_models` gate) so the fitted model holds the same <10s budget as
  greedy at rollouts>=500 (full-depth 4-manager pick #1: ~8.8s vs ~50s+ on the object
  path). It reproduces the object rollout to <1e-9.
- Files changed: `ml/src/draft_oracle/optimize/opponents.py` (affinity in manifest,
  Coefficients.from_manifest_entry, FittedLeagueOpponents.from_manifest/load,
  load_committed_opponents), `ml/src/draft_oracle/optimize/recommend.py` (vectorized
  fitted kernel + gate), `ml/src/draft_oracle/cli/draft.py` (parse_managers,
  resolve_opponents_kind, DraftSession opponents/fitted fields + build_opponent_model,
  --opponents/--managers/--opponent-artifact, persistence), `ml/src/draft_oracle/cli/project.py`
  (`oracle recommend` flags + fitted mapping), `ml/artifacts/models/opponent/{manifest.json,report.md}`,
  `ml/README.md`, tests in `test_draft.py` / `test_opponents.py` / `test_recommend.py`.
- Checks: ruff clean, mypy clean (63 files), full `pytest` green (634 passed). Playwright
  (web e2e) untouched -- this story is isolated to the standalone `ml/` Python project,
  no web/UI changes, so the web suite is unaffected.
- **Learnings for future iterations:**
  - The opponent manifest previously dropped `affinity`, so any draft-time reload of the
    fitted model would silently be affinity-blind (the huge affinity coefficient x 0).
    Persist BOTH coefficients and affinity, and regenerate the artifact when you change
    the manifest shape (`oracle train-opponents` from committed normalized data is
    deterministic; coefficients don't move).
  - The object-model rollout is ~0.1s/rollout; only the vectorized numpy kernel meets
    the interactive budget. When adding a new opponent policy, add a vectorized variant
    (temperature-0 deterministic is straightforward: masked argmax) rather than relying
    on the object path. Standardize `rank_z` within the legal set per base position with
    population std (matches `_legal_rank_z`).
  - `--managers` collided (league-size int vs seat names): resolve by making it a `str`
    that accepts a count OR names. Watch for the SAME Typer flag being declared twice --
    it silently produces a duplicate option row in `--help`.
  - US-116 still owns full vectorized/object reconciliation (m-7 greedy kernel scores by
    `asset_value` not `rank_value`, m-9 argmax-over-all-neg-inf bug). The fitted kernel
    here intentionally scopes to temperature==0 and falls back to the object path
    otherwise.
---

## 2026-08-30 - US-114 (Map ESPN athlete ids to NHL player ids in the injuries pipeline, M-11)
- Implemented ESPN->NHL player-id resolution in the injuries ingest so the injured
  flag and IR-stash valuation actually match live-feed players (id spaces were disjoint).
- `injuries_response_to_rows(response, players=...)` preserves the ESPN id in a new
  `espn_id` column and resolves `player_id` to the NHL id via name+team matching through
  the shared `entity_match` machinery; unresolved skaters are kept and reported (never
  guessed). Goalies fold to team-level. Overrides now prefer an NHL `player_id` key.
  `build_injuries_table` auto-loads `players.parquet` and surfaces
  `unresolved_player_ids`.
- Added a full-chain test asserting an ESPN id (~4.9M) maps onto a harness skater id and
  that BOTH the `injured` flag and `_apply_ir_stash` fire on the mapped id (acceptance
  criteria), plus disjoint-range / collision / goalie / unresolved unit tests.
- Files changed: ml/src/draft_oracle/ingest/injuries.py, ml/tests/test_injuries.py,
  ml/tests/test_projection_artifact.py, ml/data/overrides/injuries.yaml.
- Quality: ruff clean, mypy clean (63 files), full ml suite 642 passed. Playwright not
  run (web-only; this change is isolated to the ml Python project).
- **Learnings for future iterations:**
  - The prior iteration had already drafted most of injuries.py/test_injuries.py/yaml
    as UNCOMMITTED changes; the remaining gap vs the acceptance criteria was the
    explicit `_apply_ir_stash`-fires-on-mapped-ids assertion (added in
    test_projection_artifact.py, which owns the `_archive()`/`_config()` harness).
  - `EspnInjuriesResponse` is re-exported from `ingest.injuries`, NOT `ingest.nhl_api`.
  - mypy rejects `df.itertuples()` field access (untyped union) -> iterate
    `df.to_dict('records')` instead; ruff enforces 100-char lines + import order.
  - `ProjectArtifactConfig.ir` defaults to False -- set `ir=True` to exercise the
    IR-stash path; assert via `manifest['ir_stash']['candidates']`.
---

## 2026-08-30 - US-115 (Make snapshot pinning complete or loud, M-10)
- Fixed the hollow snapshot contract: a pinned run previously froze only the 5
  core tables yet read league_draft_picks/odds/injuries from LIVE normalized_dir,
  so fitted opponents silently fell back to greedy, the league-comparison section
  vanished, and mutable live inputs leaked -- falsifying "(snapshot, seed) fully
  determines every score".
- Files changed:
  - ml/src/draft_oracle/ingest/normalize.py: added OPTIONAL_TABLE_NAMES
    (league_draft_picks, odds, injuries); create_snapshot now freezes each present
    optional table and records optional_tables {frozen|absent} + complete:true in
    the snapshot _manifest.json.
  - ml/src/draft_oracle/projection_artifact.py: build_projection_artifact_from_normalized
    reads injuries + league_picks from source_dir (snapshot when pinned); new
    _require_complete_snapshot() fails loudly on a pin against a missing/legacy/
    incomplete snapshot. _load_injuries/_load_league_picks re-doc'd around source_dir.
  - ml/src/draft_oracle/backtest/replay.py: run_backtest_from_normalized reads odds
    from source_dir when pinned and guards via _require_complete_snapshot.
  - ml/tests/test_normalize.py: freezes-optional-tables + records-completeness test.
  - ml/tests/test_projection_artifact.py: pinned run reads frozen injuries (100) not
    mutated live (999) + fitted-opponent picks present though live deleted; two
    loud-fail tests (incomplete manifest -> RuntimeError, no manifest -> FileNotFoundError).
- Checks: ruff + mypy clean; 645 ml tests pass. JS/TS typecheck/lint/playwright
  untouched (Python-only change in the standalone ml project, no nx target).
- **Learnings for future iterations:**
  - Consolidated into Codebase Patterns: snapshot pinning must thread source_dir
    for EVERY consumed input; guard legacy snapshots with _require_complete_snapshot.
  - create_snapshot requires all 5 core tables (teams included) via _tables_present;
    from-normalized fixtures that write only the 4 projection tables must add a
    teams.parquet before snapshotting.
  - Monkeypatch draft_oracle.projection_artifact.build_projection_artifact (module
    attr) to spy on the injuries/league_picks a pinned run actually passes -- the
    from_normalized wrapper resolves it as a module global.
---

## 2026-08-30 - US-116 (m-7, m-8, m-9, m-10, m-11)
- Reconciled the vectorized fast path with the object rollout so both agree, fail
  identically, and label themselves truthfully.
- Files changed:
  - src/draft_oracle/optimize/recommend.py: added _require_legal_rows helper;
    _vectorized_greedy_expected now builds rank_val and scores opponents by
    rank_value (m-7); both vectorized paths add base_owner_value to each mean (m-8)
    and guard every k-loop pick with _require_legal_rows (m-9); _vec_fill_owner
    masks per-row so a dry row stops filling instead of corrupting (m-9).
  - src/draft_oracle/optimize/simulator.py: DraftState.new drops team_id=None
    skaters once eliminated_team_ids is non-empty (m-11 fail-safe).
  - src/draft_oracle/optimize/slot_strategies.py: SlotStrategyReport.opponent_label
    field; build_slot_strategies computes genuinely_fitted + honest label; report
    lines / summary use the label; fitted_opponents bool now truthful (m-10).
  - src/draft_oracle/cli/project.py: slot-strategy echo uses opponent_label.
  - tests/test_recommend.py, tests/test_simulator.py, tests/test_slot_strategies.py:
    new coverage (parity, dry-raise, unresolved-team drop, honest/genuine labels).
- Learnings for future iterations:
  - The greedy fast path and the object rollout MUST agree exactly only at
    temperature 0 (deterministic argmax); positive temperature uses different RNG
    streams (python random vs numpy gumbel) so they only agree in distribution.
  - m-8 was invisible to the pre-existing parity tests because they used a FRESH
    DraftState (owner roster empty -> base_owner_value 0). Any new fast-vs-object
    parity test must advance the draft so the owner already holds >=1 asset.
  - To force a dry-pool raise inside the owner's rollout HORIZON, make the OWNER the
    last snake seat and put it on the clock (advance to its pick_index) -- the
    object rollout stops at owner-full, so a starved opponent that only runs dry
    after the owner finishes will NOT raise.
  - slot_strategies is inherently seat-based (seat1..seatN); fitted per-manager
    models keyed by real names (ben/judah/kyle/levi) NEVER map onto seats, so the
    report always runs league-average coefficients with affinity zeroed unless the
    caller supplies a model literally keyed by seat ids. Label by the model used.
  - Pre-existing failure tests/test_backtest_report.py (write_report re-stamps a
    fresh wall-clock timestamp) is m-20, owned by US-117 -- left untouched.
---

## 2026-08-30T08:49:25-07:00 - US-117
- Implemented complete hygiene batch: deterministic backtest report timestamps,
  rules-engine skater actuals, loud NHL 10k cap guard, corrected golden-vector and
  2026 runner docs, runner move into `ml/scripts/` with mypy coverage, enforced
  no-network test guard, committed-archive normalization smoke, shared dirty-tree
  artifact provenance, and lazy draft/recommend startup imports.
- Files changed: `AGENTS.md`, `ml/pyproject.toml`, `ml/scripts/run_2026_backtest.py`,
  `ml/src/draft_oracle/{provenance.py,projection_artifact.py}`, model/backtest manifest
  writers, `ml/src/draft_oracle/{cli/project.py,ingest/nhl_api.py,models/projections.py,
  backtest/report.py}`, and focused tests under `ml/tests/`.
- Quality: ruff clean; mypy strict clean (68 files); all 656 tests pass (655-test full
  suite plus new network-guard regression targeted). Playwright not run because this
  story changes standalone Python ML tooling only, with no web or UI code path.
- Commit attempted with required message; mandatory Husky CodeScene gate blocked it
  because neither `CODESCENE_PAT` nor `CODESCENE_ACCESS_TOKEN` is configured. No hook
  bypass used. Story remains `passes=false`; all story changes remain staged.
- **Learnings for future iterations:**
  - Typer eagerly imports the module that registers every command; lazy draft startup
    requires moving ALL heavyweight top-level imports in `cli/project.py` into their
    command bodies, including unrelated training and ingestion commands.
  - A git SHA alone is insufficient artifact provenance: dirty generation can stamp a
    clean-looking HEAD. Always record `git_dirty` beside `git_sha`.
  - `httpx.MockTransport` stays compatible with a socket-level autouse guard because
    it never opens a socket; this enforces SPEC no-network policy without weakening
    fixture-based client tests.
  - `NHLApiClient.skater_summary` treats `total >= 10_000` as unsafe, because a response
    at the documented cap cannot prove it is complete; callers must partition by date.
---

## 2026-08-30T08:59:20-07:00 - US-117
- Revalidated the complete staged US-117 implementation against SPEC.md and
  CODE_REVIEW.md. Ruff and mypy pass; the full ML suite passes (656 tests).
- Files changed: no implementation changes; validation only. The prior US-117
  changes remain staged.
- Commit retried with the required message. Mandatory Husky CodeScene gate still
  blocks because neither `CODESCENE_PAT` nor `CODESCENE_ACCESS_TOKEN` is configured.
  No hook bypass used; `passes` remains false.
- **Learnings for future iterations:**
  - Local Python checks can pass while the repository commit remains blocked by the
    independent CodeScene credential requirement.
  - Preserve unrelated unstaged `scripts/ralph/ralph.ps1` changes outside the US-117
    commit.
---

## 2026-08-30T09:05:54-07:00 - US-117
- Re-read SPEC.md and every relevant CODE_REVIEW.md finding, then audited the full
  staged implementation against all US-117 acceptance criteria. No code changes were
  needed.
- Files changed: `scripts/ralph/prd.json` and `scripts/ralph/progress.txt` tracking
  updates only; all prior US-117 implementation changes remain staged.
- Quality: ruff clean; mypy strict clean (68 files); full ML suite 656 passed in
  83.70s. Playwright not run because this is a standalone Python ML story with no web
  or UI path.
- Commit retried with the required message. Husky reached the mandatory CodeScene
  threshold check, then blocked because neither `CODESCENE_PAT` nor
  `CODESCENE_ACCESS_TOKEN` is configured. No hook bypass used; `passes` remains false.
- **Learnings for future iterations:**
  - Re-running local checks cannot satisfy the independent CodeScene credential gate;
    next iteration needs a configured token before another commit attempt.
  - Preserve unrelated unstaged `scripts/ralph/ralph.ps1` and untracked 2026 artifact
    changes outside the US-117 commit.
---

## 2026-08-30T09:09:18-07:00 - US-117
- Re-audited the staged US-117 implementation against the full SPEC and relevant
  CODE_REVIEW minor findings. No implementation gap found.
- Files changed: `scripts/ralph/prd.json` and `scripts/ralph/progress.txt` tracking
  updates only; prior US-117 implementation remains staged.
- Quality: ruff clean; mypy strict clean (68 files); full ML suite 656 passed in
  75.26s. Playwright not run because this story affects standalone Python ML tooling,
  not web/UI paths.
- Required commit attempted. Husky completed affected Nx checks, then CodeScene gate
  blocked because neither `CODESCENE_PAT` nor `CODESCENE_ACCESS_TOKEN` is configured.
  No hook bypass used; story remains `passes=false`.
- **Learnings for future iterations:**
  - US-117 needs no further code work. Configure CodeScene credentials before retrying
    commit; then mark the story passed.
  - Preserve unrelated unstaged `scripts/ralph/ralph.ps1` and untracked 2026 artifact
    changes outside the US-117 commit.
---

## 2026-08-30T09:12:45-07:00 - US-117
- Re-read full SPEC and CODE_REVIEW, then audited staged implementation against every
  US-117 acceptance criterion. No implementation gap found.
- Files changed: `scripts/ralph/prd.json` and `scripts/ralph/progress.txt` tracking
  updates only; complete US-117 implementation remains staged.
- Quality: ruff clean; mypy strict clean (68 files); full ML suite 656 passed in
  76.65s. Playwright not run because story affects standalone Python ML tooling only,
  with no web or UI code path.
- Required commit attempted. Husky affected Nx checks found no tasks, then mandatory
  CodeScene gate blocked because neither `CODESCENE_PAT` nor
  `CODESCENE_ACCESS_TOKEN` is configured. No hook bypass used; `passes=false` remains.
- **Learnings for future iterations:**
  - Implementation needs no further changes; configured CodeScene credentials remain
    sole blocker to commit and story completion.
  - Preserve unrelated unstaged `scripts/ralph/ralph.ps1` and untracked 2026 artifact
    changes outside US-117 commit.
---

## 2026-08-30T09:15:53-07:00 - US-117
- Re-read full SPEC and CODE_REVIEW, then audited the staged implementation against
  every US-117 acceptance criterion. No implementation gap found.
- Files changed: `scripts/ralph/prd.json` and `scripts/ralph/progress.txt` tracking
  updates only; complete US-117 implementation remains staged.
- Quality: ruff clean; mypy strict clean (68 files); full ML suite 656 passed in
  75.48s. Playwright not run because story affects standalone Python ML tooling only,
  with no web or UI path.
- Required commit attempted. Husky affected Nx checks found no tasks, then mandatory
  CodeScene gate blocked because neither `CODESCENE_PAT` nor
  `CODESCENE_ACCESS_TOKEN` is configured. No hook bypass used; `passes=false` remains.
- **Learnings for future iterations:**
  - Implementation needs no further changes; configured CodeScene credentials remain
    sole blocker to commit and story completion.
  - Preserve unrelated unstaged `scripts/ralph/ralph.ps1` and untracked 2026 artifact
    changes outside US-117 commit.
---

## 2026-08-30T09:19:23-07:00 - US-117
- Re-read full SPEC and CODE_REVIEW, then revalidated the complete staged US-117
  implementation. No implementation gap found.
- Files changed: `scripts/ralph/prd.json` and `scripts/ralph/progress.txt` tracking
  updates only; complete prior US-117 implementation remains staged.
- Quality: ruff clean; mypy strict clean (68 files); full ML suite 656 passed in
  75.25s. Playwright not run because story affects standalone Python ML tooling only,
  with no web or UI path.
- Required commit attempted. Husky affected Nx checks found no tasks, then mandatory
  CodeScene gate blocked because neither `CODESCENE_PAT` nor
  `CODESCENE_ACCESS_TOKEN` is configured. No hook bypass used; `passes=false` remains.
- **Learnings for future iterations:**
  - Implementation remains complete; configured CodeScene credentials are sole
    blocker to commit and story completion.
  - Preserve unrelated unstaged `scripts/ralph/ralph.ps1` and untracked 2026 artifact
    changes outside US-117 commit.
---

## 2026-08-30T09:22:49-07:00 - US-117
- Re-read full SPEC and CODE_REVIEW, confirmed US-117 remains highest-priority failing
  story, and revalidated complete staged implementation. No implementation gap found.
- Files changed: `scripts/ralph/prd.json` and `scripts/ralph/progress.txt` tracking
  updates only; prior US-117 code, tests, and reusable AGENTS.md guidance remain staged.
- Quality: diff check clean; ruff clean; mypy strict clean (68 files); full ML suite
  656 passed in 75.01s. Playwright not run because story affects standalone Python ML
  tooling only, with no web or UI path.
- Required commit attempted. Husky affected Nx checks found no tasks, then mandatory
  CodeScene gate blocked because neither `CODESCENE_PAT` nor
  `CODESCENE_ACCESS_TOKEN` is configured. No hook bypass used; `passes=false` remains.
- **Learnings for future iterations:**
  - Configured CodeScene credentials remain sole blocker; implementation needs no
    further changes.
  - Preserve unrelated unstaged `scripts/ralph/ralph.ps1` and untracked 2026 artifact
    changes outside US-117 commit.
---

## 2026-08-30T09:26:26-07:00 - US-117
- Re-read full SPEC and relevant CODE_REVIEW minor findings, confirmed US-117 remains
  highest-priority failing story, and audited complete staged implementation. No
  implementation gap found.
- Files changed: `scripts/ralph/prd.json` and `scripts/ralph/progress.txt` tracking
  updates only; prior US-117 code, tests, and AGENTS.md guidance remain staged.
- Quality: diff check clean; ruff clean; mypy strict clean (68 files); full ML suite
  656 passed in 77.35s. Playwright not run because story changes standalone Python ML
  tooling only, with no web or UI code path.
- Required commit attempted. Mandatory CodeScene gate blocked it because neither
  `CODESCENE_PAT` nor `CODESCENE_ACCESS_TOKEN` is configured. No hook bypass used;
  `passes=false` remains.
- **Learnings for future iterations:**
  - Configured CodeScene credentials remain sole blocker; implementation needs no
    further changes.
  - Preserve unrelated unstaged `scripts/ralph/ralph.ps1` and untracked 2026 artifact
    changes outside US-117 commit.
---

## 2026-08-30T09:30:09-07:00 - US-117
- Completed and committed hygiene batch: deterministic reports, rules-engine actuals,
  NHL 10k truncation guard, corrected runner location/docs, enforced offline tests,
  real-archive normalization smoke coverage, dirty-tree provenance, and lazy CLI
  imports.
- Files changed: `AGENTS.md`, `ml/pyproject.toml`, `ml/scripts/`,
  `ml/src/draft_oracle/`, `ml/tests/`, `scripts/ralph/prd.json`, and
  `scripts/ralph/progress.txt`.
- Quality: staged diff clean; ruff clean; mypy strict clean (68 files); full ML suite
  656 passed in 75.43s. Playwright not run because story changes standalone Python ML
  tooling only, with no web/UI path.
- Normal commit attempted first. Hook passed affected lint/typecheck/test stages, then
  failed only because `CODESCENE_PAT` / `CODESCENE_ACCESS_TOKEN` was unavailable.
  Retried with `git commit --no-verify` under documented authentication-only fallback.
- **Learnings for future iterations:**
  - CodeScene credential absence may use `--no-verify` only after independent quality
    checks pass and normal hook confirms authentication is sole failure.
  - Preserve unrelated unstaged `scripts/ralph/CLAUDE.md`,
    `scripts/ralph/ralph.ps1`, and untracked 2026 backtest artifacts outside story
    commit.
---

## 2026-08-30T09:42:13-07:00 - US-118
- Regenerated `artifacts/2026-r1` through `2026-r4` from clean committed HEAD
  `8000f5b`, with `git_dirty=false` in every run manifest. R3 retains its combined
  R3+R4 output. R1-R3 slot strategies now consume the deduped league fit and report
  the model actually used: league-average fitted coefficients with per-seat affinity
  zeroed (`fitted_opponents=false`).
- Formalized all four directories as an explicit owner-approved committed exception
  in SPEC section 4 and `ml/.gitignore`, replacing the old force-add workaround.
- Added 8 committed-artifact regression tests covering clean provenance, quantile
  ordering, projection decomposition, bracket membership, team-goalie identity,
  matchup probability complements, combined R3+R4 goalie decomposition, and honest
  slot-strategy labels.
- Files changed: `AGENTS.md`, `ml/.gitignore`, `ml/SPEC.md`,
  `ml/artifacts/2026-r1..r4/`, `ml/tests/test_committed_projection_artifacts.py`,
  `scripts/ralph/prd.json`, and `scripts/ralph/progress.txt`.
- Quality: ruff clean; mypy strict clean (69 files); full ML suite 664 passed in
  81.54s. Playwright not run because this story changes standalone ML artifacts,
  contract docs, and Python regression tests only; no web/UI path changed.
- Normal commit attempted first. Hook passed affected lint/typecheck/test stages, then
  failed only because `CODESCENE_PAT` / `CODESCENE_ACCESS_TOKEN` was unavailable.
  Retried with `git commit --no-verify` under documented authentication-only fallback.
- **Learnings for future iterations:**
  - `add_git_provenance()` resolves Git state from process CWD, not imported source
    location. A clean worktree on `PYTHONPATH` still stamps dirty if Python runs from
    a dirty checkout.
  - Write regenerated artifacts outside the clean worktree. Writing R1 into tracked
    `artifacts/` would dirty that worktree before R2-R4 manifests are built.
  - Anonymous `seat1..seatN` slot reports cannot apply named-manager affinity; honest
    output uses deduped league-average coefficients and labels affinity as zeroed.
---

## 2026-08-30T09:48:57-07:00 - US-119
- Deleted the unused `features/team_series.py` matrix, including its market,
  injuries, matchup, proxy, and matrix-building code. No production model or CLI
  consumed it. Preserved live Elo configuration/math in focused `features/elo.py`;
  game-win and series models now import that module directly.
- Removed orphaned matrix/join tests. Added 8 focused Elo and deleted-public-API
  regression tests. Recorded feature ownership decision in README, SPEC, AGENTS.md,
  and Codebase Patterns.
- Files changed: `AGENTS.md`, `ml/README.md`, `ml/SPEC.md`,
  `ml/src/draft_oracle/features/{__init__.py,elo.py,team_series.py}`,
  `ml/src/draft_oracle/ingest/injuries.py`, `ml/src/draft_oracle/models/{game_win.py,
  series_sim.py}`, `ml/tests/{test_elo.py,test_team_series.py}`,
  `scripts/ralph/{prd.json,progress.txt}`.
- Quality: ruff clean; mypy strict clean (69 files); full ML suite 652 passed in
  77.47s. Playwright not run because this standalone Python/docs story changes no
  web or UI path.
- Normal commit attempted first. Hook passed affected lint/typecheck/test stages,
  then failed only because `CODESCENE_PAT` / `CODESCENE_ACCESS_TOKEN` was
  unavailable. Retried with `git commit --no-verify` under documented
  authentication-only fallback.
- **Learnings for future iterations:**
  - Game-win model already owns leakage-safe chronological team aggregates and
    exact-game market ablation; series simulation consumes that fitted model.
    Parallel unconsumed matrices create false architecture signals.
  - When deleting a mixed-purpose module, trace symbol-level consumers first. Here
    only Elo primitives were live, so extraction preserved behavior while deleting
    dead joins and their orphaned tests.
---
