# Ralph Progress Log
Started: 02/27/2026

## Codebase Patterns
- Use `@rstest/core` (describe/it/expect) for unit tests, NOT vitest or jest
- Run `npx eslint --fix` on new/changed files to fix CRLF line endings on Windows
- Extract testable logic into pure functions with client interfaces (e.g., `authOperations.ts`, `updateProfile.ts`)
- The `OtpAuthClient` interface in `authOperations.ts` mirrors the Supabase auth API surface for testing
---

## 2026-02-27T04:09:00Z - US-001
- What was implemented: Added `signInWithOtp` and `verifyOtp` methods to the `useAuth` hook, plus extracted `authOperations.ts` module with pure testable functions and 8 unit tests
- Files changed:
  - `packages/supabase/src/lib/hooks/useAuth.ts` — Added signInWithOtp and verifyOtp useCallback methods
  - `packages/supabase/src/lib/hooks/authOperations.ts` — New file: extracted OtpAuthClient interface, sendOtpCode, and verifyOtpCode pure functions
  - `packages/supabase/src/lib/hooks/authOperations.test.ts` — New file: 8 unit tests covering OTP send and verify operations
- **Learnings for future iterations:**
  - The supabase package had no existing tests — `rstest.config.ts` with `passWithNoTests: true` was already configured
  - `useRegularSeasonStats.ts` also had CRLF issues — fixed by running eslint --fix
  - E2E test results: 79 failed / 8 passed — same as pre-existing baseline on main branch
---
