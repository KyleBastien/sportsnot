# Ralph Progress Log
Started: 02/14/2026 21:14:25

## Codebase Patterns
- Package scaffolding: package.json with exports map, tsconfig.json extending root, tsconfig.lib.json with nodenext module resolution
- Path aliases go in root tsconfig.base.json paths
- Nx auto-infers typecheck target via @nx/js/typescript plugin — no need to add manually
- Lint is not per-project in Nx; use root `npx eslint <path>` directly
- Unit tests use rstest (yarn rstest <specific files>) — do NOT run bare `yarn rstest` as it picks up Playwright files
- E2e tests use Playwright against http://localhost:4200 (needs serve-static running)
- Nx sync prompt appears on first typecheck — answer "yes" to sync TS references
- tsconfig.json is auto-modified by Nx sync — commit changes after sync
- Scripts that import cross-package need separate tsconfig with `bundler` moduleResolution (not `nodenext`) — use tsconfig.scripts.json pattern
- Use `/* eslint-disable no-undef */` for Node.js scripts (process, setTimeout) since ESLint config doesn't set node env
- tsx is installed as devDependency for running TypeScript scripts
- nodenext moduleResolution requires `.js` extensions for relative imports — applies to barrel exports (index.ts) and any cross-file imports
- Types package (packages/types/src/index.ts) needs `.js` extensions in re-exports for nodenext compatibility
- Nx sync may revert manual tsconfig reference changes — let Nx manage project references
- Rspack DefinePlugin: add env vars to envVars object with process.env fallback, then JSON.stringify in DefinePlugin
- Nx typecheck for web fails (pre-existing) — use `tsc --noEmit -p packages/web/tsconfig.app.json` instead
- Mock mode code lives in packages/web/src/mock/
- Use React.lazy + dynamic import() inside build-time guard for mock-only components — enables Rspack dead-code elimination
- Playwright tests need `--config packages/e2e/playwright.config.ts` flag (bare `npx playwright test` picks up rstest files)
- MockWrapper pattern in app.tsx: conditionally wrap children with lazy-loaded provider using `if (!Comp) return <>{children}</>` guard
- Playwright full suite (86 tests) is slow; use smoke test (`packages/e2e/tests/smoke.spec.ts`) for quick validation
- AuthContext and AuthContextValue are exported from AuthContext.tsx — mock/test providers can reuse the same context
- Mock auth pattern: MockAuthProvider in main.tsx uses same AuthContext, lazy-loaded behind build-time guard
- Mock TanStack Query pattern: use makeMockQuery() helper that returns { data, isLoading: false, error: null, ... } matching UseQueryResult shape
- Mock TanStack Mutation pattern: use makeMockMutation() helper that returns { mutateAsync, mutate, isLoading: false, ... } matching UseMutationResult shape
- DraftPage inline useDraft returns Supabase snake_case: { draft_order, current_pick, draft_picks: [{ pick_number, player_id, league_members: { team_name, user_id } }] }
- Snake draft order is pre-expanded into flat array (members × rounds) — generateSnakeDraftOrder in useMockDraft.ts
- LeagueMember.user is typed as User (camelCase: displayName, avatarUrl), not snake_case — but Supabase responses use snake_case (display_name, avatar_url)
- Bot names pool: pull from fixture data players for realistic names
- Bot auto-pick: use BotAutoPickRunner (invisible component) inside MockDataProvider — triggers useBotAutoPick hook that monitors draft state
- Bot strategy: pick best available player by greatest positional need (5F/3D/2G targets), alphabetical tiebreaker
- Browser globals (setTimeout/clearTimeout) need `/* eslint-disable no-undef */` in web package files too
- Simulation state: accumulatePlayerStats and getRoundDateBounds are exported from MockDataProvider for reuse by standings/roster/scoring hooks
- Round game fixtures mapped in ROUND_GAMES: { 1: gamesR1, 2: gamesR2, 3: gamesCf, 4: gamesScf }
- SimulationControlPanel lives in packages/web/src/mock/components/ — must be inside MockDataProvider for context
- Mantine Drawer with position="right" for collapsible side panels; ActionIcon for FAB buttons
- Mock live games: useMockLiveGames returns MockLiveGame[] with game + seriesStatus; useMockLiveGamesTeamStats returns team_stats_cache shape; mockGetScoresNow returns NHLGame[]
- Series status computation: group games by sorted team ID pair (seriesKey), count wins per team through simulationDate
- Roster population on draft completion: MAKE_PICK reducer builds RosterSlot[] from picks + IR slots when isComplete; mock hooks read from state.rosters keyed by leagueMemberId
- Points calculation in mock: calculatePlayerPoints(playerId, throughDate) using SCORING constants and playerGameLogs fixture data
- Standings member shape from Supabase: { id, user_id, team_name, total_points, player_points, goalie_points, round_points, users: { display_name } }
- ScoringEvent shape: { id, player_name, team_abbreviation, event_type, points, game_date, league_member_team }
- Goalie win/shutout detection: match goalie teamAbbrev to game fixture scores; shutout (4pts) replaces win (2pts, not additive)
- Mock hook wiring pattern: `const IS_MOCK = import.meta.env.VITE_MOCK_MODE === 'true'` at module level, then `if (IS_MOCK) return useMockXxx()` inside inline hooks; mutations use `if (IS_MOCK && mockMutation)` branch before Supabase calls
- MockDataProvider exposes hooks registry via context (mockHooksRegistry.ts) — all mock hooks accessible through useMockData().hooks
- Bracket derivation: pure function (baseBracket, simulationDate, currentRound) → NHLPlayoffSeries[] with win counts overlaid from game fixtures

---

## 2026-02-15T05:15:00Z - US-001
- Created packages/mock-data/ Nx package scaffolding
- Files changed: packages/mock-data/package.json, project.json, tsconfig.json, tsconfig.lib.json, src/index.ts, root tsconfig.base.json, root tsconfig.json (Nx sync)
- **Learnings for future iterations:**
  - Follow existing package patterns: package.json with exports map, tsconfig.json + tsconfig.lib.json
  - Nx auto-detects packages in packages/ directory — no need to register manually
  - `nx show project @sportsnot/mock-data --json` verifies Nx recognition
  - Nx sync may modify root tsconfig.json references — always commit those changes
---

## 2026-02-15T05:31:00Z - US-002
- Created fixture data download script at packages/mock-data/src/scripts/download.ts
- Pipeline: bracket → team rosters → games by round → player game logs
- Includes rate limiting (10 concurrent, 100ms delay), error handling, idempotent file writes
- Files changed: packages/mock-data/src/scripts/download.ts, project.json (download target), tsconfig.lib.json (exclude scripts), tsconfig.scripts.json (new), README.md, package.json (tsx), yarn.lock, src/data/.gitkeep
- **Learnings for future iterations:**
  - tsconfig.lib.json uses `nodenext` moduleResolution which breaks cross-package imports via path aliases — scripts need separate tsconfig with `bundler` resolution
  - Don't include tsconfig.scripts.json in tsconfig.json references — it confuses Nx typecheck when `noEmit: true`
  - Download script uses @sportsnot/nhl-api functions directly — no need to reimplement API logic
  - NHL playoff game IDs encode round in position 8 (1-indexed) of the 10-digit ID
  - Nx `command` target in project.json supports `{workspaceRoot}` for cwd option
  - ESLint config has no node environment — use eslint-disable for `no-undef` in Node scripts
---

## 2026-02-15T05:44:00Z - US-003
- Created stub fixture data files with correct TypeScript types conforming to @sportsnot/types
- Files: teams.ts (4 teams: EDM, FLA, DAL, CAR), players.ts (3 players/team: F, D, G), bracket.ts (2 R1 series), games-r1.ts through games-scf.ts (2 games/round), player-game-logs.ts (2 entries/player)
- Updated index.ts barrel exports with .js extensions for nodenext compatibility
- Fixed packages/types/src/index.ts to use .js extensions in re-exports
- Files changed: packages/mock-data/src/data/*.ts (8 new), packages/mock-data/src/index.ts, packages/types/src/index.ts, packages/mock-data/tsconfig.lib.json
- **Learnings for future iterations:**
  - nodenext moduleResolution requires .js extensions in all relative imports (even for .ts files)
  - The types package index.ts had pre-existing nodenext incompatibility — needed .js extension fix
  - Nx sync manages project references automatically — manual edits to tsconfig references may be reverted
  - Playwright e2e tests fail when run via rstest (expected) — only unit tests (utils, nhl-api) run through rstest
  - `as const` on exported data arrays/objects works well with type imports for fixture data
---

## 2026-02-15T05:53:00Z - US-004
- Added VITE_MOCK_MODE to Rspack DefinePlugin in packages/web/rspack.config.js with 'false' default
- Added VITE_MOCK_MODE=false to .env.example with documentation comment
- Created packages/web/src/mock/utils.ts with isMockMode() utility function
- Created packages/web/src/env.d.ts with ImportMetaEnv type declarations for all VITE_ vars
- Files changed: packages/web/rspack.config.js, .env.example, packages/web/src/env.d.ts (new), packages/web/src/mock/utils.ts (new)
- **Learnings for future iterations:**
  - Rspack DefinePlugin pattern: add env var to `envVars` object with `process.env` fallback, then add `JSON.stringify` entry in DefinePlugin
  - Nx typecheck target (`tsc --build --emitDeclarationOnly`) fails for web package due to `composite: false` in tsconfig.app.json — this is pre-existing; use `tsc --noEmit -p tsconfig.app.json` to verify types
  - rspack.config.js has pre-existing eslint no-undef errors (require, process, module, __dirname) — these are expected for Node CJS files
  - The `packages/web/src/mock/` directory is the home for all mock mode code in the web package
  - Unit tests run with `yarn rstest <file paths>` — running just `yarn rstest` picks up Playwright files and fails
---

## 2026-02-15T05:57:00Z - US-005
- Created MockModeBanner.tsx with Mantine Alert (yellow, sticky, non-dismissible, z-index 1000)
- Added build-time guard in app.tsx: `import.meta.env.VITE_MOCK_MODE === 'true'` with React.lazy + dynamic import
- Banner renders above AppShell only when mock mode is active; null when inactive (tree-shaken)
- Files changed: packages/web/src/mock/MockModeBanner.tsx (new), packages/web/src/app/app.tsx
- **Learnings for future iterations:**
  - React.lazy + ternary pattern: `const Comp = guard ? lazy(() => import(...)) : null` — clean conditional loading
  - Wrap lazy component in `{Comp && <Suspense><Comp /></Suspense>}` for safe rendering
  - Playwright tests need explicit config flag: `npx playwright test --config packages/e2e/playwright.config.ts`
  - Banner placement: outside AppShell but inside ErrorBoundary for correct stacking
---

## 2026-02-15T06:00:00Z - US-006
- Created MockDataProvider.tsx with React context, useReducer, full MockState type, and MockAction union type
- MockState includes: leagues, currentLeague, draftState, rosters, simulationDate, currentRound, roundComplete, seasonComplete, playerStats, mockUser
- Actions defined: CREATE_LEAGUE, JOIN_LEAGUE, START_DRAFT, MAKE_PICK, ADVANCE_DAY, ADVANCE_ROUND, ACTIVATE_IR, DEACTIVATE_IR, RESET_ALL
- RESET_ALL handler clears all state to initial values; other actions are stubs returning state unchanged
- useMockData() hook provides state + dispatch from context
- MockDataProvider wired into app.tsx via React.lazy + dynamic import (same pattern as MockModeBanner)
- Created MockWrapper component in app.tsx that wraps children with MockDataProvider when mock mode is active
- Files changed: packages/web/src/mock/MockDataProvider.tsx (new), packages/web/src/app/app.tsx
- **Learnings for future iterations:**
  - MockDataProvider wraps inside ErrorBoundary but outside AppShell — same level as MockModeBanner
  - MockWrapper pattern: `if (!LazyComponent) return <>{children}</>; else return <Suspense><LazyComponent>{children}</LazyComponent></Suspense>`
  - Initial simulationDate is '2025-04-18' (day before first R1 game in fixture data)
  - Playwright full suite (86 tests) takes very long to run; use smoke test for quick validation
  - Navigation test failures are pre-existing (auth/profile route) — not caused by mock changes
---

## 2026-02-15T06:52:00Z - US-007
- Created mock auth provider that bypasses Supabase auth with automatic mock user login
- useMockAuth.ts returns deterministic mock user (id: mock-user-001, display_name: Mock User) with compatible Supabase User/Session types
- MockAuthProvider.tsx provides mock auth through the same AuthContext used by real AuthProvider
- main.tsx conditionally lazy-loads MockAuthProvider when VITE_MOCK_MODE=true, otherwise uses real AuthProvider
- Sign-out shows alert('🧪 Mock mode — auth disabled') and remains logged in
- All auth-gated routes (ProtectedRoute) work normally with mock user
- No Supabase network requests in mock mode (useAuth from @sportsnot/supabase is never called)
- Files changed: packages/web/src/mock/hooks/useMockAuth.ts (new), packages/web/src/mock/MockAuthProvider.tsx (new), packages/web/src/app/context/AuthContext.tsx (exported AuthContext + AuthContextValue), packages/web/src/main.tsx (conditional auth wrapper)
- **Learnings for future iterations:**
  - AuthContext and AuthContextValue are now exported from AuthContext.tsx — mock providers can reuse the same context
  - MockAuthProvider uses the same lazy-load + build-time guard pattern as MockModeBanner/MockDataProvider in main.tsx
  - Supabase User type has many required fields — use `as unknown as User` cast for mock objects with the fields actually used by app code
  - The AuthWrapper pattern in main.tsx mirrors the MockWrapper pattern in app.tsx
  - Sign-out in mock mode is a no-op — mock user stays logged in
---

## 2026-02-15T07:10:00Z - US-008
- Created packages/web/src/mock/hooks/useMockNhlApi.ts with mock implementations of NHL API functions and Supabase hooks
- Mock functions: mockGetPlayoffBracket, mockGetTeamRoster, mockGetPlayer, mockGetTeams, mockGetPlayoffSchedule — all return fixture data from @sportsnot/mock-data
- Mock hooks: useMockPlayoffPlayers and useMockPlayoffTeams — return data shaped like player_stats_cache and team_stats_cache tables, wrapped in TanStack Query-compatible response objects
- No network requests to NHL API or Supabase when mocks are active
- Files changed: packages/web/src/mock/hooks/useMockNhlApi.ts (new)
- **Learnings for future iterations:**
  - DraftPage.tsx consumes usePlayoffPlayers/usePlayoffTeams from @sportsnot/supabase — mock data must match the Supabase table column shapes (player_id, player_name, position, team_abbreviation, goals, assists, games_played, is_injured for players; team_id, team_name, team_abbreviation, wins, shutouts, is_eliminated for teams)
  - TanStack Query result shape needs: data, isLoading, isError, error, isFetching, isSuccess, status, refetch — a makeMockQuery helper function simplifies this
  - ESLint no-unused-vars doesn't respect underscore prefix — use eslint-disable comments for placeholder params
  - `as unknown as NHLGame[]` cast needed for `as const` fixture arrays to match mutable type signatures
---

## 2026-02-15T07:31:00Z - US-009
- Created packages/web/src/mock/hooks/useMockLeagues.ts with mock league hooks
- Hooks: useMockLeagues, useMockLeague, useMockCreateLeague, useMockJoinLeague
- Implemented CREATE_LEAGUE and JOIN_LEAGUE reducer actions in MockDataProvider.tsx
- Bot members generated with NHL player names from fixture data (players.ts)
- Mock leagues include `isMock: true` flag for UI badge display
- makeMockMutation helper mirrors makeMockQuery for TanStack mutation-shaped returns
- Files changed: packages/web/src/mock/hooks/useMockLeagues.ts (new), packages/web/src/mock/MockDataProvider.tsx
- **Learnings for future iterations:**
  - User type uses camelCase (displayName, avatarUrl) but Supabase responses from useLeague return snake_case (display_name, avatar_url) — mock hooks must map between the two
  - ESLint no-unused-vars flags function type parameter names in interfaces — use eslint-disable comments around interface blocks
  - makeMockMutation pattern: fn parameter called with `v` to avoid unused-var lint issues, uses `mutateAsync` and `mutate` to match TanStack mutation shape
  - CREATE_LEAGUE reducer sets currentLeague to the new league ID for navigation
  - Bot member IDs follow pattern `bot-member-{leagueId}-{index}`, user IDs follow `bot-user-{paddedIndex}`
---

## 2026-02-15T07:46:00Z - US-010
- Created packages/web/src/mock/hooks/useMockDraft.ts with mock draft hooks
- Hooks: useMockDraft, useMockStartDraft, useMockMakePick, useMockLeagueMembers
- Implemented START_DRAFT and MAKE_PICK reducer actions in MockDataProvider.tsx
- Snake draft ordering: generateSnakeDraftOrder creates 1-2-3...3-2-1 pattern for N members × T rounds
- Draft completion: when currentPick exceeds draftOrder length, status → 'completed', league → 'active'
- All hooks return Supabase snake_case shaped data (draft_order, current_pick, draft_picks, pick_number, league_members)
- Files changed: packages/web/src/mock/hooks/useMockDraft.ts (new), packages/web/src/mock/MockDataProvider.tsx
- **Learnings for future iterations:**
  - DraftPage uses inline useDraft hook (not from @sportsnot/supabase) that returns Supabase snake_case fields
  - Draft data shape from Supabase: { id, league_id, round, status, current_pick, draft_order, draft_picks: [{ pick_number, player_id, team_id, position, league_members: { team_name, user_id } }] }
  - Snake draft order is pre-expanded: draftOrder array contains all picks in sequence (e.g., 60 entries for 6 members × 10 rounds)
  - DraftPick.playerId/teamId in types are optional (number | undefined) but Supabase returns null — mock hooks use `?? null` for compatibility
  - Draft completion check: `nextPick > draftOrder.length` (1-indexed currentPick)
  - generateSnakeDraftOrder is exported for reuse by bot auto-pick (US-011)
---

## 2026-02-15T08:00:00Z - US-011
- Created bot auto-pick AI for mock drafts with 'best available by position need' strategy
- autoPick.ts: selectBotPick evaluates unfilled roster slots (5F/3D/2G targets), picks best available by positional need with alphabetical tiebreaker
- useBotAutoPick.ts: React hook monitors draft state, triggers MAKE_PICK dispatch after 1-2s random delay when it's a bot's turn
- BotAutoPickRunner.tsx: invisible component mounted inside MockDataProvider that activates the hook
- Bot picks update draft board in real-time via state updates (same MAKE_PICK dispatch as manual picks)
- Bots cannot pick already-drafted players (filtered by availablePlayerIds)
- Files changed: packages/web/src/mock/bot/autoPick.ts (new), packages/web/src/mock/bot/useBotAutoPick.ts (new), packages/web/src/mock/bot/BotAutoPickRunner.tsx (new), packages/web/src/mock/MockDataProvider.tsx
- **Learnings for future iterations:**
  - BotAutoPickRunner pattern: invisible component inside provider that runs a hook — avoids conditional hook calls
  - Bot position counting uses draftOrder index to map picks back to user IDs (since picks store leagueMemberId, not userId)
  - ESLint no-undef applies to setTimeout/clearTimeout in web package too — use eslint-disable comment
  - useEffect with state dependency re-fires on each state change, which naturally chains bot picks (bot1 picks → state changes → hook re-runs → bot2 picks)
  - Goalie picks use teamId (not playerId) matching the real DraftPage pattern
---

## 2026-02-15T08:15:00Z - US-012
- Implemented ADVANCE_DAY and ADVANCE_ROUND reducer actions in MockDataProvider.tsx
- Created pure function accumulatePlayerStats(logs, throughDate) that sums per-game stats from player-game-logs.ts where gameDate <= throughDate
- Created getRoundDateBounds(round) to get firstDate/lastDate boundaries per round from fixture data
- ADVANCE_DAY: increments simulationDate by one day, recalculates cumulative playerStats, sets roundComplete when date >= round's lastDate, sets seasonComplete when round 4 completes
- ADVANCE_ROUND: increments currentRound (1→2→3→4), sets simulationDate to day before next round's first game, resets roundComplete, recalculates stats
- Both actions are no-ops when seasonComplete is true; ADVANCE_ROUND requires roundComplete=true
- RESET_ALL already resets to initial state (simulationDate='2025-04-18', currentRound=1, empty stats)
- Imported fixture game data (gamesR1/R2/Cf/Scf) and playerGameLogs from @sportsnot/mock-data
- Exported accumulatePlayerStats and getRoundDateBounds for use by future hooks (standings, roster, etc.)
- Files changed: packages/web/src/mock/MockDataProvider.tsx
- **Learnings for future iterations:**
  - Round date boundaries derived from fixture game files — each round's games define firstDate and lastDate
  - Stat accumulation is a pure function: (logs, throughDate) → cumulative stats map, suitable for use by standings/roster hooks
  - ADVANCE_DAY guards: no-op if seasonComplete or roundComplete (user must advance round first)
  - ADVANCE_ROUND guards: no-op if !roundComplete or seasonComplete or currentRound >= 4
  - `as unknown as NHLGame[]` cast needed because fixture data uses `as const` (readonly arrays)
  - addOneDay uses noon UTC to avoid DST edge cases in date arithmetic
---

## 2026-02-15T08:29:00Z - US-013
- Created SimulationControlPanel.tsx as a floating overlay with FAB (bottom-right) and Drawer panel
- Panel displays: current simulated date, current round label, status badges (Round Complete, Season Complete)
- Buttons: Next Day (ADVANCE_DAY), Advance Round (ADVANCE_ROUND), Reset Season (RESET_ALL with confirmation), Dump State to Console
- Next Day disabled when roundComplete or seasonComplete; shows contextual text
- Advance Round disabled until roundComplete; both disabled when seasonComplete with trophy indicator
- Reset uses double-click confirmation pattern (3s timeout)
- Wired into app.tsx via React.lazy + dynamic import inside MockWrapper (inside MockDataProvider for context access)
- Files changed: packages/web/src/mock/components/SimulationControlPanel.tsx (new), packages/web/src/app/app.tsx
- **Learnings for future iterations:**
  - Mantine v8 Drawer works well for collapsible side panels - position right with size sm
  - SimulationControlPanel must be inside MockDataProvider to access useMockData context
  - FAB pattern: ActionIcon with fixed position, high z-index, and box-shadow for visibility
  - console.log needs eslint-disable-next-line no-console comment (ESLint only allows warn/error)
  - Double-click confirmation pattern with setTimeout fallback is simpler than Modal for destructive actions
---

## 2026-02-15T08:49:00Z - US-014
- Created packages/web/src/mock/hooks/useMockLiveGames.ts with three mock implementations
- useMockLiveGames(): Returns games for current simulationDate with series status (e.g., "FLA leads 2-1"), wrapped in MockLiveGamesResult with hasGames/message fields
- useMockLiveGamesTeamStats(): Returns team_stats_cache shaped data (team_id, team_name, team_abbreviation, wins, shutouts, is_eliminated) — drop-in replacement for DashboardPage's inline useLiveGames()
- mockGetScoresNow(): Standalone function returning NHLGame[] for a given date — replaces getScoresNow() from @sportsnot/nhl-api
- Series status computed by grouping games by sorted team ID pair, counting wins through simulationDate
- No network requests to NHL API or Supabase when mocks are active
- Files changed: packages/web/src/mock/hooks/useMockLiveGames.ts (new)
- **Learnings for future iterations:**
  - Series key pattern: sort two team IDs and join with '-' for deterministic matchup identification
  - bracket fixture may have undefined topSeedTeam/bottomSeedTeam — use optional chaining for safety
  - ROUND_GAMES mapping already exists in MockDataProvider — duplicated locally for decoupling (avoids circular dependency)
  - Three-level mock hierarchy: (1) raw function (mockGetScoresNow), (2) enriched hook with series status (useMockLiveGames), (3) Supabase-shaped hook (useMockLiveGamesTeamStats)
  - Playwright smoke test requires serve-static running on localhost:4200
---

## 2026-02-15T09:08:00Z - US-015
- Created packages/web/src/mock/hooks/useMockRoster.ts with mock roster management hooks
- Hooks: useMockRoster, useMockLeagueRosters, useMockActivateIR, useMockDeactivateIR
- useMockRoster returns roster for current user in Supabase snake_case shape (memberId, round, slots) with points_earned calculated from fixture data through simulationDate
- useMockLeagueRosters returns all rosters for all members in a league with nested league_members data
- ACTIVATE_IR reducer action: marks IR slot as active + activatedFromIr, deactivates matching position player
- DEACTIVATE_IR reducer action: marks slot as inactive, clears activatedFromIr flag
- MAKE_PICK reducer now populates rosters on draft completion: builds RosterSlot entries from picks + adds IR_F and IR_D empty slots per member
- Added Position import to MockDataProvider.tsx for IR slot typing
- Files changed: packages/web/src/mock/hooks/useMockRoster.ts (new), packages/web/src/mock/MockDataProvider.tsx
- **Learnings for future iterations:**
  - RosterPage uses inline useMyRoster hook (not @sportsnot/supabase export) that returns Supabase snake_case fields (is_active, points_earned, activated_from_ir, player_id, team_id)
  - Roster population happens in MAKE_PICK reducer when draft completes (isComplete=true) - builds slots from all draft picks grouped by member
  - Each member gets 10 drafted slots (5F+3D+2G) plus 2 IR slots (IR_F, IR_D) with isActive=false
  - Points calculation uses SCORING constants (goal=1, assist=1) and playerGameLogs fixture data filtered by throughDate
  - ACTIVATE_IR finds matching position (IR_F->F, IR_D->D) and deactivates the first active player at that position
---

## 2026-02-15T09:30:00Z - US-016
- Created useMockStandings.ts with mock standings hook calculating total_points, player_points, goalie_points, and round_points per member
- Created useMockScoringHistory.ts with mock scoring history hook generating individual scoring events (goals, assists, wins, shutouts) from fixture data
- Goalie win/shutout detection: matches goalie teamAbbrev to game fixture scores; shutout (4pts) replaces win (2pts)
- Standings update on each ADVANCE_DAY via recalculation from fixture data through simulationDate
- Files changed: packages/web/src/mock/hooks/useMockStandings.ts (new), packages/web/src/mock/hooks/useMockScoringHistory.ts (new)
- **Learnings for future iterations:**
  - Standings member shape from Supabase: { id, user_id, team_name, total_points, player_points, goalie_points, round_points: Record<number, number>, users: { display_name } }
  - ScoringEvent shape: { id, player_name, team_abbreviation, event_type: 'goal'|'assist'|'win'|'shutout', points, game_date, league_member_team }
  - Goalie win/shutout detection: match goalie's teamAbbrev to game fixture homeTeam/awayTeam, compare scores; shutout replaces win (4pts, not 4+2=6)
  - Round assignment for round_points: use getRoundDateBounds(r) to check if gameDate falls within round's date range
  - Shared patterns across mock hooks: getPlayerInfo() lookup, GAME_MAP for game data, makeMockQuery helper
---

## 2026-02-15T09:52:00Z - US-017
- Created packages/web/src/mock/hooks/useMockPlayoffBracket.ts with mock playoff bracket hook
- useMockPlayoffBracket(): Returns bracket data derived from simulation progress, wrapped in TanStack Query shape with { bracket, champion } payload
- deriveBracketState(): Pure function that overlays win counts from fixture game data through simulationDate onto the base bracket.ts fixture
- Before simulation: all teams shown in correct seeding positions with 0-0 records
- During simulation: series scores updated from game results through simulationDate
- After a round: series winners shown (isComplete=true, seriesWinner set), losers eliminated
- After round 4 (seasonComplete): Stanley Cup champion extracted from final series winner
- mockGetPlayoffBracketForDate(): Async function replacement for getPlayoffBracket() returning NHLPlayoffSeries[]
- Series win counting reuses same seriesKey pattern as useMockLiveGames (sorted team IDs)
- Files changed: packages/web/src/mock/hooks/useMockPlayoffBracket.ts (new)
- **Learnings for future iterations:**
  - Bracket derivation is a pure function of (fixtureData, simulationDate, currentRound) — no side effects
  - Win counts computed per-series by matching game fixture team IDs to bracket series topSeed/bottomSeed IDs
  - Series completion: topSeedWins >= 4 || bottomSeedWins >= 4 triggers isComplete + seriesWinner
  - For rounds beyond currentRound, return teams but with 0-0 records and isComplete=false
  - Champion detection: find round 4 series where isComplete=true and seasonComplete flag is set
  - seriesKey pattern (sorted team IDs joined with '-') is reused across multiple mock hooks — could be extracted to shared util
---

## 2026-02-15 02:27 - US-018
- Implemented: Wire mock hooks into MockDataProvider
- Created mockHooksRegistry.ts with typed registry of all mock hooks
- Updated MockDataProvider to expose hooks registry through context
- Created useMockMyLeagues adapter for DashboardPage shape compatibility
- Wired 8 page components with build-time IS_MOCK guard pattern:
  - DashboardPage (useMyLeagues, useLiveGames)
  - DraftPage (useDraft, useLeagueMembers, usePlayoffPlayers, usePlayoffTeams, handleConfirmPick, realtime no-op)
  - DraftLobbyPage (useLeagueForLobby, useActiveDraftCheck, handleStartDraft)
  - RosterPage (useMyRoster, handleActivateIR)
  - StandingsPage (useStandings)
  - LeagueDashboardPage (useLeague)
  - CreateLeaguePage (handleSubmit with useMockCreateLeague)
  - ScoringHistoryPage (useScoringHistory)
- Files changed:
  - packages/web/src/mock/mockHooksRegistry.ts (new)
  - packages/web/src/mock/MockDataProvider.tsx
  - packages/web/src/mock/hooks/useMockLeagues.ts
  - packages/web/src/app/routes/dashboard/DashboardPage.tsx
  - packages/web/src/app/routes/draft/DraftPage.tsx
  - packages/web/src/app/routes/draft/DraftLobbyPage.tsx
  - packages/web/src/app/routes/roster/RosterPage.tsx
  - packages/web/src/app/routes/standings/StandingsPage.tsx
  - packages/web/src/app/routes/leagues/LeagueDashboardPage.tsx
  - packages/web/src/app/routes/leagues/CreateLeaguePage.tsx
  - packages/web/src/app/routes/scoring/ScoringHistoryPage.tsx
- **Learnings for future iterations:**
  - Build-time guard pattern: const IS_MOCK = import.meta.env.VITE_MOCK_MODE === 'true' + if (IS_MOCK) return useMockXxx() inside inline hooks — Rspack eliminates dead branch at build time
  - ESLint react-hooks/rules-of-hooks plugin is NOT installed — eslint-disable comments for it produce "Definition not found" warnings (harmless)
  - DashboardPage expects league-centric shape (league as top-level with league_members array), while mock league data is stored in camelCase — useMockMyLeagues adapter bridges this gap
  - For mutation handlers (handleConfirmPick, handleStartDraft, handleActivateIR, handleSubmit), add if (IS_MOCK && mockMutation) branch BEFORE the Supabase calls
  - DraftLobbyPage needed custom useLeagueForLobby and useActiveDraftCheck wrapper hooks to cleanly separate mock vs real queries
  - Mock hooks that use useMockData() must be called inside the React tree (not at module level) — they access context
---
