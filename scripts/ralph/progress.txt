# Ralph Progress Log
Started: 02/16/2026 18:44:09

## Codebase Patterns
- Use `calculateRoundMemberPoints(playerIds, goalieTeamIds, fromDate, throughDate)` from `utils.ts` for all round-specific scoring — never filter by upper bound only
- `getRoundDateBounds(round)` from `MockDataProvider.tsx` returns `{ firstDate, lastDate }` for each NHL playoff round
- Test framework: `@rstest/core` with `describe/it/expect` — NOT vitest or jest
- Mock hooks follow `makeMockQuery<T>(data)` pattern to simulate TanStack Query return shapes
- Line endings: run `npx eslint --fix` on new files to fix CRLF→LF issues before committing
- E2e tests (Playwright) have 67 pre-existing failures on main; 20 pass — baseline not caused by changes
- Round date ranges: R1=04-19/05-04, R2=05-05/05-18, R3(CF)=05-20/05-29, R4(SCF)=06-04/06-17
- Table name is `rosters` (not `roster_slots`) — confirmed in Supabase migration 001_initial_schema.sql
- DraftPage.tsx has TWO draft history sections: post-draft-complete (~line 610) and live-draft (~line 782) — edit the correct one
- SKIP_TO_ROUND4 reducer must copy R3→R4 rosters + archive to rosterHistory — ADVANCE_ROUND may not have run first
- `league_members.total_points` is refreshed by `refresh_league_standings` RPC — use for cumulative totals
- Production and mock roster hooks return same shape: `{ memberId, round, slots, totalPoints }`
---

## 2026-02-17T02:44 - US-001
- Implemented: Refactored useMockRoster.ts to use round-specific date bounds for points calculation
- Removed standalone `calculatePlayerPoints` and `calculateGoaliePoints` that only had upper-bound filtering
- Added `calculateSlotPoints` helper that uses `getRoundDateBounds(slot.round)` + `calculateRoundMemberPoints`
- Both `useMockRoster` and `useMockLeagueRosters` now compute per-slot points within correct round date range
- Files changed:
  - `packages/web/src/mock/hooks/useMockRoster.ts` (refactored)
  - `packages/web/src/mock/hooks/useMockRoster.test.ts` (new, 8 tests)
- **Learnings for future iterations:**
  - `calculateRoundMemberPoints` in utils.ts is the canonical scoring function — always prefer it
  - Slot type has `round` field that maps to getRoundDateBounds for date filtering
  - When slot has both playerId and teamId, playerId takes precedence (player, not goalie)
---

## 2026-02-17T03:01 - US-002
- Implemented: Added `totalPoints` field to `useMockRoster` return value
- Imported `calculateMemberPoints` from utils.ts and called it with `(state, member.id)`
- Added `totalPoints: pts.totalPoints` to the makeMockQuery return object
- Added 4 unit tests verifying totalPoints contract (R1 equals slot sum, R2+ includes prior rounds, player+goalie breakdown, roundPoints sum)
- Files changed:
  - `packages/web/src/mock/hooks/useMockRoster.ts` (added import + totalPoints to return)
  - `packages/web/src/mock/hooks/useMockRoster.test.ts` (added 4 tests for totalPoints)
- **Learnings for future iterations:**
  - The useMockRoster return shape is `{ memberId, round, slots, totalPoints }` — consumers (RosterPage) will need to destructure totalPoints
  - Hook tests can't call hooks directly; test the pure functions that compose the hook's return value
  - CRLF line ending fix (`npx eslint --fix`) is always needed after editing files on Windows
---

## 2026-02-17T03:04 - US-003
- Implemented: Audited and fixed production (Supabase) roster points
- Edge function `sync-nhl-stats`: Added `gameDate` to `PlayerGameLog` interface, added `round_start_date`/`round_end_date` request params, filtered player game logs and team game scores by round date boundaries
- Production `useMyRoster` hook: Added `totalPoints` field from `league_members.total_points`, optimized by fetching in existing member query
- Added 12 unit tests for date filtering logic and production hook return shape contract
- Files changed:
  - `packages/supabase-db/functions/sync-nhl-stats/index.ts` (date filtering)
  - `packages/web/src/app/routes/roster/RosterPage.tsx` (totalPoints in hook)
  - `packages/web/src/app/routes/roster/productionRoster.test.ts` (new, 12 tests)
- **Learnings for future iterations:**
  - The edge function receives `playoff_round` param but needs explicit date range params (`round_start_date`/`round_end_date`) to filter NHL API data — the NHL API returns ALL playoff games, not per-round
  - Table name is `rosters` (not `roster_slots`) — confirmed in 001_initial_schema.sql migration
  - `league_members.total_points` is refreshed by `refresh_league_standings` RPC — use this for cumulative totals
  - Production hook query can include `total_points` in the initial `league_members` select to avoid a second query
  - Playwright e2e baseline: 20 pass, 67 pre-existing failures — unchanged by this story
---

## 2026-02-17T04:15 - US-004
- Implemented: Updated Roster page to show both round-specific and cumulative total points
- Renamed `totalPoints` variable to `roundPoints` (sum of active slots' points_earned in current round)
- Added `totalPoints` from `data.totalPoints` (cumulative across all rounds from hook)
- Updated header to show two side-by-side Mantine Card components: "Round N Points" and "Total Points"
- Added 7 unit tests for roundPoints/totalPoints display logic
- Files changed:
  - `packages/web/src/app/routes/roster/RosterPage.tsx` (header cards, variable rename)
  - `packages/web/src/app/routes/roster/productionRoster.test.ts` (7 new tests)
- **Learnings for future iterations:**
  - The RosterPage has a local `roundPoints` computed from slots and a `totalPoints` from hook data — keep these separate
  - Both mock and production hooks now return `totalPoints` in the data shape — consumers just use `data.totalPoints`
  - When adding Mantine Card components side-by-side, wrap in `<Group gap="md">` for consistent spacing
---

## 2026-02-17T04:18 - US-005
- Implemented: Made draft history scrollable during live draft
- Removed `.slice(0, 10)` limit from draft history picks rendering
- Wrapped the picks `Stack` in `<ScrollArea h={400}>` for scrollable overflow
- All picks now visible, sorted descending by pick_number (most recent first)
- Added 6 unit tests for sort logic, no-truncation, immutability, and large draft handling
- Files changed:
  - `packages/web/src/app/routes/draft/DraftPage.tsx` (ScrollArea wrap, removed slice)
  - `packages/web/src/app/routes/draft/DraftPage.test.ts` (new, 6 tests)
- **Learnings for future iterations:**
  - `ScrollArea` is already imported in DraftPage.tsx (used in post-draft-complete section) — no new imports needed
  - Playwright e2e tests require a running dev server on localhost:4200 — they all fail without it
  - DraftPage has two draft history sections: one for draft-complete (lines ~610-644) and one for live draft (~782-816) — make sure to edit the right one
---

## 2026-02-17T04:31 - US-006
- Implemented: Added My Team collapsible section to DraftPage during live draft
- Added `useDisclosure` hook for collapse state, `Collapse` and `UnstyledButton` from Mantine
- Built `myRosterSlots` useMemo that groups user's picks by position (F, D, G, IR_F, IR_D) using ROSTER_COMPOSITION constants
- Each position group shows filled picks with player/team names and Badge, plus "Empty X slot" placeholders for unfilled positions
- Section is collapsed by default, placed between Draft History and Available Players cards
- Added 6 unit tests for roster grouping logic (empty state, position grouping, other user exclusion, full slots, goalie team_id, real-time updates)
- Files changed:
  - `packages/web/src/app/routes/draft/DraftPage.tsx` (imports, useDisclosure, myRosterSlots useMemo, My Team JSX section)
  - `packages/web/src/app/routes/draft/DraftPage.test.ts` (6 new tests for My Team grouping)
- **Learnings for future iterations:**
  - `useDisclosure` from `@mantine/hooks` is the idiomatic way to handle Mantine Collapse toggle state
  - `ROSTER_COMPOSITION` from `@sportsnot/types` has the slot limits: forwards=5, defensemen=3, goalies=1, irForwards=1, irDefensemen=1
  - The `mySlotCounts` useMemo already exists for position pre-selection logic — `myRosterSlots` extends this with actual pick objects
  - `resolvePickName(player_id, team_id, playerNameMap, teamNameMap)` handles both player and goalie (team-based) pick name resolution
---

## 2026-02-17T04:35 - US-007
- Implemented: Fixed SKIP_TO_ROUND4 mock reducer to copy R3 rosters to R4
- Added idempotent check: if R4 rosters already exist (from ADVANCE_ROUND), skip copy
- Archives current R3 rosters to rosterHistory before copying
- Copies R3 slots to R4 with round=4 and pointsEarned=0 for all members
- Replaced old "does not modify rosters" test with 5 new tests covering copy, archive, idempotency, empty state, and multi-member
- Files changed:
  - `packages/web/src/mock/MockDataProvider.tsx` (SKIP_TO_ROUND4 reducer case expanded)
  - `packages/web/src/mock/MockDataProvider.test.ts` (5 new tests replacing 1 old test)
- **Learnings for future iterations:**
  - SKIP_TO_ROUND4 is called from RoundTransitionPage when skipping the R4 draft — it must handle roster carry-over independently of ADVANCE_ROUND
  - Idempotent check uses `slots.some(s => s.round === 4)` on existing rosters to detect if ADVANCE_ROUND already ran
  - The ADVANCE_ROUND reducer is the model for roster archival + copy logic — keep them in sync
---

## 2026-02-17T04:40 - US-008
- Implemented: Fixed production handleSkipToRound4 roster copy table name
- Changed `roster_slots` → `rosters` in both the SELECT and INSERT queries in handleSkipToRound4
- Verified correct table name is `rosters` per 001_initial_schema.sql migration
- Production useMyRoster (RosterPage.tsx) already uses `rosters` — this was a mismatch
- Added 8 unit tests for the R3→R4 copy transformation logic (id stripping, round/points reset, position types, multi-member)
- Files changed:
  - `packages/web/src/app/routes/draft/RoundTransitionPage.tsx` (table name fix: roster_slots → rosters)
  - `packages/web/src/app/routes/draft/RoundTransitionPage.test.ts` (new, 8 tests)
- **Learnings for future iterations:**
  - Always verify Supabase table names against 001_initial_schema.sql — the table is `rosters`, not `roster_slots`
  - handleSkipToRound4 strips the `id` field from R3 slots so Supabase auto-generates new UUIDs for R4
  - The eslint no-unused-vars rule requires destructured-but-unused vars to be prefixed with `_` (e.g., `{ id: _id, ...rest }`)
---

## 2026-02-17T04:44 - US-009
- Implemented: Hidden 'Start Next Draft' button after Round 3, added 'Advance to Finals' button
- Added `league.current_round < 3` condition to 'Start Next Draft' visibility
- Added 'Advance to Finals' button: visible when `isCommissioner && currentRound === 3 && roundComplete && !seasonComplete`
- 'Advance to Finals' navigates to `/draft/{leagueId}/transition` (existing RoundTransitionPage handles R3->R4)
- Added 16 unit tests for button visibility logic (show/hide conditions, mutual exclusivity, disabled states)
- Files changed:
  - `packages/web/src/app/routes/leagues/LeagueDashboardPage.tsx` (button conditions)
  - `packages/web/src/app/routes/leagues/LeagueDashboardPage.test.ts` (new, 16 tests)
- **Learnings for future iterations:**
  - 'Start Next Draft' and 'Advance to Finals' are mutually exclusive by design (round < 3 vs round === 3)
  - `handleStartNextDraft` sets league status to 'drafting' -- not needed for R4 (no draft), so 'Advance to Finals' uses direct navigation
  - `useRoundComplete(currentRound)` provides roundComplete and seasonComplete -- both needed for button logic
  - Prettier auto-fix via `npx eslint --fix` handles multi-line arrow function formatting
---
