{
  "project": "SportsNot",
  "branchName": "ralph/ml-draft-oracle-v2",
  "description": "Draft Oracle v2 — model and analysis improvements from tasks/prd-ml-model-improvements.md (PRD US-501..US-520, split here into right-sized stories; each title names its PRD story). READ FIRST, EVERY ITERATION: ml/SPEC.md (§3 stack, §6 leakage, §7 honesty), ml/README.md §Commands, scripts/ralph/progress.txt §Codebase Patterns, and the PRD section for the story you pick (its Description, Acceptance Criteria, plus the PRD's Glossary, Functional Requirements FR-1..FR-21 and Decisions §1–7). WORK FROM ml/: quality gates are `uv run ruff check .`, `uv run mypy`, `uv run pytest` (all must be clean; never run `uv run ruff format .` repo-wide — it rewrites 60 unrelated files); when any file outside ml/ changes also run `yarn nx affected -t lint typecheck test` and `yarn nx e2e @sportsnot/e2e` from the repo root. DATA: all raw sources are already committed (ml/data/raw/nhl-archive incl. skater-toi/skater-pp/goalie-games and game-times 2007-08..2025-26; ml/data/raw/nhl-shifts; ml/data/raw/moneypuck; ml/data/raw/odds-archive/the-odds-api-history as AES-256-GCM ciphertext opened only with ODDS_ARCHIVE_KEY from the environment or ml/.env — never print, log, or commit the key or any decrypted Odds API data; ml/data/raw/naturalstattrick holds only a failure report and is never used). Build `data/normalized` with `uv run oracle normalize --force`, `uv run oracle league-drafts`, `uv run oracle match-drafts`, `uv run oracle odds` before stories that need real data. Tests never touch the network and never read real Odds API data. EVIDENCE TIERS: champion evidence (ml/artifacts/models/*, ml/artifacts/backtests/*, ml/artifacts/2026-r*) shares the sha in ml/artifacts/EVIDENCE_PASS.json and is regenerated ONLY in the two full passes (US-505d, US-520a); every other story writes its challenger report + manifest under ml/artifacts/experiments/<STORY-ID>/ (US-500). HONESTY (SPEC §7): a missed target is reported with the real number and the challenger is not adopted; never weaken baselines, change splits, reroll seeds, or relabel data. CODESCENE MUST BE FULLY GREEN: every story carries the contract (function complexity <= 8, branches <= 8, statements <= 40, params <= 4 via dataclass requests, nesting <= 3, no multi-operator conditions, new modules <= 400 lines, never grow a module past 600 lines, <= 3 consecutive asserts in tests) enforced by the US-500 ruff rules and, when the CodeScene MCP tools exist, by `codescene-code_health_review` on every touched file before each commit; US-521 is a final sweep. Commit per story as `feat: [Story ID] - [Story Title]`; the repo pre-commit hook may fail only on missing CODESCENE_PAT — follow scripts/ralph/CLAUDE.md for that single case. Long-running stories (US-505d, US-506d, US-514b, US-520a: hours) must be started when nothing else is pending and their durations logged in progress.txt.",
  "userStories": [
    {
      "id": "US-500",
      "title": "Code-health proxy gate and experiments evidence tier (prerequisite for every story)",
      "description": "As the tool owner, I want the CodeScene rules enforced locally by ruff and a place for per-story challenger reports that does not break the shared-sha evidence guard, so every later story can commit green. Files: ml/pyproject.toml, ml/tests/backtest_fixtures.py, ml/tests/test_committed_model_evidence.py (read only), new ml/tests/test_experiments_evidence.py, ml/README.md, ml/SPEC.md §4.",
      "acceptanceCriteria": [
        "Read ml/SPEC.md, ml/README.md §Commands, scripts/ralph/progress.txt §Codebase Patterns, and tasks/prd-ml-model-improvements.md (Introduction, Glossary, Functional Requirements, Decisions) in full before starting",
        "In ml/pyproject.toml `[tool.ruff.lint]` add `\"C90\"`, `\"PLR0912\"`, `\"PLR0913\"`, `\"PLR0915\"`, `\"PLR0917\"` to `select`; add `[tool.ruff.lint.mccabe] max-complexity = 8` and `[tool.ruff.lint.pylint] max-args = 4`, `max-positional-args = 4`, `max-branches = 8`, `max-statements = 40`; keep `extend-exclude = [\"data\", \"artifacts\"]`",
        "Fix the only current violations: split `_four_round_archive` in ml/tests/backtest_fixtures.py (complexity 16, 16 branches, 43 statements) into per-round helper functions with identical output (prove identity with a test that compares the fixture frames before and after via a stored checksum or a direct equality against the pre-split output committed as a parquet/CSV fixture); `uv run ruff check .` reports zero errors",
        "Create the experiments tier: directory `ml/artifacts/experiments/` with a `README.md` explaining that each subfolder `<STORY-ID>/` holds one challenger `report.md` + `manifest.json`; add `ml/tests/test_experiments_evidence.py` that, for every `ml/artifacts/experiments/*/manifest.json`, asserts a 40-hex `git_sha`, `git_dirty == false`, an integer `seed`, and a `story` field equal to the folder name (skips cleanly when the folder has no manifests)",
        "Add `write_experiment_manifest(story: str, out_dir: Path, payload: Mapping[str, object]) -> Path` in a new module `ml/src/draft_oracle/experiments.py` that stamps `git_sha`/`git_dirty` from `provenance.py`, writes `manifest.json` deterministically (sorted keys, indent 2, trailing newline) and is unit-tested with a tmp_path",
        "Document both in ml/README.md (new section 'Code health contract' listing the numeric limits above, and 'Experiments evidence tier') and add the experiments folder to the SPEC §4 directory contract",
        "`uv run ruff check .`, `uv run mypy`, and `uv run pytest` all pass from ml/",
        "CodeScene contract: every function you add or edit has cyclomatic complexity <= 8, <= 8 branches, <= 40 statements, <= 4 parameters (bundle more into a frozen dataclass request object, as cli/_request_commands.py does), nesting depth <= 3, and no condition with more than one `and`/`or` (extract a named predicate function); `uv run ruff check .` passes with the US-500 rule set",
        "CodeScene contract: new code goes into NEW modules of <= 400 lines (private helpers in `_name.py` files next to the public module); never grow an existing module past 600 lines and never make a hotspot file longer than you found it; no copy-pasted block of 5+ lines (extract a shared helper)",
        "CodeScene contract: tests use at most 3 consecutive `assert` statements per test function (use a comparison helper or `pytest.mark.parametrize`) and never repeat the same assertion block in two tests",
        "CodeScene contract: if CodeScene MCP tools are available, run `codescene-code_health_review` on every .py file you touched before committing and fix every reported issue; if they are not available, say so in progress.txt and rely on the ruff proxy rules",
        "All Typecheck passes (even if it's outside of your changes)",
        "Lint passes (even if it's outside of your changes)",
        "Unit tests have been added",
        "All Unit tests pass (even if it's outside of your changes)",
        "All Playwright tests pass (even if it's outside of your changes)"
      ],
      "priority": 1,
      "passes": false,
      "notes": ""
    },
    {
      "id": "US-501a",
      "title": "Paired-bootstrap, decision-regret and seat-rank metrics as pure functions (PRD US-501 part 1 of 3)",
      "description": "As the tool owner, I want the statistical machinery of the champion/challenger harness as small, tested pure functions before any CLI is built. New module ml/src/draft_oracle/backtest/metrics.py (+ ml/tests/test_backtest_metrics.py). Read PRD US-501 and Glossary (Paired bootstrap, Decision regret) first.",
      "acceptanceCriteria": [
        "`paired_bootstrap_interval(a: np.ndarray, b: np.ndarray, *, seed: int, n_resamples: int = 2000, level: float = 0.95) -> BootstrapInterval` resamples draft indices with replacement (same indices for a and b), returns a frozen dataclass with `mean_diff`, `lower`, `upper`, `n_resamples`, `excludes_zero: bool`; deterministic for a given seed (test: two calls equal; test: identical inputs give interval [0,0])",
        "`decision_regret(available_by_turn: Sequence[frozenset[str]], picks: Sequence[str], actual_points: Mapping[str, float], roster_rule: RosterRule) -> float` returns hindsight-optimal legal roster points (best legal roster choosable from the players actually available at each of the owner's turns, scored on actuals through `rules.py`) minus the drafted roster's actual points; test on a 3-turn toy league where the optimum is known by hand",
        "`seat_rank(points_by_seat: Mapping[str, float], seat: str) -> int` returns 1 for the best seat, ties share the better rank; tested",
        "`season_outcomes(round_points: pd.DataFrame) -> SeasonOutcome` takes columns `season, league, round, seat, points, is_oracle` and returns per season-league: `oracle_mean_seat_wins_season: bool`, `rounds_best_seat_ge_league_best: int`, `rounds_total: int`; tested on a two-league synthetic frame reproducing the PRD's 2/4 and 7/12 style fractions",
        "All functions are typed (`mypy --strict`), have no I/O, and each has a docstring stating its formula",
        "CodeScene contract: every function you add or edit has cyclomatic complexity <= 8, <= 8 branches, <= 40 statements, <= 4 parameters (bundle more into a frozen dataclass request object, as cli/_request_commands.py does), nesting depth <= 3, and no condition with more than one `and`/`or` (extract a named predicate function); `uv run ruff check .` passes with the US-500 rule set",
        "CodeScene contract: new code goes into NEW modules of <= 400 lines (private helpers in `_name.py` files next to the public module); never grow an existing module past 600 lines and never make a hotspot file longer than you found it; no copy-pasted block of 5+ lines (extract a shared helper)",
        "CodeScene contract: tests use at most 3 consecutive `assert` statements per test function (use a comparison helper or `pytest.mark.parametrize`) and never repeat the same assertion block in two tests",
        "CodeScene contract: if CodeScene MCP tools are available, run `codescene-code_health_review` on every .py file you touched before committing and fix every reported issue; if they are not available, say so in progress.txt and rely on the ruff proxy rules",
        "All Typecheck passes (even if it's outside of your changes)",
        "Lint passes (even if it's outside of your changes)",
        "Unit tests have been added",
        "All Unit tests pass (even if it's outside of your changes)",
        "All Playwright tests pass (even if it's outside of your changes)"
      ],
      "priority": 2,
      "passes": false,
      "notes": ""
    },
    {
      "id": "US-501b",
      "title": "Backtest evaluation profile, decision metrics and league-outcome rows in report.md (PRD US-501 part 2 of 3)",
      "description": "As the tool owner, I want the backtest to compute the US-501a metrics per draft and to run at evidence power when asked. Files: ml/src/draft_oracle/cli/_project_training_commands.py (BacktestCommandRequest), ml/src/draft_oracle/backtest/replay.py, backtest/_replay_league.py, backtest/report.py, ml/tests/test_backtest.py, ml/tests/test_backtest_report.py.",
      "acceptanceCriteria": [
        "Add `profile: str = field(default=\"quick\", metadata=option(...))` to `BacktestCommandRequest` with choices `quick|evidence`; `evidence` forces `n_drafts >= 8` per (round, slot) and `rollouts >= 200` (raise a clear typer error if the user passes lower explicit values); `quick` keeps today's defaults; the profile name and the effective n_drafts/rollouts are written to `manifest.json` and to the first table of `report.md` with the sentence 'quick profile: not evidence-grade' when quick",
        "Each replayed draft records `decision_regret` and `seat_rank` (US-501a) for the oracle seat; `report.md` gains a 'Decision metrics' table with mean regret and mean seat rank per strategy per season plus pooled, each with a paired-bootstrap 95% interval vs `greedy_vor`",
        "`report.md` gains a 'League outcomes' table: for every replayed season-league, whether the oracle mean seat wins the season and the count of rounds where the oracle's best seat >= league best, plus the two pooled fractions written as `k of n`",
        "Existing tests keep passing unchanged; new tests use the committed synthetic backtest fixtures (tests/backtest_fixtures.py) and assert the new tables exist with the expected row counts and that `quick` reports carry the not-evidence-grade sentence",
        "ml/README.md §Backtest reporting documents the profile switch and the new tables",
        "CodeScene contract: every function you add or edit has cyclomatic complexity <= 8, <= 8 branches, <= 40 statements, <= 4 parameters (bundle more into a frozen dataclass request object, as cli/_request_commands.py does), nesting depth <= 3, and no condition with more than one `and`/`or` (extract a named predicate function); `uv run ruff check .` passes with the US-500 rule set",
        "CodeScene contract: new code goes into NEW modules of <= 400 lines (private helpers in `_name.py` files next to the public module); never grow an existing module past 600 lines and never make a hotspot file longer than you found it; no copy-pasted block of 5+ lines (extract a shared helper)",
        "CodeScene contract: tests use at most 3 consecutive `assert` statements per test function (use a comparison helper or `pytest.mark.parametrize`) and never repeat the same assertion block in two tests",
        "CodeScene contract: if CodeScene MCP tools are available, run `codescene-code_health_review` on every .py file you touched before committing and fix every reported issue; if they are not available, say so in progress.txt and rely on the ruff proxy rules",
        "All Typecheck passes (even if it's outside of your changes)",
        "Lint passes (even if it's outside of your changes)",
        "Unit tests have been added",
        "All Unit tests pass (even if it's outside of your changes)",
        "All Playwright tests pass (even if it's outside of your changes)"
      ],
      "priority": 3,
      "passes": false,
      "notes": ""
    },
    {
      "id": "US-501c",
      "title": "`oracle evaluate` champion/challenger command with frozen-champion guard and calibration report (PRD US-501 part 3 of 3)",
      "description": "As the tool owner, I want one command that replays the same seasons/rounds/seats for a challenger and the committed champion with paired seeds and prints every metric difference with a 95% interval. New modules: ml/src/draft_oracle/backtest/evaluate.py (+ _evaluate_report.py if needed), CLI registration via `register_request_command(app, \"evaluate\", EvaluateCommandRequest, run_evaluate)` in cli/_project_training_commands.py; tests in ml/tests/test_evaluate.py.",
      "acceptanceCriteria": [
        "`EvaluateCommandRequest` fields: `champion: Path` (a committed backtest run directory containing manifest.json), `challenger: Path` (a run directory or a projection artifact/config to replay), `seasons: list[int]`, `seed: int`, `profile: str = \"evidence\"`, `out_dir: Path`; both sides are replayed with the SAME seed sequence per draft (paired)",
        "Frozen-champion guard: the command reads the champion `manifest.json`, requires `git_dirty == false` and a 40-hex `git_sha` equal to `ml/artifacts/EVIDENCE_PASS.json`, and exits with a one-line error otherwise (tested with a tmp copy whose sha is altered)",
        "Output `report.md` starts with a verdict block (challenger better / worse / tie per metric, with the interval), then per-metric tables (mean points, win rate, decision regret, seat rank, season-win fraction, best-seat fraction) each with `diff`, `lower`, `upper`, `excludes_zero`",
        "Quantile calibration section: empirical coverage of skater p10/p90 and a reliability table (10 bins) for `p_series_win` on the replayed rounds, computed by a new pure function `reliability_table(pred: np.ndarray, obs: np.ndarray, bins: int = 10) -> pd.DataFrame` in backtest/metrics.py (tested)",
        "The command imports nothing from lightgbm/sklearn at startup (extend the blocked-import check in tests/test_cli.py to cover `evaluate --help`)",
        "ml/README.md documents `oracle evaluate` and restates SPEC §7: a challenger that loses is reported with its interval and not adopted",
        "CodeScene contract: every function you add or edit has cyclomatic complexity <= 8, <= 8 branches, <= 40 statements, <= 4 parameters (bundle more into a frozen dataclass request object, as cli/_request_commands.py does), nesting depth <= 3, and no condition with more than one `and`/`or` (extract a named predicate function); `uv run ruff check .` passes with the US-500 rule set",
        "CodeScene contract: new code goes into NEW modules of <= 400 lines (private helpers in `_name.py` files next to the public module); never grow an existing module past 600 lines and never make a hotspot file longer than you found it; no copy-pasted block of 5+ lines (extract a shared helper)",
        "CodeScene contract: tests use at most 3 consecutive `assert` statements per test function (use a comparison helper or `pytest.mark.parametrize`) and never repeat the same assertion block in two tests",
        "CodeScene contract: if CodeScene MCP tools are available, run `codescene-code_health_review` on every .py file you touched before committing and fix every reported issue; if they are not available, say so in progress.txt and rely on the ruff proxy rules",
        "All Typecheck passes (even if it's outside of your changes)",
        "Lint passes (even if it's outside of your changes)",
        "Unit tests have been added",
        "All Unit tests pass (even if it's outside of your changes)",
        "All Playwright tests pass (even if it's outside of your changes)"
      ],
      "priority": 4,
      "passes": false,
      "notes": ""
    },
    {
      "id": "US-502a",
      "title": "`ingest/sealed.py`: in-package AES-256-GCM open/seal with the ODDS_ARCHIVE_KEY contract (PRD US-502 part 1 of 3)",
      "description": "As the tool owner, I want the pipeline to decrypt the committed Odds API tarballs in memory using the same format the fetch tooling wrote (`ml/data/raw/odds-archive/the-odds-api-history/sealed_archive.py`: 12-byte nonce || AES-GCM ciphertext of a gzip of a deterministic tar). New module ml/src/draft_oracle/ingest/sealed.py; tests ml/tests/test_sealed.py with SYNTHETIC fixtures only. Add `cryptography>=42` to ml/pyproject.toml dependencies and refresh uv.lock.",
      "acceptanceCriteria": [
        "`seal(plaintext: bytes, key: bytes) -> bytes` and `open_sealed(blob: bytes, key: bytes) -> bytes` (nonce || ciphertext, AESGCM, no associated data) round-trip in a test and interoperate with `sealed_archive.py` (test: seal with one, open with the other, on a small tar built in tmp_path)",
        "`archive_key() -> bytes | None` reads `ODDS_ARCHIVE_KEY` from the environment, then from `ml/.env` (KEY=VALUE lines, no third-party dotenv library), urlsafe-base64-decodes it, and requires exactly 32 bytes; a malformed key raises `SealedKeyError` with the message 'ODDS_ARCHIVE_KEY must be urlsafe-base64 of 32 bytes' (tested); a missing key returns None",
        "`read_sealed_tar(path: Path, key: bytes) -> dict[str, bytes]` returns member name -> bytes, rejects members whose normalized path escapes the archive root (tested with a crafted `../x` member) and never writes plaintext to disk",
        "A wrong key produces `SealedKeyError('ODDS_ARCHIVE_KEY does not open <file>')` with no traceback in CLI use (tested by catching the exception type)",
        "No real Odds API data, no real key, and no ciphertext from `ml/data/raw/odds-archive/the-odds-api-history/*.tar.enc` is read in tests (the test module must not reference that directory)",
        "`ml/.gitignore` still ignores `.env`; README gets a 'Sealed odds archive' subsection with the key contract (env var, ml/.env, owner password manager, Claude Code environment secret) and the stat-only fallback promise",
        "CodeScene contract: every function you add or edit has cyclomatic complexity <= 8, <= 8 branches, <= 40 statements, <= 4 parameters (bundle more into a frozen dataclass request object, as cli/_request_commands.py does), nesting depth <= 3, and no condition with more than one `and`/`or` (extract a named predicate function); `uv run ruff check .` passes with the US-500 rule set",
        "CodeScene contract: new code goes into NEW modules of <= 400 lines (private helpers in `_name.py` files next to the public module); never grow an existing module past 600 lines and never make a hotspot file longer than you found it; no copy-pasted block of 5+ lines (extract a shared helper)",
        "CodeScene contract: tests use at most 3 consecutive `assert` statements per test function (use a comparison helper or `pytest.mark.parametrize`) and never repeat the same assertion block in two tests",
        "CodeScene contract: if CodeScene MCP tools are available, run `codescene-code_health_review` on every .py file you touched before committing and fix every reported issue; if they are not available, say so in progress.txt and rely on the ruff proxy rules",
        "All Typecheck passes (even if it's outside of your changes)",
        "Lint passes (even if it's outside of your changes)",
        "Unit tests have been added",
        "All Unit tests pass (even if it's outside of your changes)",
        "All Playwright tests pass (even if it's outside of your changes)"
      ],
      "priority": 5,
      "passes": false,
      "notes": ""
    },
    {
      "id": "US-502b",
      "title": "The Odds API snapshot parser into `odds_by_source` rows with two-sided de-vig and Pinnacle-preferred consensus (PRD US-502 part 2 of 3)",
      "description": "As the tool owner, I want the sealed snapshots turned into per-game rows in the existing odds schema. New module ml/src/draft_oracle/ingest/_odds_the_odds_api.py; tests ml/tests/test_odds_the_odds_api.py on hand-written synthetic JSON fixtures shaped like the API's historical response (read `ml/data/raw/odds-archive/the-odds-api-history/README.md` and `PROVENANCE.md` for the layout of `index` and `lines` members; never copy real payloads into tests).",
      "acceptanceCriteria": [
        "`parse_the_odds_api_snapshot(payload: Mapping[str, object], snapshot_ts: datetime) -> list[OddsRowGame]` (reuse `ingest/odds.py` `OddsRowGame`, `resolve_team_id`, `devig_proportional`; NEVER `devig_favorite_only`) emits one row per bookmaker per game for the `h2h` market, keeping `spreads`/`totals` prices in per-book columns where present",
        "Team names normalize through `resolve_team_id` after stripping punctuation and case (test: 'St. Louis Blues', 'St Louis Blues', 'Montréal Canadiens' all resolve); an unknown name is collected in a `unmatched_names` list on the result, never guessed",
        "`consensus_probability(rows: Sequence[OddsRowGame]) -> float` returns Pinnacle's de-vigged home probability when a `pinnacle` bookmaker row exists, else the median across books; tested for both branches and for the empty case (raises `ValueError`)",
        "Every row carries `source=\"the_odds_api\"`, `snapshot_ts`, `game_date` (local date via `ingest/_odds_consolidate.load_local_game_dates` logic or the index's `commence_time` converted to America/New_York), `home_implied`, `away_implied`, `book`; the two-sided de-vig sums to 1.0 within 1e-9 (property test with hypothesis)",
        "Regular-season daily `h2h` snapshots and playoff per-game snapshots are parsed by the same function; the fixture set includes one of each",
        "CodeScene contract: every function you add or edit has cyclomatic complexity <= 8, <= 8 branches, <= 40 statements, <= 4 parameters (bundle more into a frozen dataclass request object, as cli/_request_commands.py does), nesting depth <= 3, and no condition with more than one `and`/`or` (extract a named predicate function); `uv run ruff check .` passes with the US-500 rule set",
        "CodeScene contract: new code goes into NEW modules of <= 400 lines (private helpers in `_name.py` files next to the public module); never grow an existing module past 600 lines and never make a hotspot file longer than you found it; no copy-pasted block of 5+ lines (extract a shared helper)",
        "CodeScene contract: tests use at most 3 consecutive `assert` statements per test function (use a comparison helper or `pytest.mark.parametrize`) and never repeat the same assertion block in two tests",
        "CodeScene contract: if CodeScene MCP tools are available, run `codescene-code_health_review` on every .py file you touched before committing and fix every reported issue; if they are not available, say so in progress.txt and rely on the ruff proxy rules",
        "All Typecheck passes (even if it's outside of your changes)",
        "Lint passes (even if it's outside of your changes)",
        "Unit tests have been added",
        "All Unit tests pass (even if it's outside of your changes)",
        "All Playwright tests pass (even if it's outside of your changes)"
      ],
      "priority": 6,
      "passes": false,
      "notes": ""
    },
    {
      "id": "US-502c",
      "title": "Wire The Odds API source into `oracle odds`, consolidation preference, coverage tables and the ≥90-series market track (PRD US-502 part 3 of 3)",
      "description": "As the tool owner, I want `oracle odds` to decrypt the committed tarballs in memory when the key is present and to behave exactly as today when it is not. Files: ml/src/draft_oracle/ingest/odds.py (`build_odds_table`), ingest/_odds_consolidate.py (`consolidate_odds`), ingest/_odds_sources.py, cli/project.py `odds` command, models/_game_win_report.py, models/series_sim.py (Track 2), tests test_odds_build.py / test_odds_consolidation*.py / test_series_sim.py.",
      "acceptanceCriteria": [
        "`build_odds_table` gains a source step that, when `archive_key()` returns a key, opens each `ml/data/raw/odds-archive/the-odds-api-history/*.tar.enc` via US-502a, parses via US-502b, and appends rows with `source=\"the_odds_api\"`; with no key it logs one line 'the_odds_api: skipped (no ODDS_ARCHIVE_KEY)' and produces the same table as today (regression test: table equality with key absent)",
        "`consolidate_odds` prefers `the_odds_api` over SBR/Kaggle/ESPN when both cover a game and records the chosen source per row in a `source_used` column plus per-season counts in the consolidation stats (tests extend tests/odds_consolidation_helpers.py fixtures with a synthetic `the_odds_api` row)",
        "Derived per-game market probabilities from The Odds API are written only to gitignored `data/normalized/` outputs; a test asserts that no file under `ml/data/raw/odds-archive/the-odds-api-history/` other than `*.tar.enc`, `*.md`, `*.py`, `.gitignore`, `.gitattributes` is tracked by git (`git ls-files`)",
        "`oracle odds` prints a coverage table with per-season, per-source counts including `the_odds_api`; the game-win report (`models/_game_win_report.py`) shows the same per-season coverage for the market feature",
        "Series-sim Track 2 (market-aware) scores every postseason series 2020–2026 that has a game-1 line (expected ≥ 90) and prints the count; when the key is absent it prints the previous 14-series result and the sentence 'market track limited: no ODDS_ARCHIVE_KEY' (both paths tested with fixtures; the real-key path is exercised only by hand and its counts recorded in the story's experiment report)",
        "Evidence tier: this story writes `report.md` and `manifest.json` under `ml/artifacts/experiments/US-502c/` (manifest has `git_sha` of the code commit, `git_dirty: false`, seed, config, per-season and pooled metrics); do NOT add it to MODEL_ARTIFACTS / BACKTEST_ARTIFACTS in tests/test_committed_model_evidence.py and do NOT touch `ml/artifacts/EVIDENCE_PASS.json`; the US-500 experiments guard test covers it",
        "CodeScene contract: every function you add or edit has cyclomatic complexity <= 8, <= 8 branches, <= 40 statements, <= 4 parameters (bundle more into a frozen dataclass request object, as cli/_request_commands.py does), nesting depth <= 3, and no condition with more than one `and`/`or` (extract a named predicate function); `uv run ruff check .` passes with the US-500 rule set",
        "CodeScene contract: new code goes into NEW modules of <= 400 lines (private helpers in `_name.py` files next to the public module); never grow an existing module past 600 lines and never make a hotspot file longer than you found it; no copy-pasted block of 5+ lines (extract a shared helper)",
        "CodeScene contract: tests use at most 3 consecutive `assert` statements per test function (use a comparison helper or `pytest.mark.parametrize`) and never repeat the same assertion block in two tests",
        "CodeScene contract: if CodeScene MCP tools are available, run `codescene-code_health_review` on every .py file you touched before committing and fix every reported issue; if they are not available, say so in progress.txt and rely on the ruff proxy rules",
        "All Typecheck passes (even if it's outside of your changes)",
        "Lint passes (even if it's outside of your changes)",
        "Unit tests have been added",
        "All Unit tests pass (even if it's outside of your changes)",
        "All Playwright tests pass (even if it's outside of your changes)"
      ],
      "priority": 7,
      "passes": false,
      "notes": ""
    },
    {
      "id": "US-503a",
      "title": "Normalize the 2007-08..2025-26 archive: relocations, points-based seeding, era and scoring-environment features (PRD US-503 part 1 of 2)",
      "description": "As the tool owner, I want the already-committed 2007-08+ archive (`ml/data/raw/nhl-archive/`, see its PROVENANCE §7) flowing through `oracle normalize` with stable team identity and a seeding definition that does not depend on bracket format. Files: ml/src/draft_oracle/ingest/normalize.py, ingest/odds.py `resolve_team_id`, new features/era.py, tests test_normalize.py, test_features.py.",
      "acceptanceCriteria": [
        "`oracle normalize` ingests every `team-games-*`, `skater-games-*`, `skater-bios-*`, `game-times-*` and `bracket-*.json` file from 2007-08 onward; `resolve_team_id` maps ATL->WPG (2011), PHX->ARI (2014), ARI->UTA (2024) so a franchise keeps one `team_id` across seasons, and Utah's two API ids (59 and 68) collapse to one (tests for each mapping)",
        "`series` table gains `higher_seed_team_id` defined by regular-season points, tie-broken by regulation wins then goal differential, computed from `team_games`; a test covers one pre-2014 series (1–8 conference seeding era) and one post-2014 series and shows the definition is identical in both",
        "New `features/era.py`: `era_indicator(season_end_year: int) -> str` returning `pre2014` / `post2014` and `league_goals_per_game(team_games: pd.DataFrame) -> pd.Series` (per season); `features/skater.py` and `models/game_win.py` feature rows expose `era` and `goals_per_game_index` (season goals per game divided by the 2015–2019 mean) as as-of-safe columns (leakage test added in test_features.py)",
        "`oracle normalize` on the full archive completes and the row counts per season match `ml/data/raw/nhl-archive/PROVENANCE.md §7.2` (a test reads the PROVENANCE table and compares to the normalized parquet when `data/normalized` exists; skips with a clear reason otherwise)",
        "ml/README.md §Normalized tables documents the new columns and the relocation map",
        "CodeScene contract: every function you add or edit has cyclomatic complexity <= 8, <= 8 branches, <= 40 statements, <= 4 parameters (bundle more into a frozen dataclass request object, as cli/_request_commands.py does), nesting depth <= 3, and no condition with more than one `and`/`or` (extract a named predicate function); `uv run ruff check .` passes with the US-500 rule set",
        "CodeScene contract: new code goes into NEW modules of <= 400 lines (private helpers in `_name.py` files next to the public module); never grow an existing module past 600 lines and never make a hotspot file longer than you found it; no copy-pasted block of 5+ lines (extract a shared helper)",
        "CodeScene contract: tests use at most 3 consecutive `assert` statements per test function (use a comparison helper or `pytest.mark.parametrize`) and never repeat the same assertion block in two tests",
        "CodeScene contract: if CodeScene MCP tools are available, run `codescene-code_health_review` on every .py file you touched before committing and fix every reported issue; if they are not available, say so in progress.txt and rely on the ruff proxy rules",
        "All Typecheck passes (even if it's outside of your changes)",
        "Lint passes (even if it's outside of your changes)",
        "Unit tests have been added",
        "All Unit tests pass (even if it's outside of your changes)",
        "All Playwright tests pass (even if it's outside of your changes)"
      ],
      "priority": 8,
      "passes": false,
      "notes": ""
    },
    {
      "id": "US-503b",
      "title": "Ablation: series model with vs without 2007–2013 seasons (PRD US-503 part 2 of 2)",
      "description": "As the tool owner, I want to know whether the older seasons help the series model before every later story trains on them. Uses `eval-series-sim` (models/series_sim.py `evaluate_series_sim_from_normalized`) twice with a `--min-season` argument and writes an experiment report.",
      "acceptanceCriteria": [
        "`eval-series-sim` and `train-game-win` accept `--min-season YYYY` (default 2008, the first end-year in the archive) that filters training rows; documented in README",
        "Run the leave-future-out protocol (train <= t-1, test t, t in 2023..2026) for min-season 2008 vs 2014; report held-out Brier, log loss, and the 10-bin reliability table for both, plus the paired-bootstrap interval of the Brier difference (bootstrap over series) using backtest/metrics.py",
        "The report states in its first paragraph which setting is adopted as the default `--min-season` for later stories and why (the interval); if 2008 hurts, the default stays 2014 and the report says so",
        "The adopted default is set as the constant `DEFAULT_MIN_SEASON` in models/series_sim.py and read by both commands (test asserts the CLI default equals the constant)",
        "Evidence tier: this story writes `report.md` and `manifest.json` under `ml/artifacts/experiments/US-503b/` (manifest has `git_sha` of the code commit, `git_dirty: false`, seed, config, per-season and pooled metrics); do NOT add it to MODEL_ARTIFACTS / BACKTEST_ARTIFACTS in tests/test_committed_model_evidence.py and do NOT touch `ml/artifacts/EVIDENCE_PASS.json`; the US-500 experiments guard test covers it",
        "Honesty (SPEC §7): the target above is attempted and then reported truthfully; a miss is written into the report with the real number and the interval, and the challenger is NOT adopted; never change baselines, splits, seeds, or held-out years to manufacture a pass",
        "CodeScene contract: every function you add or edit has cyclomatic complexity <= 8, <= 8 branches, <= 40 statements, <= 4 parameters (bundle more into a frozen dataclass request object, as cli/_request_commands.py does), nesting depth <= 3, and no condition with more than one `and`/`or` (extract a named predicate function); `uv run ruff check .` passes with the US-500 rule set",
        "CodeScene contract: new code goes into NEW modules of <= 400 lines (private helpers in `_name.py` files next to the public module); never grow an existing module past 600 lines and never make a hotspot file longer than you found it; no copy-pasted block of 5+ lines (extract a shared helper)",
        "CodeScene contract: tests use at most 3 consecutive `assert` statements per test function (use a comparison helper or `pytest.mark.parametrize`) and never repeat the same assertion block in two tests",
        "CodeScene contract: if CodeScene MCP tools are available, run `codescene-code_health_review` on every .py file you touched before committing and fix every reported issue; if they are not available, say so in progress.txt and rely on the ruff proxy rules",
        "All Typecheck passes (even if it's outside of your changes)",
        "Lint passes (even if it's outside of your changes)",
        "Unit tests have been added",
        "All Unit tests pass (even if it's outside of your changes)",
        "All Playwright tests pass (even if it's outside of your changes)"
      ],
      "priority": 9,
      "passes": false,
      "notes": ""
    },
    {
      "id": "US-504a",
      "title": "Normalize MoneyPuck game aggregates into skater_xg / goalie_xg / team_xg tables with reproducibility tests (PRD US-504 part 1 of 3)",
      "description": "As the tool owner, I want the committed MoneyPuck data (`ml/data/raw/moneypuck/`, read its PROVENANCE.md and aggregate_shots.py header first) in the normalized store, keyed on NHL ids. New module ml/src/draft_oracle/ingest/moneypuck.py; tests ml/tests/test_moneypuck_normalize.py (tests/test_moneypuck.py already exists — extend, do not duplicate).",
      "acceptanceCriteria": [
        "`normalize_moneypuck(raw_dir: Path, out_dir: Path) -> MoneyPuckResult` reads `game-aggregates/{team-game-xg,skater-game-xg,goalie-game}-<season>.csv.gz` for every season and writes `data/normalized/skater_xg.parquet` (game_id, player_id, team_id, ixg, shots, xg_for_onice?, game_date), `goalie_xg.parquet` (game_id, goalie_id, team_id, shots_faced, xg_faced, goals_against, gsax, starter, shots_faced_share, game_date) and `team_xg.parquet` (game_id, team_id, opponent_id, is_home, xg_for, xg_against, shots_for, shots_against, high_danger_for, high_danger_against, game_date); MoneyPuck team codes (`T.B`, `L.A`, `N.J`, `S.J`, etc.) map to `team_id` through a small explicit dict in the module plus `resolve_team_id`; unmapped codes are listed in the result and raise if any remain",
        "`game_date` comes from `game-times-<season>.csv.gz` by `gameId`; every output table has `game_date` and is registered with `features/leakage.py` checks (test: `assert_no_leakage` rejects a row dated after a round cutoff)",
        "Reproducibility test: recompute the sha256 of every `shots/shots_<year>.zip` and compare to the values in `ml/data/raw/moneypuck/PROVENANCE.md` (parse the table; fail on any mismatch); then re-derive the 2012-13 season's three aggregate tables from `shots_2012.zip` by importing/invoking `aggregate_shots.py` logic in a tmp_path and assert byte identity with the committed `game-aggregates/*-2012-13.csv.gz` files (mark `@pytest.mark.slow` but it must run in the default `uv run pytest`)",
        "`oracle normalize` calls the new step and prints the three row counts; README §Normalized tables documents the tables and the documented MoneyPuck goals -1 mismatch pattern",
        "CodeScene contract: every function you add or edit has cyclomatic complexity <= 8, <= 8 branches, <= 40 statements, <= 4 parameters (bundle more into a frozen dataclass request object, as cli/_request_commands.py does), nesting depth <= 3, and no condition with more than one `and`/`or` (extract a named predicate function); `uv run ruff check .` passes with the US-500 rule set",
        "CodeScene contract: new code goes into NEW modules of <= 400 lines (private helpers in `_name.py` files next to the public module); never grow an existing module past 600 lines and never make a hotspot file longer than you found it; no copy-pasted block of 5+ lines (extract a shared helper)",
        "CodeScene contract: tests use at most 3 consecutive `assert` statements per test function (use a comparison helper or `pytest.mark.parametrize`) and never repeat the same assertion block in two tests",
        "CodeScene contract: if CodeScene MCP tools are available, run `codescene-code_health_review` on every .py file you touched before committing and fix every reported issue; if they are not available, say so in progress.txt and rely on the ruff proxy rules",
        "All Typecheck passes (even if it's outside of your changes)",
        "Lint passes (even if it's outside of your changes)",
        "Unit tests have been added",
        "All Unit tests pass (even if it's outside of your changes)",
        "All Playwright tests pass (even if it's outside of your changes)"
      ],
      "priority": 10,
      "passes": false,
      "notes": ""
    },
    {
      "id": "US-504b",
      "title": "Normalize NHL TOI reports and shift-derived deployment tables with TOI reconciliation (PRD US-504 part 2 of 3)",
      "description": "As the tool owner, I want per-game strength TOI, lines, PP units and dressed lineups in the normalized store, with the REST report as the authority on per-skater TOI. Inputs (all committed): `ml/data/raw/nhl-archive/skater-toi-*.csv.gz`, `skater-pp-*.csv.gz`, `goalie-games-*.csv.gz`, `ml/data/raw/nhl-shifts/derived/<season>/{skater-game-toi,lines,pp-units,dressed}.csv.gz` (read both PROVENANCE files, especially nhl-shifts §8–§10). New module ml/src/draft_oracle/ingest/deployment.py; tests ml/tests/test_deployment_normalize.py.",
      "acceptanceCriteria": [
        "`normalize_deployment(raw_root: Path, out_dir: Path) -> DeploymentResult` writes `skater_toi.parquet` (game_id, player_id, team_id, toi_all, toi_ev, toi_pp, toi_pk, shifts, toi_all_shifts, toi_pp_shifts, toi_conflict: bool, game_date) where `toi_*` come from the REST `skater-toi` report, `*_shifts` from the derived table, and `toi_conflict = abs(toi_all - toi_all_shifts) > 60`; `lines.parquet` (game_id, team_id, unit_type, player_ids as sorted list column, toi_5v5), `pp_units.parquet` (game_id, team_id, player_ids, toi_pp, pp_rank), `dressed.parquet` (game_id, team_id, player_id, position, toi_all), and `goalie_games.parquet` (game_id, goalie_id, team_id, started, toi, shots_against, goals_against, saves, shutout, decision, game_date)",
        "A test on committed data asserts the 2023-24 conflict share is below 1% and the 2019-20 share is reported (not asserted) in the test log; a unit test on a synthetic 3-row frame checks the flag arithmetic",
        "Byte-identity test: run `ml/data/raw/nhl-shifts/derive_deployment.py --season 2012-13` into a tmp copy (pass `--archive`) and compare the four produced gzip files byte-for-byte to the committed `derived/2012-13/` files (mark slow; must run by default; runtime under 60 s)",
        "`oracle normalize` runs the step; README documents the tables and the rule 'per-skater TOI from REST, composition from shifts'",
        "All five tables carry `game_date` and are covered by `assert_no_leakage` tests",
        "CodeScene contract: every function you add or edit has cyclomatic complexity <= 8, <= 8 branches, <= 40 statements, <= 4 parameters (bundle more into a frozen dataclass request object, as cli/_request_commands.py does), nesting depth <= 3, and no condition with more than one `and`/`or` (extract a named predicate function); `uv run ruff check .` passes with the US-500 rule set",
        "CodeScene contract: new code goes into NEW modules of <= 400 lines (private helpers in `_name.py` files next to the public module); never grow an existing module past 600 lines and never make a hotspot file longer than you found it; no copy-pasted block of 5+ lines (extract a shared helper)",
        "CodeScene contract: tests use at most 3 consecutive `assert` statements per test function (use a comparison helper or `pytest.mark.parametrize`) and never repeat the same assertion block in two tests",
        "CodeScene contract: if CodeScene MCP tools are available, run `codescene-code_health_review` on every .py file you touched before committing and fix every reported issue; if they are not available, say so in progress.txt and rely on the ruff proxy rules",
        "All Typecheck passes (even if it's outside of your changes)",
        "Lint passes (even if it's outside of your changes)",
        "Unit tests have been added",
        "All Unit tests pass (even if it's outside of your changes)",
        "All Playwright tests pass (even if it's outside of your changes)"
      ],
      "priority": 11,
      "passes": false,
      "notes": ""
    },
    {
      "id": "US-504c",
      "title": "On-ice xG per skater from shifts × shots, and starter/backup goalie time series (PRD US-504 part 3 of 3)",
      "description": "As the tool owner, I want who-was-on-ice shot quality per skater and a per-team starter/backup goalie quality series with as-of discipline. New modules ml/src/draft_oracle/features/onice.py and features/goalies.py; tests test_onice.py, test_goalies.py.",
      "acceptanceCriteria": [
        "`build_skater_onice_xg(shifts: pd.DataFrame, shots: pd.DataFrame) -> pd.DataFrame` joins each MoneyPuck shot (from the raw `shots_<year>.zip`, columns `game_id`, `period`, `time` in period seconds, `teamCode`, `xGoal`, `isHomeTeam`) to the skaters on ice at that second from `ml/data/raw/nhl-shifts/shifts-<season>.csv.gz` and produces per (game_id, player_id): `xgf_onice`, `xga_onice`, `toi_5v5`; the function is tested on a synthetic 2-shift, 3-shot game with hand-computed results; the full-archive run is invoked by `oracle normalize --onice` and writes `skater_onice_xg.parquet` with `game_date`",
        "`starting_goalie_by_game(goalie_games: pd.DataFrame) -> pd.DataFrame` returns one starter per (game_id, team_id) using `started == 1`, falling back to the goalie with the most TOI (>= 1800 s) when the flag is absent; tested including a two-goalie game",
        "`goalie_quality_series(goalie_games, goalie_xg) -> pd.DataFrame` gives per (team_id, as_of_date) rolling starter GSAx/60 and sv% over the last 25 starts and the same for the backup (second-most starts in the last 40 team games), computed strictly from games before `as_of_date` (leakage test: a game on the as-of date is excluded)",
        "Both outputs are documented in README §Team model features and are available to `models/game_win.py` `matchup_feature_row` via new optional columns (no behaviour change to the existing model in this story; regression test that `train-game-win` output metrics are unchanged when the new columns are absent)",
        "CodeScene contract: every function you add or edit has cyclomatic complexity <= 8, <= 8 branches, <= 40 statements, <= 4 parameters (bundle more into a frozen dataclass request object, as cli/_request_commands.py does), nesting depth <= 3, and no condition with more than one `and`/`or` (extract a named predicate function); `uv run ruff check .` passes with the US-500 rule set",
        "CodeScene contract: new code goes into NEW modules of <= 400 lines (private helpers in `_name.py` files next to the public module); never grow an existing module past 600 lines and never make a hotspot file longer than you found it; no copy-pasted block of 5+ lines (extract a shared helper)",
        "CodeScene contract: tests use at most 3 consecutive `assert` statements per test function (use a comparison helper or `pytest.mark.parametrize`) and never repeat the same assertion block in two tests",
        "CodeScene contract: if CodeScene MCP tools are available, run `codescene-code_health_review` on every .py file you touched before committing and fix every reported issue; if they are not available, say so in progress.txt and rely on the ruff proxy rules",
        "All Typecheck passes (even if it's outside of your changes)",
        "Lint passes (even if it's outside of your changes)",
        "Unit tests have been added",
        "All Unit tests pass (even if it's outside of your changes)",
        "All Playwright tests pass (even if it's outside of your changes)"
      ],
      "priority": 12,
      "passes": false,
      "notes": ""
    },
    {
      "id": "US-505a",
      "title": "Bivariate-Poisson (Dixon–Coles) goals model producing coherent P(win), P(shutout win) and margin (PRD US-505 part 1 of 4)",
      "description": "As the tool owner, I want one score-distribution model for team games so win, shutout and margin are consistent by construction. New modules ml/src/draft_oracle/models/goals.py (+ models/_goals_fit.py for the optimizer); tests test_goals.py. Fit with scipy/numpy only (already available via scikit-learn's dependency; add `scipy>=1.13` explicitly to pyproject).",
      "acceptanceCriteria": [
        "`GoalsModel.fit(games: pd.DataFrame, config: GoalsConfig) -> GoalsModel` estimates per-team attack and defence strengths, home advantage, a Dixon–Coles low-score correction `rho`, and coefficients for `starter_gsax` (from US-504c), `rest_days`, `is_playoff`, with L2 regularization `config.l2` toward zero; the fit is deterministic (fixed initial values, no randomness) and completes under 60 s on the 2010–2026 archive",
        "`GoalsModel.score_distribution(home_row, away_row, max_goals: int = 10) -> np.ndarray` returns the joint probability matrix; `p_home_win`, `p_shutout_win_home`, `p_shutout_win_away`, `expected_margin` are derived from that matrix in `models/goals.py` and a test proves `p_home_win + p_away_win == 1` (no ties in playoffs: overtime resolved by splitting the diagonal mass proportionally to the two teams' scoring rates, documented in the module docstring)",
        "`train-goals` CLI command (register via `register_request_command`) trains under the leave-future-out protocol (2023..2026) and writes `report.md` + `manifest.json` under the experiments tier with per-season and pooled Brier for wins and shutouts, compared against the committed game-win (Brier 0.2427) and shutout (0.0923) champions with paired-bootstrap intervals over games",
        "Unit tests: a synthetic league where team A scores twice as often as team B recovers `attack_A > attack_B`; the shutout probability of a 0.5-goal team is above that of a 3.0-goal team; the fit is invariant to team order",
        "Evidence tier: this story writes `report.md` and `manifest.json` under `ml/artifacts/experiments/US-505a/` (manifest has `git_sha` of the code commit, `git_dirty: false`, seed, config, per-season and pooled metrics); do NOT add it to MODEL_ARTIFACTS / BACKTEST_ARTIFACTS in tests/test_committed_model_evidence.py and do NOT touch `ml/artifacts/EVIDENCE_PASS.json`; the US-500 experiments guard test covers it",
        "Honesty (SPEC §7): the target above is attempted and then reported truthfully; a miss is written into the report with the real number and the interval, and the challenger is NOT adopted; never change baselines, splits, seeds, or held-out years to manufacture a pass",
        "CodeScene contract: every function you add or edit has cyclomatic complexity <= 8, <= 8 branches, <= 40 statements, <= 4 parameters (bundle more into a frozen dataclass request object, as cli/_request_commands.py does), nesting depth <= 3, and no condition with more than one `and`/`or` (extract a named predicate function); `uv run ruff check .` passes with the US-500 rule set",
        "CodeScene contract: new code goes into NEW modules of <= 400 lines (private helpers in `_name.py` files next to the public module); never grow an existing module past 600 lines and never make a hotspot file longer than you found it; no copy-pasted block of 5+ lines (extract a shared helper)",
        "CodeScene contract: tests use at most 3 consecutive `assert` statements per test function (use a comparison helper or `pytest.mark.parametrize`) and never repeat the same assertion block in two tests",
        "CodeScene contract: if CodeScene MCP tools are available, run `codescene-code_health_review` on every .py file you touched before committing and fix every reported issue; if they are not available, say so in progress.txt and rely on the ruff proxy rules",
        "All Typecheck passes (even if it's outside of your changes)",
        "Lint passes (even if it's outside of your changes)",
        "Unit tests have been added",
        "All Unit tests pass (even if it's outside of your changes)",
        "All Playwright tests pass (even if it's outside of your changes)"
      ],
      "priority": 13,
      "passes": false,
      "notes": ""
    },
    {
      "id": "US-505b",
      "title": "Hierarchical cross-season prior, xG-Elo variant, and post-hoc series calibration (PRD US-505 part 2 of 4)",
      "description": "As the tool owner, I want team strength to start each season from a shrunken prior, an xG-based Elo evaluated against the goals-based Elo, and series probabilities calibrated on validation years. Files: models/goals.py (prior), features/elo.py (xG variant), new models/calibration.py (isotonic + Platt with validation-only fitting), models/series_sim.py, tests.",
      "acceptanceCriteria": [
        "`GoalsConfig.prior_weight` shrinks each team's season-start strengths toward `prior_weight * last_season + (1 - prior_weight) * 0` (league mean); `prior_weight` is chosen on validation years by a small grid in `train-goals` and recorded in the manifest",
        "`features/elo.py` gains `xg_elo_ratings(team_xg: pd.DataFrame, k: float) -> pd.DataFrame` using MoneyPuck xG for/against instead of goals; the game-win report shows held-out Brier for goals-Elo vs xG-Elo with an interval and states which is kept (constant `ELO_VARIANT` in models/game_win.py)",
        "`models/calibration.py`: `fit_isotonic(pred, obs)`, `fit_platt(pred, obs)`, `apply(calibrator, pred)`; both are fit ONLY on validation years inside the leave-future-out loop; a test asserts the calibrator never sees test-season rows (pass a sentinel test season and check it is absent from the fitting frame)",
        "Series-sim held-out reliability: the 0.40–0.60 predicted bin's observed frequency is within ±0.10 of predicted after calibration, and the 7-game share is within ±5 points of observed; both are reported per season and pooled in the experiment report; if either misses, the report says so with the number",
        "Evidence tier: this story writes `report.md` and `manifest.json` under `ml/artifacts/experiments/US-505b/` (manifest has `git_sha` of the code commit, `git_dirty: false`, seed, config, per-season and pooled metrics); do NOT add it to MODEL_ARTIFACTS / BACKTEST_ARTIFACTS in tests/test_committed_model_evidence.py and do NOT touch `ml/artifacts/EVIDENCE_PASS.json`; the US-500 experiments guard test covers it",
        "Honesty (SPEC §7): the target above is attempted and then reported truthfully; a miss is written into the report with the real number and the interval, and the challenger is NOT adopted; never change baselines, splits, seeds, or held-out years to manufacture a pass",
        "CodeScene contract: every function you add or edit has cyclomatic complexity <= 8, <= 8 branches, <= 40 statements, <= 4 parameters (bundle more into a frozen dataclass request object, as cli/_request_commands.py does), nesting depth <= 3, and no condition with more than one `and`/`or` (extract a named predicate function); `uv run ruff check .` passes with the US-500 rule set",
        "CodeScene contract: new code goes into NEW modules of <= 400 lines (private helpers in `_name.py` files next to the public module); never grow an existing module past 600 lines and never make a hotspot file longer than you found it; no copy-pasted block of 5+ lines (extract a shared helper)",
        "CodeScene contract: tests use at most 3 consecutive `assert` statements per test function (use a comparison helper or `pytest.mark.parametrize`) and never repeat the same assertion block in two tests",
        "CodeScene contract: if CodeScene MCP tools are available, run `codescene-code_health_review` on every .py file you touched before committing and fix every reported issue; if they are not available, say so in progress.txt and rely on the ruff proxy rules",
        "All Typecheck passes (even if it's outside of your changes)",
        "Lint passes (even if it's outside of your changes)",
        "Unit tests have been added",
        "All Unit tests pass (even if it's outside of your changes)",
        "All Playwright tests pass (even if it's outside of your changes)"
      ],
      "priority": 14,
      "passes": false,
      "notes": ""
    },
    {
      "id": "US-505c",
      "title": "Market consensus as a shrunken feature and the ≥90-series market benchmark (PRD US-505 part 3 of 4)",
      "description": "As the tool owner, I want the goals model to use the US-502 de-vigged game-1 consensus with a weight fit on validation years, and to be judged against the pre-series market benchmark on every 2020–2026 series. Files: models/goals.py, models/series_sim.py Track 2, tests.",
      "acceptanceCriteria": [
        "`GoalsConfig.market_weight` blends `logit(p_model)` and `logit(p_market)` when a consensus line exists for the game; the weight is fit on validation years by grid and recorded; when no line exists the stat-only probability is used unchanged (test both branches)",
        "The series-sim market track pushes the game-1 consensus through the best-of-7 simulator to a series probability (the pre-series benchmark, PRD Glossary) and reports model vs market Brier on every 2020–2026 series with a game-1 line (count printed; target ≥ 90) with a paired-bootstrap interval over series",
        "The experiment report's verdict states whether series Brier <= market on that set, whether pooled team goalie-slot rank ρ >= 0.50 (today 0.276), and whether shutout Brier is strictly below the base rate — each met / not met with its number",
        "The stat-only path (no key) is reported alongside in the same report, so the tool's value without the key is visible",
        "Evidence tier: this story writes `report.md` and `manifest.json` under `ml/artifacts/experiments/US-505c/` (manifest has `git_sha` of the code commit, `git_dirty: false`, seed, config, per-season and pooled metrics); do NOT add it to MODEL_ARTIFACTS / BACKTEST_ARTIFACTS in tests/test_committed_model_evidence.py and do NOT touch `ml/artifacts/EVIDENCE_PASS.json`; the US-500 experiments guard test covers it",
        "Honesty (SPEC §7): the target above is attempted and then reported truthfully; a miss is written into the report with the real number and the interval, and the challenger is NOT adopted; never change baselines, splits, seeds, or held-out years to manufacture a pass",
        "CodeScene contract: every function you add or edit has cyclomatic complexity <= 8, <= 8 branches, <= 40 statements, <= 4 parameters (bundle more into a frozen dataclass request object, as cli/_request_commands.py does), nesting depth <= 3, and no condition with more than one `and`/`or` (extract a named predicate function); `uv run ruff check .` passes with the US-500 rule set",
        "CodeScene contract: new code goes into NEW modules of <= 400 lines (private helpers in `_name.py` files next to the public module); never grow an existing module past 600 lines and never make a hotspot file longer than you found it; no copy-pasted block of 5+ lines (extract a shared helper)",
        "CodeScene contract: tests use at most 3 consecutive `assert` statements per test function (use a comparison helper or `pytest.mark.parametrize`) and never repeat the same assertion block in two tests",
        "CodeScene contract: if CodeScene MCP tools are available, run `codescene-code_health_review` on every .py file you touched before committing and fix every reported issue; if they are not available, say so in progress.txt and rely on the ruff proxy rules",
        "All Typecheck passes (even if it's outside of your changes)",
        "Lint passes (even if it's outside of your changes)",
        "Unit tests have been added",
        "All Unit tests pass (even if it's outside of your changes)",
        "All Playwright tests pass (even if it's outside of your changes)"
      ],
      "priority": 15,
      "passes": false,
      "notes": ""
    },
    {
      "id": "US-505d",
      "title": "Adopt the team/goalie model into the projection artifact and run a full evidence pass (PRD US-505 part 4 of 4)",
      "description": "As the tool owner, I want `p_series_win`, `E[wins]`, `E[shutout wins]` and `e_goalie_points` produced by the adopted US-505 model, and ALL committed champion evidence regenerated together at a clean HEAD (the US-406 pattern in scripts/ralph/progress.txt archive). Files: models/series_sim.py, projection_artifact.py, _projection_slot_report.py, ml/artifacts/** (regenerated), EVIDENCE_PASS.json. This story runs for many hours (the 2026 r500 backtest alone took ~7h50m); start it only when nothing else is pending.",
      "acceptanceCriteria": [
        "If US-505c's report adopted the goals model (interval excluding zero on series Brier vs the committed champion) wire it as the default series-probability source; otherwise keep the champion wiring and record 'not adopted' in the projection manifest — either way the artifact fields are produced by exactly one documented source (`series_model` key in run_manifest.json)",
        "Commit the code first; then, from that clean commit (`git status` clean, `git_dirty=false`), regenerate: all model reports/manifests listed in MODEL_ARTIFACTS, both backtests in BACKTEST_ARTIFACTS (`scripts/run_2026_backtest.py` and the 2023–2025 run with the same seed 20260827 and the evidence profile), the four `2026-r1..r4` fixtures via `oracle project` with the exact flags recorded in their current run_manifest.json, and `ml/artifacts/EVIDENCE_PASS.json`; commit those artifacts as a second commit",
        "Every regenerated `report.md` starts with a champion/challenger block comparing to the previous committed numbers (from git, pre-regeneration) with intervals from US-501; `oracle evaluate` is run once with the previous committed backtest as champion and the new one as challenger and its report is committed under `ml/artifacts/experiments/US-505d/`",
        "All tests in tests/test_committed_model_evidence.py and tests/test_committed_projection_artifacts.py pass on the second commit; a `git clone --depth 1` of the branch into a temp directory passes the suite (missing-normalized-data skips allowed, 0 failures)",
        "progress.txt records the wall-clock time of each regeneration step so US-520a can be planned",
        "Honesty (SPEC §7): the target above is attempted and then reported truthfully; a miss is written into the report with the real number and the interval, and the challenger is NOT adopted; never change baselines, splits, seeds, or held-out years to manufacture a pass",
        "CodeScene contract: every function you add or edit has cyclomatic complexity <= 8, <= 8 branches, <= 40 statements, <= 4 parameters (bundle more into a frozen dataclass request object, as cli/_request_commands.py does), nesting depth <= 3, and no condition with more than one `and`/`or` (extract a named predicate function); `uv run ruff check .` passes with the US-500 rule set",
        "CodeScene contract: new code goes into NEW modules of <= 400 lines (private helpers in `_name.py` files next to the public module); never grow an existing module past 600 lines and never make a hotspot file longer than you found it; no copy-pasted block of 5+ lines (extract a shared helper)",
        "CodeScene contract: tests use at most 3 consecutive `assert` statements per test function (use a comparison helper or `pytest.mark.parametrize`) and never repeat the same assertion block in two tests",
        "CodeScene contract: if CodeScene MCP tools are available, run `codescene-code_health_review` on every .py file you touched before committing and fix every reported issue; if they are not available, say so in progress.txt and rely on the ruff proxy rules",
        "All Typecheck passes (even if it's outside of your changes)",
        "Lint passes (even if it's outside of your changes)",
        "Unit tests have been added",
        "All Unit tests pass (even if it's outside of your changes)",
        "All Playwright tests pass (even if it's outside of your changes)"
      ],
      "priority": 16,
      "passes": false,
      "notes": ""
    },
    {
      "id": "US-506a",
      "title": "Seeded scenario tensor in the projection artifact (PRD US-506 part 1 of 4)",
      "description": "As the tool owner, I want every live series simulated once per Monte-Carlo draw and every skater's games/points derived from that shared draw, stored compactly next to the marginal tables. New modules ml/src/draft_oracle/scenarios.py (+ _scenario_io.py); projection_artifact.py writes it; tests test_scenarios.py.",
      "acceptanceCriteria": [
        "`simulate_scenarios(inputs: ScenarioInputs, *, n_draws: int, seed: int) -> ScenarioTensor` produces, for N >= 2000 draws: per live series `length` (uint8), `winner` (bool), per-game winner and shutout flags (bool arrays), per team `games_played` (uint8), and per skater `games_played` (uint8) and `points` (uint8 or float16); the per-skater points use the same per-game Poisson/NB rate machinery as `models/projections.py` given the drawn games",
        "`ScenarioTensor.save(dir) / load(dir)` use `.npz` (compressed) plus a `scenarios.json` sidecar recording `n_draws`, `seed`, series ids and player id order; `oracle project` writes it into the artifact directory and records `n_draws` and `seed` in `run_manifest.json`; artifact size for the 2026-r1 fixture inputs stays under 50 MB (test measures the file)",
        "Regression: the marginal `expected_points` per skater from the tensor mean matches the existing marginal projection within 1e-6 relative on the synthetic projection fixtures (`tests/projection_artifact_fixtures.py`) when both use the same rate inputs and N = 200000 in a slow test, and within 0.02 at N = 2000 in the default test",
        "Determinism: same inputs + seed -> byte-identical `.npz` (test)",
        "The draft-time import guard still passes: `scenarios.py` imports only numpy/pandas",
        "CodeScene contract: every function you add or edit has cyclomatic complexity <= 8, <= 8 branches, <= 40 statements, <= 4 parameters (bundle more into a frozen dataclass request object, as cli/_request_commands.py does), nesting depth <= 3, and no condition with more than one `and`/`or` (extract a named predicate function); `uv run ruff check .` passes with the US-500 rule set",
        "CodeScene contract: new code goes into NEW modules of <= 400 lines (private helpers in `_name.py` files next to the public module); never grow an existing module past 600 lines and never make a hotspot file longer than you found it; no copy-pasted block of 5+ lines (extract a shared helper)",
        "CodeScene contract: tests use at most 3 consecutive `assert` statements per test function (use a comparison helper or `pytest.mark.parametrize`) and never repeat the same assertion block in two tests",
        "CodeScene contract: if CodeScene MCP tools are available, run `codescene-code_health_review` on every .py file you touched before committing and fix every reported issue; if they are not available, say so in progress.txt and rely on the ruff proxy rules",
        "All Typecheck passes (even if it's outside of your changes)",
        "Lint passes (even if it's outside of your changes)",
        "Unit tests have been added",
        "All Unit tests pass (even if it's outside of your changes)",
        "All Playwright tests pass (even if it's outside of your changes)"
      ],
      "priority": 17,
      "passes": false,
      "notes": ""
    },
    {
      "id": "US-506b",
      "title": "Roster valuation from the tensor with exact R3+R4 fold (PRD US-506 part 2 of 4)",
      "description": "As the tool owner, I want any roster valued as the empirical distribution of its total points over the shared draws, scored by the rules engine, replacing the `p_advance × e_goalie_r4` approximation. New module ml/src/draft_oracle/optimize/roster_value.py; tests test_roster_value.py.",
      "acceptanceCriteria": [
        "`roster_value(tensor: ScenarioTensor, roster: Roster) -> RosterValue` returns `mean`, `p10`, `p90`, `std`, and `p_beats(total: float) -> float`, computed by summing precomputed per-player cumulative points per draw (a `(n_players, n_draws)` matrix) over the roster's columns plus the goalie-slot points per draw derived from the team's per-game win/shutout flags through `rules.py` scoring (win=2, shutout win=4 replacing the 2)",
        "Combined R3+R4 event: when the tensor spans two rounds (advance-or-not per team in the same draw), the goalie slot's R4 points are counted only in draws where the team advances; a test shows the tensor-based expectation agrees with the existing `p_advance × e_goalie_r4` fold within 1e-3 on the synthetic combined fixture",
        "Single-player marginal check: `roster_value` of a one-skater roster has `mean` equal to the artifact's marginal `expected_points` within 1e-6 (test)",
        "Latency: valuing 1,000 random legal rosters on a 2,000-draw × 450-player tensor takes under 1 s (test with a generous 3 s ceiling)",
        "CodeScene contract: every function you add or edit has cyclomatic complexity <= 8, <= 8 branches, <= 40 statements, <= 4 parameters (bundle more into a frozen dataclass request object, as cli/_request_commands.py does), nesting depth <= 3, and no condition with more than one `and`/`or` (extract a named predicate function); `uv run ruff check .` passes with the US-500 rule set",
        "CodeScene contract: new code goes into NEW modules of <= 400 lines (private helpers in `_name.py` files next to the public module); never grow an existing module past 600 lines and never make a hotspot file longer than you found it; no copy-pasted block of 5+ lines (extract a shared helper)",
        "CodeScene contract: tests use at most 3 consecutive `assert` statements per test function (use a comparison helper or `pytest.mark.parametrize`) and never repeat the same assertion block in two tests",
        "CodeScene contract: if CodeScene MCP tools are available, run `codescene-code_health_review` on every .py file you touched before committing and fix every reported issue; if they are not available, say so in progress.txt and rely on the ruff proxy rules",
        "All Typecheck passes (even if it's outside of your changes)",
        "Lint passes (even if it's outside of your changes)",
        "Unit tests have been added",
        "All Unit tests pass (even if it's outside of your changes)",
        "All Playwright tests pass (even if it's outside of your changes)"
      ],
      "priority": 18,
      "passes": false,
      "notes": ""
    },
    {
      "id": "US-506c",
      "title": "Recommender and VOR consume roster-level values; `recommend` shows mean and p10/p90 deltas (PRD US-506 part 3 of 4)",
      "description": "As the tool owner, I want VOR, greedy-VOR and the rollout recommender to price candidates by the change in roster value on the shared draws. Files: optimize/vor.py, optimize/_recommend_core.py, _recommend_kernels.py, recommend.py, cli/_draft_runtime.py and the recommend rendering; tests test_vor.py, test_recommend.py, test_draft.py.",
      "acceptanceCriteria": [
        "`recommend_pick` accepts an optional `tensor: ScenarioTensor`; when present each candidate's value is `roster_value(current + candidate).mean - roster_value(current).mean` and the output `PickEvaluation` carries `delta_mean`, `delta_p10`, `delta_p90`; when absent behaviour is byte-identical to today (regression test on the existing fixtures)",
        "`oracle recommend` and the interactive `draft` loop show three new columns (Δmean, Δp10, Δp90) and load the tensor from the artifact directory if `scenarios.npz` exists; the cheat sheet is unchanged",
        "Latency test on the committed 2026-r1 fixture (with a tensor generated at N = 2000 in a tmp dir): `recommend` at full depth completes under 10 s and depth 1 under 5 s",
        "The draft-time import guard (tests/test_cli.py blocked roots) still passes",
        "CodeScene contract: every function you add or edit has cyclomatic complexity <= 8, <= 8 branches, <= 40 statements, <= 4 parameters (bundle more into a frozen dataclass request object, as cli/_request_commands.py does), nesting depth <= 3, and no condition with more than one `and`/`or` (extract a named predicate function); `uv run ruff check .` passes with the US-500 rule set",
        "CodeScene contract: new code goes into NEW modules of <= 400 lines (private helpers in `_name.py` files next to the public module); never grow an existing module past 600 lines and never make a hotspot file longer than you found it; no copy-pasted block of 5+ lines (extract a shared helper)",
        "CodeScene contract: tests use at most 3 consecutive `assert` statements per test function (use a comparison helper or `pytest.mark.parametrize`) and never repeat the same assertion block in two tests",
        "CodeScene contract: if CodeScene MCP tools are available, run `codescene-code_health_review` on every .py file you touched before committing and fix every reported issue; if they are not available, say so in progress.txt and rely on the ruff proxy rules",
        "All Typecheck passes (even if it's outside of your changes)",
        "Lint passes (even if it's outside of your changes)",
        "Unit tests have been added",
        "All Unit tests pass (even if it's outside of your changes)",
        "All Playwright tests pass (even if it's outside of your changes)"
      ],
      "priority": 19,
      "passes": false,
      "notes": ""
    },
    {
      "id": "US-506d",
      "title": "Backtest strategy `oracle_joint` evaluated against oracle, greedy_vor and one_step (PRD US-506 part 4 of 4)",
      "description": "As the tool owner, I want to know whether correlation-aware roster valuation actually wins more. Files: optimize/_recommend_strategies.py / backtest/_replay_playout.py (add strategy), backtest/replay.py `STRATEGIES`, an experiment report via `oracle evaluate`.",
      "acceptanceCriteria": [
        "Add `oracle_joint` to `Strategy`/`STRATEGIES`: identical to `oracle` except candidates are valued by roster-mean delta on a tensor generated per replayed round (N = 2000, seed from the run seed); the replay writes the tensor to the run's tmp dir, never to the artifact fixtures",
        "Run the evidence profile (≥ 8 drafts per round-slot, ≥ 200 rollouts) on 2023–2026 for `oracle_joint` vs `oracle`, `greedy_vor`, `one_step`; report mean points, decision regret, seat rank, season-win fraction and best-seat fraction with paired-bootstrap intervals",
        "The report's first paragraph names the winner or says 'tie' when the interval includes zero; `oracle_joint` becomes the default recommender strategy only if it beats `oracle` with the interval excluding zero (constant `DEFAULT_STRATEGY` in optimize/_recommend_strategies.py; test asserts the default equals the constant and the report states the decision)",
        "Evidence tier: this story writes `report.md` and `manifest.json` under `ml/artifacts/experiments/US-506d/` (manifest has `git_sha` of the code commit, `git_dirty: false`, seed, config, per-season and pooled metrics); do NOT add it to MODEL_ARTIFACTS / BACKTEST_ARTIFACTS in tests/test_committed_model_evidence.py and do NOT touch `ml/artifacts/EVIDENCE_PASS.json`; the US-500 experiments guard test covers it",
        "Honesty (SPEC §7): the target above is attempted and then reported truthfully; a miss is written into the report with the real number and the interval, and the challenger is NOT adopted; never change baselines, splits, seeds, or held-out years to manufacture a pass",
        "CodeScene contract: every function you add or edit has cyclomatic complexity <= 8, <= 8 branches, <= 40 statements, <= 4 parameters (bundle more into a frozen dataclass request object, as cli/_request_commands.py does), nesting depth <= 3, and no condition with more than one `and`/`or` (extract a named predicate function); `uv run ruff check .` passes with the US-500 rule set",
        "CodeScene contract: new code goes into NEW modules of <= 400 lines (private helpers in `_name.py` files next to the public module); never grow an existing module past 600 lines and never make a hotspot file longer than you found it; no copy-pasted block of 5+ lines (extract a shared helper)",
        "CodeScene contract: tests use at most 3 consecutive `assert` statements per test function (use a comparison helper or `pytest.mark.parametrize`) and never repeat the same assertion block in two tests",
        "CodeScene contract: if CodeScene MCP tools are available, run `codescene-code_health_review` on every .py file you touched before committing and fix every reported issue; if they are not available, say so in progress.txt and rely on the ruff proxy rules",
        "All Typecheck passes (even if it's outside of your changes)",
        "Lint passes (even if it's outside of your changes)",
        "Unit tests have been added",
        "All Unit tests pass (even if it's outside of your changes)",
        "All Playwright tests pass (even if it's outside of your changes)"
      ],
      "priority": 20,
      "passes": false,
      "notes": ""
    },
    {
      "id": "US-507a",
      "title": "Skater usage and shot-quality as-of features (PRD US-507 part 1 of 3)",
      "description": "As the tool owner, I want deployment and shot-quality features for each skater as of a round cutoff. New module ml/src/draft_oracle/features/skater_usage.py; tests test_skater_usage.py. Inputs: `skater_toi`, `pp_units`, `lines`, `skater_xg`, `skater_onice_xg` (US-504a–c).",
      "acceptanceCriteria": [
        "`build_usage_features(tables: UsageTables, cutoff: date) -> pd.DataFrame` returns per player_id: `ixg_per60`, `onice_xgf_share`, `pp1_share` (share of team PP TOI while on a rank-1 unit), `pp_toi_per60`, `ev_toi_per_game_last10`, `ev_toi_trend` (last 10 minus season), `line_assignment` (1–4 for forwards by 5v5 TOI rank among the team's F3 units, 1–3 for D pairs), `career_sh_pct_shrunk` (career sh% shrunk toward the league rate with n/(n+300) shots), using only games strictly before `cutoff`",
        "Every feature is covered by a hand-computed synthetic test (3 players, 4 games) and by an `assert_no_leakage` test that a game on or after the cutoff changes nothing",
        "`features/skater.py` `build_skater_features` merges these columns when the tables exist and fills documented neutral values (0 for shares, NaN-safe league mean for rates) when they do not; the existing feature tests pass unchanged",
        "README §Skater features lists the new columns with one-line definitions",
        "CodeScene contract: every function you add or edit has cyclomatic complexity <= 8, <= 8 branches, <= 40 statements, <= 4 parameters (bundle more into a frozen dataclass request object, as cli/_request_commands.py does), nesting depth <= 3, and no condition with more than one `and`/`or` (extract a named predicate function); `uv run ruff check .` passes with the US-500 rule set",
        "CodeScene contract: new code goes into NEW modules of <= 400 lines (private helpers in `_name.py` files next to the public module); never grow an existing module past 600 lines and never make a hotspot file longer than you found it; no copy-pasted block of 5+ lines (extract a shared helper)",
        "CodeScene contract: tests use at most 3 consecutive `assert` statements per test function (use a comparison helper or `pytest.mark.parametrize`) and never repeat the same assertion block in two tests",
        "CodeScene contract: if CodeScene MCP tools are available, run `codescene-code_health_review` on every .py file you touched before committing and fix every reported issue; if they are not available, say so in progress.txt and rely on the ruff proxy rules",
        "All Typecheck passes (even if it's outside of your changes)",
        "Lint passes (even if it's outside of your changes)",
        "Unit tests have been added",
        "All Unit tests pass (even if it's outside of your changes)",
        "All Playwright tests pass (even if it's outside of your changes)"
      ],
      "priority": 21,
      "passes": false,
      "notes": ""
    },
    {
      "id": "US-507b",
      "title": "Matchup features and the multi-season player prior (PRD US-507 part 2 of 3)",
      "description": "As the tool owner, I want each skater's projection to know the upcoming opponent and to start from the player's own history rather than a single-season shrink. Files: features/skater_usage.py (matchup), models/skater_production.py (`shrink_to_prior`, `fit_priors`), tests.",
      "acceptanceCriteria": [
        "`matchup_features(series: pd.DataFrame, team_xg, goalie_series, team_games, cutoff) -> pd.DataFrame` gives per (player team, series): `opp_xga_per60`, `opp_penalty_rate` (penalties taken per 60 from team_games), `opp_starter_gsax` (US-504c series), as-of the cutoff; tested with a synthetic two-team series",
        "`fit_priors` gains a multi-season mode: the player's prior mean is a weighted average of his previous seasons' rates (weights `w_k = decay**k`, k = seasons back) shrunk toward the position+team mean; the shrink strength `n0` and `decay` are selected on validation years by a small grid inside `train-skater-production` and written to the manifest; single-season `n/(n+10)` remains available behind `SkaterProductionConfig.prior_mode = \"single\"` for the champion comparison",
        "Held-out (2023..2026) MAE and rank ρ for `single` vs `multi` priors are written into the experiment report with paired-bootstrap intervals over skater-rounds",
        "Evidence tier: this story writes `report.md` and `manifest.json` under `ml/artifacts/experiments/US-507b/` (manifest has `git_sha` of the code commit, `git_dirty: false`, seed, config, per-season and pooled metrics); do NOT add it to MODEL_ARTIFACTS / BACKTEST_ARTIFACTS in tests/test_committed_model_evidence.py and do NOT touch `ml/artifacts/EVIDENCE_PASS.json`; the US-500 experiments guard test covers it",
        "Honesty (SPEC §7): the target above is attempted and then reported truthfully; a miss is written into the report with the real number and the interval, and the challenger is NOT adopted; never change baselines, splits, seeds, or held-out years to manufacture a pass",
        "CodeScene contract: every function you add or edit has cyclomatic complexity <= 8, <= 8 branches, <= 40 statements, <= 4 parameters (bundle more into a frozen dataclass request object, as cli/_request_commands.py does), nesting depth <= 3, and no condition with more than one `and`/`or` (extract a named predicate function); `uv run ruff check .` passes with the US-500 rule set",
        "CodeScene contract: new code goes into NEW modules of <= 400 lines (private helpers in `_name.py` files next to the public module); never grow an existing module past 600 lines and never make a hotspot file longer than you found it; no copy-pasted block of 5+ lines (extract a shared helper)",
        "CodeScene contract: tests use at most 3 consecutive `assert` statements per test function (use a comparison helper or `pytest.mark.parametrize`) and never repeat the same assertion block in two tests",
        "CodeScene contract: if CodeScene MCP tools are available, run `codescene-code_health_review` on every .py file you touched before committing and fix every reported issue; if they are not available, say so in progress.txt and rely on the ruff proxy rules",
        "All Typecheck passes (even if it's outside of your changes)",
        "Lint passes (even if it's outside of your changes)",
        "Unit tests have been added",
        "All Unit tests pass (even if it's outside of your changes)",
        "All Playwright tests pass (even if it's outside of your changes)"
      ],
      "priority": 22,
      "passes": false,
      "notes": ""
    },
    {
      "id": "US-507c",
      "title": "Model family search for skater production and the negative-binomial per-game distribution (PRD US-507 part 3 of 3)",
      "description": "As the tool owner, I want LightGBM, CatBoost, a Bayesian hierarchical rate model and a small MLP compared under one harness with the same features, and the per-game distribution's coverage verified. Add optional extras to ml/pyproject.toml: `catboost`, `numpyro` (or `pymc`) under `[project.optional-dependencies] research`; the draft-time CLI must import none of them (extend the blocked roots in tests/test_cli.py with `catboost`, `numpyro`, `pymc`, `jax`, `torch`). New module models/skater_families.py; tests test_skater_families.py.",
      "acceptanceCriteria": [
        "`FAMILIES = (\"lightgbm\", \"catboost\", \"bayes_nb\", \"mlp\")`; each family implements `fit(X, y, seed) / predict(X)` behind one protocol class; `bayes_nb` is a hierarchical negative-binomial rate model with player, team and opponent effects (numpyro NUTS or pymc, <= 15 minutes CPU on the full data, seed fixed); `mlp` is `sklearn.neural_network.MLPRegressor` with a fixed small architecture",
        "`train-skater-production --family all` runs every family under leave-future-out with identical features, selects the champion on validation-year MAE, reports test-year MAE and rank ρ for ALL families in one table with intervals, and writes the chosen family to the manifest",
        "Per-game distribution: implement `NegativeBinomialDraws` alongside the Poisson draws in models/projections.py; on held-out rounds compare p10/p90 empirical coverage for both; the report states which is adopted (`DISTRIBUTION` constant) and whether coverage is within ±5 points of nominal",
        "Held-out targets in the report: production MAE >= 10% better than the PPG baseline (today 8%) and round-point rank ρ >= 0.65 (today 0.593), each per season and pooled, met / not met",
        "A skipped-import test proves the research extras are optional: with `catboost` absent, `--family lightgbm` still runs and `--family all` prints which families were skipped",
        "Evidence tier: this story writes `report.md` and `manifest.json` under `ml/artifacts/experiments/US-507c/` (manifest has `git_sha` of the code commit, `git_dirty: false`, seed, config, per-season and pooled metrics); do NOT add it to MODEL_ARTIFACTS / BACKTEST_ARTIFACTS in tests/test_committed_model_evidence.py and do NOT touch `ml/artifacts/EVIDENCE_PASS.json`; the US-500 experiments guard test covers it",
        "Honesty (SPEC §7): the target above is attempted and then reported truthfully; a miss is written into the report with the real number and the interval, and the challenger is NOT adopted; never change baselines, splits, seeds, or held-out years to manufacture a pass",
        "CodeScene contract: every function you add or edit has cyclomatic complexity <= 8, <= 8 branches, <= 40 statements, <= 4 parameters (bundle more into a frozen dataclass request object, as cli/_request_commands.py does), nesting depth <= 3, and no condition with more than one `and`/`or` (extract a named predicate function); `uv run ruff check .` passes with the US-500 rule set",
        "CodeScene contract: new code goes into NEW modules of <= 400 lines (private helpers in `_name.py` files next to the public module); never grow an existing module past 600 lines and never make a hotspot file longer than you found it; no copy-pasted block of 5+ lines (extract a shared helper)",
        "CodeScene contract: tests use at most 3 consecutive `assert` statements per test function (use a comparison helper or `pytest.mark.parametrize`) and never repeat the same assertion block in two tests",
        "CodeScene contract: if CodeScene MCP tools are available, run `codescene-code_health_review` on every .py file you touched before committing and fix every reported issue; if they are not available, say so in progress.txt and rely on the ruff proxy rules",
        "All Typecheck passes (even if it's outside of your changes)",
        "Lint passes (even if it's outside of your changes)",
        "Unit tests have been added",
        "All Unit tests pass (even if it's outside of your changes)",
        "All Playwright tests pass (even if it's outside of your changes)"
      ],
      "priority": 23,
      "passes": false,
      "notes": ""
    },
    {
      "id": "US-508",
      "title": "Nested temporal cross-validation and bounded hyper-parameter search (PRD US-508)",
      "description": "As the tool owner, I want hyper-parameters chosen by rolling-origin validation rather than a single year. New package ml/src/draft_oracle/tuning/ (`__init__.py`, `rolling.py`, `search.py`); tests test_tuning.py.",
      "acceptanceCriteria": [
        "`rolling_origin_folds(seasons: Sequence[int]) -> list[Fold]` yields for each test season t in the last four: train <= t-2, validate t-1, test t; tested for 2023..2026",
        "`search(model_factory, space: Mapping[str, Sequence[object]], folds, *, budget: int, seed: int) -> SearchResult` evaluates at most `budget` configurations (random subset of the grid with the seed), picks the configuration with the best mean validation metric, and returns the full table; deterministic (test)",
        "A guard test proves the tuner cannot see test rows: pass a frame with a `season` column and assert that the frame passed to `model_factory().fit` never contains the fold's test season",
        "`train-goals` and `train-skater-production` gain `--tune` which uses the module and writes the chosen configuration plus the full search table to the manifest; the experiment report states whether the US-505/US-507 champion choices changed under the nested protocol",
        "Evidence tier: this story writes `report.md` and `manifest.json` under `ml/artifacts/experiments/US-508/` (manifest has `git_sha` of the code commit, `git_dirty: false`, seed, config, per-season and pooled metrics); do NOT add it to MODEL_ARTIFACTS / BACKTEST_ARTIFACTS in tests/test_committed_model_evidence.py and do NOT touch `ml/artifacts/EVIDENCE_PASS.json`; the US-500 experiments guard test covers it",
        "Honesty (SPEC §7): the target above is attempted and then reported truthfully; a miss is written into the report with the real number and the interval, and the challenger is NOT adopted; never change baselines, splits, seeds, or held-out years to manufacture a pass",
        "CodeScene contract: every function you add or edit has cyclomatic complexity <= 8, <= 8 branches, <= 40 statements, <= 4 parameters (bundle more into a frozen dataclass request object, as cli/_request_commands.py does), nesting depth <= 3, and no condition with more than one `and`/`or` (extract a named predicate function); `uv run ruff check .` passes with the US-500 rule set",
        "CodeScene contract: new code goes into NEW modules of <= 400 lines (private helpers in `_name.py` files next to the public module); never grow an existing module past 600 lines and never make a hotspot file longer than you found it; no copy-pasted block of 5+ lines (extract a shared helper)",
        "CodeScene contract: tests use at most 3 consecutive `assert` statements per test function (use a comparison helper or `pytest.mark.parametrize`) and never repeat the same assertion block in two tests",
        "CodeScene contract: if CodeScene MCP tools are available, run `codescene-code_health_review` on every .py file you touched before committing and fix every reported issue; if they are not available, say so in progress.txt and rely on the ruff proxy rules",
        "All Typecheck passes (even if it's outside of your changes)",
        "Lint passes (even if it's outside of your changes)",
        "Unit tests have been added",
        "All Unit tests pass (even if it's outside of your changes)",
        "All Playwright tests pass (even if it's outside of your changes)"
      ],
      "priority": 24,
      "passes": false,
      "notes": ""
    },
    {
      "id": "US-509",
      "title": "Conformal prediction intervals for skater round points (PRD US-509)",
      "description": "As the tool owner, I want distribution-free p10/p90 bands with verified coverage. New module ml/src/draft_oracle/models/conformal.py; tests test_conformal.py. Add `mapie` to the `research` extras only if used; a pure-numpy split-conformal implementation is preferred.",
      "acceptanceCriteria": [
        "`fit_split_conformal(residuals: np.ndarray, alpha: float) -> float` returns the (1-alpha) empirical quantile of absolute residuals with the finite-sample correction; `conformal_interval(point: np.ndarray, q: float)` returns lower/upper; tested against a hand-computed example",
        "Calibration residuals come ONLY from validation years inside the leave-future-out loop (test with a sentinel test season as in US-505b)",
        "Held-out p10/p90 coverage per season and pooled is written to the experiment report; target within ±3 points of nominal; met / not met",
        "The cheat sheet and `recommend` show the conformal band in place of the Monte-Carlo p10/p90 when the artifact carries `conformal_q` in its manifest (tests on the projection artifact fixtures)",
        "Evidence tier: this story writes `report.md` and `manifest.json` under `ml/artifacts/experiments/US-509/` (manifest has `git_sha` of the code commit, `git_dirty: false`, seed, config, per-season and pooled metrics); do NOT add it to MODEL_ARTIFACTS / BACKTEST_ARTIFACTS in tests/test_committed_model_evidence.py and do NOT touch `ml/artifacts/EVIDENCE_PASS.json`; the US-500 experiments guard test covers it",
        "Honesty (SPEC §7): the target above is attempted and then reported truthfully; a miss is written into the report with the real number and the interval, and the challenger is NOT adopted; never change baselines, splits, seeds, or held-out years to manufacture a pass",
        "CodeScene contract: every function you add or edit has cyclomatic complexity <= 8, <= 8 branches, <= 40 statements, <= 4 parameters (bundle more into a frozen dataclass request object, as cli/_request_commands.py does), nesting depth <= 3, and no condition with more than one `and`/`or` (extract a named predicate function); `uv run ruff check .` passes with the US-500 rule set",
        "CodeScene contract: new code goes into NEW modules of <= 400 lines (private helpers in `_name.py` files next to the public module); never grow an existing module past 600 lines and never make a hotspot file longer than you found it; no copy-pasted block of 5+ lines (extract a shared helper)",
        "CodeScene contract: tests use at most 3 consecutive `assert` statements per test function (use a comparison helper or `pytest.mark.parametrize`) and never repeat the same assertion block in two tests",
        "CodeScene contract: if CodeScene MCP tools are available, run `codescene-code_health_review` on every .py file you touched before committing and fix every reported issue; if they are not available, say so in progress.txt and rely on the ruff proxy rules",
        "All Typecheck passes (even if it's outside of your changes)",
        "Lint passes (even if it's outside of your changes)",
        "Unit tests have been added",
        "All Unit tests pass (even if it's outside of your changes)",
        "All Playwright tests pass (even if it's outside of your changes)"
      ],
      "priority": 25,
      "passes": false,
      "notes": ""
    },
    {
      "id": "US-510",
      "title": "Per-skater per-game dress probability from absence spells and dressed lineups (PRD US-510)",
      "description": "As the tool owner, I want the probability that a skater dresses for each game of a series instead of assuming 1.0. New module ml/src/draft_oracle/models/availability.py; tests test_availability.py. Inputs: `dressed.parquet` (US-504b), `skater_games`, the absence-spell logic in models/returns.py.",
      "acceptanceCriteria": [
        "`dress_probability(history: pd.DataFrame, cutoff: date, horizon_games: int = 7) -> pd.DataFrame` returns per player_id P(dressed for game k), k = 1..7, from a logistic model on: healthy-scratch rate in the last 20 team games, games since last absence, position, and whether currently absent (spell length so far); fit on seasons before the cutoff only (leakage test)",
        "Held-out calibration (10-bin reliability table, pooled 2023..2026) is written to the experiment report",
        "`simulate_scenarios` (US-506a) accepts an optional dress-probability frame and thins each skater's games per draw accordingly; the marginal expected games decreases by exactly the mean (1 - p) in a synthetic test",
        "Evidence tier: this story writes `report.md` and `manifest.json` under `ml/artifacts/experiments/US-510/` (manifest has `git_sha` of the code commit, `git_dirty: false`, seed, config, per-season and pooled metrics); do NOT add it to MODEL_ARTIFACTS / BACKTEST_ARTIFACTS in tests/test_committed_model_evidence.py and do NOT touch `ml/artifacts/EVIDENCE_PASS.json`; the US-500 experiments guard test covers it",
        "Honesty (SPEC §7): the target above is attempted and then reported truthfully; a miss is written into the report with the real number and the interval, and the challenger is NOT adopted; never change baselines, splits, seeds, or held-out years to manufacture a pass",
        "CodeScene contract: every function you add or edit has cyclomatic complexity <= 8, <= 8 branches, <= 40 statements, <= 4 parameters (bundle more into a frozen dataclass request object, as cli/_request_commands.py does), nesting depth <= 3, and no condition with more than one `and`/`or` (extract a named predicate function); `uv run ruff check .` passes with the US-500 rule set",
        "CodeScene contract: new code goes into NEW modules of <= 400 lines (private helpers in `_name.py` files next to the public module); never grow an existing module past 600 lines and never make a hotspot file longer than you found it; no copy-pasted block of 5+ lines (extract a shared helper)",
        "CodeScene contract: tests use at most 3 consecutive `assert` statements per test function (use a comparison helper or `pytest.mark.parametrize`) and never repeat the same assertion block in two tests",
        "CodeScene contract: if CodeScene MCP tools are available, run `codescene-code_health_review` on every .py file you touched before committing and fix every reported issue; if they are not available, say so in progress.txt and rely on the ruff proxy rules",
        "All Typecheck passes (even if it's outside of your changes)",
        "Lint passes (even if it's outside of your changes)",
        "Unit tests have been added",
        "All Unit tests pass (even if it's outside of your changes)",
        "All Playwright tests pass (even if it's outside of your changes)"
      ],
      "priority": 26,
      "passes": false,
      "notes": ""
    },
    {
      "id": "US-511",
      "title": "Rest, back-to-back, travel and series-start rest features with ablation (PRD US-511)",
      "description": "As the tool owner, I want schedule effects available to the goals model and kept only if they help. New module ml/src/draft_oracle/features/schedule.py; tests test_schedule.py. Inputs: `game-times-*.csv.gz` (startTimeUTC, venueCity) via the normalized `games`/`team_games` tables.",
      "acceptanceCriteria": [
        "`schedule_features(team_games: pd.DataFrame, game_times: pd.DataFrame) -> pd.DataFrame` gives per (game_id, team_id): `rest_days`, `back_to_back: bool`, `tz_shift_hours` (venue time zone minus the team's home time zone, from a small explicit city -> IANA zone dict in the module; unknown city raises), `series_start_rest_diff` (rest days minus opponent's rest days before game 1); tested on a synthetic 3-game schedule",
        "`GoalsConfig.use_schedule: bool`; `train-goals` is run with and without under leave-future-out; the experiment report shows held-out Brier for both with an interval and the constant default is set to the winner (kept only if Brier improves with the interval excluding zero; otherwise off and the report says so)",
        "Evidence tier: this story writes `report.md` and `manifest.json` under `ml/artifacts/experiments/US-511/` (manifest has `git_sha` of the code commit, `git_dirty: false`, seed, config, per-season and pooled metrics); do NOT add it to MODEL_ARTIFACTS / BACKTEST_ARTIFACTS in tests/test_committed_model_evidence.py and do NOT touch `ml/artifacts/EVIDENCE_PASS.json`; the US-500 experiments guard test covers it",
        "Honesty (SPEC §7): the target above is attempted and then reported truthfully; a miss is written into the report with the real number and the interval, and the challenger is NOT adopted; never change baselines, splits, seeds, or held-out years to manufacture a pass",
        "CodeScene contract: every function you add or edit has cyclomatic complexity <= 8, <= 8 branches, <= 40 statements, <= 4 parameters (bundle more into a frozen dataclass request object, as cli/_request_commands.py does), nesting depth <= 3, and no condition with more than one `and`/`or` (extract a named predicate function); `uv run ruff check .` passes with the US-500 rule set",
        "CodeScene contract: new code goes into NEW modules of <= 400 lines (private helpers in `_name.py` files next to the public module); never grow an existing module past 600 lines and never make a hotspot file longer than you found it; no copy-pasted block of 5+ lines (extract a shared helper)",
        "CodeScene contract: tests use at most 3 consecutive `assert` statements per test function (use a comparison helper or `pytest.mark.parametrize`) and never repeat the same assertion block in two tests",
        "CodeScene contract: if CodeScene MCP tools are available, run `codescene-code_health_review` on every .py file you touched before committing and fix every reported issue; if they are not available, say so in progress.txt and rely on the ruff proxy rules",
        "All Typecheck passes (even if it's outside of your changes)",
        "Lint passes (even if it's outside of your changes)",
        "Unit tests have been added",
        "All Unit tests pass (even if it's outside of your changes)",
        "All Playwright tests pass (even if it's outside of your changes)"
      ],
      "priority": 27,
      "passes": false,
      "notes": ""
    },
    {
      "id": "US-512",
      "title": "In-series dynamics: venue-aware game probabilities and goalie-change hazard (PRD US-512)",
      "description": "As the tool owner, I want the series simulator to use the real home/away sequence from the bracket and to model a starter being replaced inside a series. Files: models/series_sim.py (`simulate_series`, `simulate_series_monte_carlo`), new models/_series_dynamics.py, tests test_series_sim.py.",
      "acceptanceCriteria": [
        "`simulate_series` accepts a `venue_sequence: Sequence[bool]` (home flags for games 1..7, default the NHL 2-2-1-1-1 pattern) and per-game rest days, and uses per-game probabilities from the goals model for each venue instead of one fixed probability; a test shows a home-advantage model produces a higher series probability for the team with games 1, 2, 5, 7 at home",
        "`goalie_change_hazard(goalie_games: pd.DataFrame) -> HazardTable` estimates from the archive the probability the starter is replaced for the next game given (goals against in the last game, consecutive losses); tested on synthetic data with a known rate; the simulator applies it by switching to the backup's GSAx (US-504c) for subsequent games in that draw",
        "Held-out (2023..2026) series length distribution and winner Brier for the dynamic simulator vs the static one are written to the experiment report with intervals; the dynamic version becomes default only if winner Brier does not worsen and the 7-game share error improves",
        "Evidence tier: this story writes `report.md` and `manifest.json` under `ml/artifacts/experiments/US-512/` (manifest has `git_sha` of the code commit, `git_dirty: false`, seed, config, per-season and pooled metrics); do NOT add it to MODEL_ARTIFACTS / BACKTEST_ARTIFACTS in tests/test_committed_model_evidence.py and do NOT touch `ml/artifacts/EVIDENCE_PASS.json`; the US-500 experiments guard test covers it",
        "Honesty (SPEC §7): the target above is attempted and then reported truthfully; a miss is written into the report with the real number and the interval, and the challenger is NOT adopted; never change baselines, splits, seeds, or held-out years to manufacture a pass",
        "CodeScene contract: every function you add or edit has cyclomatic complexity <= 8, <= 8 branches, <= 40 statements, <= 4 parameters (bundle more into a frozen dataclass request object, as cli/_request_commands.py does), nesting depth <= 3, and no condition with more than one `and`/`or` (extract a named predicate function); `uv run ruff check .` passes with the US-500 rule set",
        "CodeScene contract: new code goes into NEW modules of <= 400 lines (private helpers in `_name.py` files next to the public module); never grow an existing module past 600 lines and never make a hotspot file longer than you found it; no copy-pasted block of 5+ lines (extract a shared helper)",
        "CodeScene contract: tests use at most 3 consecutive `assert` statements per test function (use a comparison helper or `pytest.mark.parametrize`) and never repeat the same assertion block in two tests",
        "CodeScene contract: if CodeScene MCP tools are available, run `codescene-code_health_review` on every .py file you touched before committing and fix every reported issue; if they are not available, say so in progress.txt and rely on the ruff proxy rules",
        "All Typecheck passes (even if it's outside of your changes)",
        "Lint passes (even if it's outside of your changes)",
        "Unit tests have been added",
        "All Unit tests pass (even if it's outside of your changes)",
        "All Playwright tests pass (even if it's outside of your changes)"
      ],
      "priority": 28,
      "passes": false,
      "notes": ""
    },
    {
      "id": "US-513a",
      "title": "Labeled injury validation slice and return-time refit (PRD US-513 part 1 of 3)",
      "description": "As the tool owner, I want the return-time model validated on real labeled injury statuses. Inputs: the committed ESPN `injuries` blocks under `ml/data/raw/odds-archive/espn-2025-26-completion/raw/summary/` (Dec 2025–Jun 2026). Files: ingest/injuries.py (+ new ingest/_injuries_espn_labels.py), models/returns.py, tests test_injuries.py, test_returns.py.",
      "acceptanceCriteria": [
        "`load_espn_injury_labels(raw_dir: Path) -> pd.DataFrame` produces per (player_id, as_of_date): `status` (Out/IR/Day-To-Day/Questionable/...), `observed_games_missed_after` (from `skater_games`), with `as_of_date` = the game date of the summary the block came from; player names resolve through `entity_match` and unresolved names are counted and listed, never guessed (tests on a synthetic summary JSON fixture)",
        "`models/returns.py` gains `evaluate_on_labels(labels) -> ReturnsEvaluation` reporting predicted vs observed P(available for game k), k = 1..7, per status; and `refit_status_map(labels)` producing the new status -> mean-absence map; the experiment report shows the map before and after and the per-status calibration",
        "The refit map becomes the default only if pooled calibration error improves; the constant lives in models/_returns_absence.py and the report states the decision",
        "Evidence tier: this story writes `report.md` and `manifest.json` under `ml/artifacts/experiments/US-513a/` (manifest has `git_sha` of the code commit, `git_dirty: false`, seed, config, per-season and pooled metrics); do NOT add it to MODEL_ARTIFACTS / BACKTEST_ARTIFACTS in tests/test_committed_model_evidence.py and do NOT touch `ml/artifacts/EVIDENCE_PASS.json`; the US-500 experiments guard test covers it",
        "Honesty (SPEC §7): the target above is attempted and then reported truthfully; a miss is written into the report with the real number and the interval, and the challenger is NOT adopted; never change baselines, splits, seeds, or held-out years to manufacture a pass",
        "CodeScene contract: every function you add or edit has cyclomatic complexity <= 8, <= 8 branches, <= 40 statements, <= 4 parameters (bundle more into a frozen dataclass request object, as cli/_request_commands.py does), nesting depth <= 3, and no condition with more than one `and`/`or` (extract a named predicate function); `uv run ruff check .` passes with the US-500 rule set",
        "CodeScene contract: new code goes into NEW modules of <= 400 lines (private helpers in `_name.py` files next to the public module); never grow an existing module past 600 lines and never make a hotspot file longer than you found it; no copy-pasted block of 5+ lines (extract a shared helper)",
        "CodeScene contract: tests use at most 3 consecutive `assert` statements per test function (use a comparison helper or `pytest.mark.parametrize`) and never repeat the same assertion block in two tests",
        "CodeScene contract: if CodeScene MCP tools are available, run `codescene-code_health_review` on every .py file you touched before committing and fix every reported issue; if they are not available, say so in progress.txt and rely on the ruff proxy rules",
        "All Typecheck passes (even if it's outside of your changes)",
        "Lint passes (even if it's outside of your changes)",
        "Unit tests have been added",
        "All Unit tests pass (even if it's outside of your changes)",
        "All Playwright tests pass (even if it's outside of your changes)"
      ],
      "priority": 29,
      "passes": false,
      "notes": ""
    },
    {
      "id": "US-513b",
      "title": "Starter probability model, NHL pre-game lineup pin, and `starting_goalie:` override (PRD US-513 part 2 of 3)",
      "description": "As the tool owner, I want P(starter) per team for game 1 with three authorities in order: override YAML, posted NHL lineup, model. New module ml/src/draft_oracle/models/starters.py; ingest/nhl_api.py gains one read; data/overrides/injuries.yaml schema gains `starting_goalie:`; tests test_starters.py, test_injuries.py. No third-party lineup site is contacted (SPEC §5, PRD Decisions §2).",
      "acceptanceCriteria": [
        "`starter_probability(goalie_games, cutoff, team_id) -> dict[int, float]` uses share of starts in the last 20 team games, playoff-start history, and back-to-back pattern, returning P(starter = goalie_id) summing to 1; calibration on the archive's observed starters (2019..2026) is written to the experiment report (10-bin table)",
        "`blended_starter_gsax(probabilities, goalie_quality) -> float` feeds the goals model (US-505) when no pin exists",
        "`ingest/nhl_api.py` gains `fetch_pregame_starters(game_id) -> dict[int, int] | None` reading the public `gamecenter/<id>/landing` lineup when posted (httpx, tested with a recorded synthetic fixture; network forbidden in tests); `oracle project` uses it only when `--live-lineups` is passed and records `starter_source` per team in the manifest (`override` > `nhl_lineup` > `model`)",
        "`injuries.yaml` accepts `starting_goalie: {team: ABBR, goalie_id: int}` entries validated by pydantic; the cheat sheet shows 'starter unconfirmed (p=0.71)' when the source is `model` and the goalie name when pinned (tests on fixtures)",
        "Evidence tier: this story writes `report.md` and `manifest.json` under `ml/artifacts/experiments/US-513b/` (manifest has `git_sha` of the code commit, `git_dirty: false`, seed, config, per-season and pooled metrics); do NOT add it to MODEL_ARTIFACTS / BACKTEST_ARTIFACTS in tests/test_committed_model_evidence.py and do NOT touch `ml/artifacts/EVIDENCE_PASS.json`; the US-500 experiments guard test covers it",
        "Honesty (SPEC §7): the target above is attempted and then reported truthfully; a miss is written into the report with the real number and the interval, and the challenger is NOT adopted; never change baselines, splits, seeds, or held-out years to manufacture a pass",
        "CodeScene contract: every function you add or edit has cyclomatic complexity <= 8, <= 8 branches, <= 40 statements, <= 4 parameters (bundle more into a frozen dataclass request object, as cli/_request_commands.py does), nesting depth <= 3, and no condition with more than one `and`/`or` (extract a named predicate function); `uv run ruff check .` passes with the US-500 rule set",
        "CodeScene contract: new code goes into NEW modules of <= 400 lines (private helpers in `_name.py` files next to the public module); never grow an existing module past 600 lines and never make a hotspot file longer than you found it; no copy-pasted block of 5+ lines (extract a shared helper)",
        "CodeScene contract: tests use at most 3 consecutive `assert` statements per test function (use a comparison helper or `pytest.mark.parametrize`) and never repeat the same assertion block in two tests",
        "CodeScene contract: if CodeScene MCP tools are available, run `codescene-code_health_review` on every .py file you touched before committing and fix every reported issue; if they are not available, say so in progress.txt and rely on the ruff proxy rules",
        "All Typecheck passes (even if it's outside of your changes)",
        "Lint passes (even if it's outside of your changes)",
        "Unit tests have been added",
        "All Unit tests pass (even if it's outside of your changes)",
        "All Playwright tests pass (even if it's outside of your changes)"
      ],
      "priority": 30,
      "passes": false,
      "notes": ""
    },
    {
      "id": "US-513c",
      "title": "As-of injury replay in the 2026 backtest and the `oracle project` injury-impact summary (PRD US-513 part 3 of 3)",
      "description": "As the tool owner, I want the injury haircut and IR valuation exercised in evidence for the first time, and a readable summary at build time. Files: backtest/replay.py (+ _replay_leakage.py), cli/_project_artifact_commands.py, _projection_ir.py, tests test_backtest.py, test_cli.py.",
      "acceptanceCriteria": [
        "`BacktestConfig.injuries_as_of: bool`; when true, the 2026 replay reconstructs each round's injury statuses from the US-513a labels dated strictly before the round cutoff (leakage test: a label dated on the cutoff is excluded) and applies the haircut/IR valuation as `oracle project` would",
        "The experiment report replays 2026 with and without as-of injuries (evidence profile) and states the marginal effect on oracle points and decision regret with intervals",
        "`oracle project` prints an injury-impact summary: players discounted, expected points removed, unresolved ids, and starter status per team; the cheat sheet Status column shows 'back G3' style return-game expectations (tests on fixtures)",
        "Evidence tier: this story writes `report.md` and `manifest.json` under `ml/artifacts/experiments/US-513c/` (manifest has `git_sha` of the code commit, `git_dirty: false`, seed, config, per-season and pooled metrics); do NOT add it to MODEL_ARTIFACTS / BACKTEST_ARTIFACTS in tests/test_committed_model_evidence.py and do NOT touch `ml/artifacts/EVIDENCE_PASS.json`; the US-500 experiments guard test covers it",
        "Honesty (SPEC §7): the target above is attempted and then reported truthfully; a miss is written into the report with the real number and the interval, and the challenger is NOT adopted; never change baselines, splits, seeds, or held-out years to manufacture a pass",
        "CodeScene contract: every function you add or edit has cyclomatic complexity <= 8, <= 8 branches, <= 40 statements, <= 4 parameters (bundle more into a frozen dataclass request object, as cli/_request_commands.py does), nesting depth <= 3, and no condition with more than one `and`/`or` (extract a named predicate function); `uv run ruff check .` passes with the US-500 rule set",
        "CodeScene contract: new code goes into NEW modules of <= 400 lines (private helpers in `_name.py` files next to the public module); never grow an existing module past 600 lines and never make a hotspot file longer than you found it; no copy-pasted block of 5+ lines (extract a shared helper)",
        "CodeScene contract: tests use at most 3 consecutive `assert` statements per test function (use a comparison helper or `pytest.mark.parametrize`) and never repeat the same assertion block in two tests",
        "CodeScene contract: if CodeScene MCP tools are available, run `codescene-code_health_review` on every .py file you touched before committing and fix every reported issue; if they are not available, say so in progress.txt and rely on the ruff proxy rules",
        "All Typecheck passes (even if it's outside of your changes)",
        "Lint passes (even if it's outside of your changes)",
        "Unit tests have been added",
        "All Unit tests pass (even if it's outside of your changes)",
        "All Playwright tests pass (even if it's outside of your changes)"
      ],
      "priority": 31,
      "passes": false,
      "notes": ""
    },
    {
      "id": "US-514a",
      "title": "Standings input and the `mean` / `p_win_season` / `mean_minus_lambda_var` objectives (PRD US-514 part 1 of 2)",
      "description": "As the tool owner, I want the recommender to accept the season standings and an objective. New module ml/src/draft_oracle/optimize/objectives.py; cli/_draft_parsing.py (`--standings`), RecommendCommandRequest and DraftCommandRequest (`--objective`), tests test_objectives.py, test_draft.py.",
      "acceptanceCriteria": [
        "`parse_standings(text: str) -> dict[str, float]` parses `ben=120,judah=104,...` (case-insensitive ids validated against `parse_managers`; duplicates rejected) and the draft session log can supply them (`DraftState.standings`)",
        "`Objective = Literal[\"mean\", \"p_win_season\", \"mean_minus_lambda_var\"]`; `objective_value(objective, roster_value: RosterValue, standings, remaining_rounds, lambda_: float = 0.5) -> float` where `p_win_season` is the share of tensor draws in which the owner's cumulative total finishes first over the remaining rounds (opponent remaining-round points drawn from their projected roster distributions when known, else the league's per-round mean and sd); tested on a synthetic 4-seat standing where the trailing seat prefers the higher-variance roster",
        "Default rule implemented in `default_objective(round_number: int, adoption: AdoptionRecord) -> Objective`: round 1 -> `mean`; rounds 2–4 -> the variance-aware objective only if `ml/artifacts/experiments/US-514b/manifest.json` records `adopted: true` (missing file -> `mean`); tested for both states",
        "`recommend` names the objective in use in its header and shows for each candidate Δmean, Δp10, Δp90 and ΔP(win season)",
        "CodeScene contract: every function you add or edit has cyclomatic complexity <= 8, <= 8 branches, <= 40 statements, <= 4 parameters (bundle more into a frozen dataclass request object, as cli/_request_commands.py does), nesting depth <= 3, and no condition with more than one `and`/`or` (extract a named predicate function); `uv run ruff check .` passes with the US-500 rule set",
        "CodeScene contract: new code goes into NEW modules of <= 400 lines (private helpers in `_name.py` files next to the public module); never grow an existing module past 600 lines and never make a hotspot file longer than you found it; no copy-pasted block of 5+ lines (extract a shared helper)",
        "CodeScene contract: tests use at most 3 consecutive `assert` statements per test function (use a comparison helper or `pytest.mark.parametrize`) and never repeat the same assertion block in two tests",
        "CodeScene contract: if CodeScene MCP tools are available, run `codescene-code_health_review` on every .py file you touched before committing and fix every reported issue; if they are not available, say so in progress.txt and rely on the ruff proxy rules",
        "All Typecheck passes (even if it's outside of your changes)",
        "Lint passes (even if it's outside of your changes)",
        "Unit tests have been added",
        "All Unit tests pass (even if it's outside of your changes)",
        "All Playwright tests pass (even if it's outside of your changes)"
      ],
      "priority": 32,
      "passes": false,
      "notes": ""
    },
    {
      "id": "US-514b",
      "title": "Backtest strategies `oracle_pwin` and `oracle_meanvar` with real standings and the adoption decision (PRD US-514 part 2 of 2)",
      "description": "As the tool owner, I want to know whether taking variance by standing wins more seasons. Files: backtest strategies, backtest/_replay_league.py (standings entering each round from replayed actuals), experiment report.",
      "acceptanceCriteria": [
        "Add `oracle_pwin` and `oracle_meanvar` to `STRATEGIES`; the replay computes each seat's real cumulative standings entering rounds 2–4 from the replayed rounds and passes them to the objective",
        "Evidence-profile run on 2023–2026: season-win fraction, best-seat fraction, mean points and decision regret vs `oracle` and `greedy_vor` with paired-bootstrap intervals",
        "`manifest.json` under the experiments tier records `adopted: true|false` and `objective`; adopted only if the season-win interval excludes zero; the report says so plainly either way and US-514a's default rule reads that file",
        "Evidence tier: this story writes `report.md` and `manifest.json` under `ml/artifacts/experiments/US-514b/` (manifest has `git_sha` of the code commit, `git_dirty: false`, seed, config, per-season and pooled metrics); do NOT add it to MODEL_ARTIFACTS / BACKTEST_ARTIFACTS in tests/test_committed_model_evidence.py and do NOT touch `ml/artifacts/EVIDENCE_PASS.json`; the US-500 experiments guard test covers it",
        "Honesty (SPEC §7): the target above is attempted and then reported truthfully; a miss is written into the report with the real number and the interval, and the challenger is NOT adopted; never change baselines, splits, seeds, or held-out years to manufacture a pass",
        "CodeScene contract: every function you add or edit has cyclomatic complexity <= 8, <= 8 branches, <= 40 statements, <= 4 parameters (bundle more into a frozen dataclass request object, as cli/_request_commands.py does), nesting depth <= 3, and no condition with more than one `and`/`or` (extract a named predicate function); `uv run ruff check .` passes with the US-500 rule set",
        "CodeScene contract: new code goes into NEW modules of <= 400 lines (private helpers in `_name.py` files next to the public module); never grow an existing module past 600 lines and never make a hotspot file longer than you found it; no copy-pasted block of 5+ lines (extract a shared helper)",
        "CodeScene contract: tests use at most 3 consecutive `assert` statements per test function (use a comparison helper or `pytest.mark.parametrize`) and never repeat the same assertion block in two tests",
        "CodeScene contract: if CodeScene MCP tools are available, run `codescene-code_health_review` on every .py file you touched before committing and fix every reported issue; if they are not available, say so in progress.txt and rely on the ruff proxy rules",
        "All Typecheck passes (even if it's outside of your changes)",
        "Lint passes (even if it's outside of your changes)",
        "Unit tests have been added",
        "All Unit tests pass (even if it's outside of your changes)",
        "All Playwright tests pass (even if it's outside of your changes)"
      ],
      "priority": 33,
      "passes": false,
      "notes": ""
    },
    {
      "id": "US-515",
      "title": "Stacking across model families with validation-fitted weights (PRD US-515)",
      "description": "As the tool owner, I want the best families combined with weights fit on validation years. New module ml/src/draft_oracle/models/stacking.py; tests test_stacking.py.",
      "acceptanceCriteria": [
        "`fit_stack(predictions: Mapping[str, np.ndarray], target: np.ndarray, *, nonneg: bool = True) -> StackWeights` solves constrained least squares (weights >= 0, sum to 1) on validation rows only (sentinel test season guard as in US-505b); `apply_stack`",
        "Stacks for the game-win probability (stat-only, market-aware, goals model) and for skater production (LightGBM, CatBoost, bayes_nb, mlp where available) are evaluated under leave-future-out; component metrics and weights are written to the manifest and the experiment report",
        "Adopted (constant `USE_STACK` per model) only if the held-out interval vs the single champion excludes zero; the report states the decision",
        "Evidence tier: this story writes `report.md` and `manifest.json` under `ml/artifacts/experiments/US-515/` (manifest has `git_sha` of the code commit, `git_dirty: false`, seed, config, per-season and pooled metrics); do NOT add it to MODEL_ARTIFACTS / BACKTEST_ARTIFACTS in tests/test_committed_model_evidence.py and do NOT touch `ml/artifacts/EVIDENCE_PASS.json`; the US-500 experiments guard test covers it",
        "Honesty (SPEC §7): the target above is attempted and then reported truthfully; a miss is written into the report with the real number and the interval, and the challenger is NOT adopted; never change baselines, splits, seeds, or held-out years to manufacture a pass",
        "CodeScene contract: every function you add or edit has cyclomatic complexity <= 8, <= 8 branches, <= 40 statements, <= 4 parameters (bundle more into a frozen dataclass request object, as cli/_request_commands.py does), nesting depth <= 3, and no condition with more than one `and`/`or` (extract a named predicate function); `uv run ruff check .` passes with the US-500 rule set",
        "CodeScene contract: new code goes into NEW modules of <= 400 lines (private helpers in `_name.py` files next to the public module); never grow an existing module past 600 lines and never make a hotspot file longer than you found it; no copy-pasted block of 5+ lines (extract a shared helper)",
        "CodeScene contract: tests use at most 3 consecutive `assert` statements per test function (use a comparison helper or `pytest.mark.parametrize`) and never repeat the same assertion block in two tests",
        "CodeScene contract: if CodeScene MCP tools are available, run `codescene-code_health_review` on every .py file you touched before committing and fix every reported issue; if they are not available, say so in progress.txt and rely on the ruff proxy rules",
        "All Typecheck passes (even if it's outside of your changes)",
        "Lint passes (even if it's outside of your changes)",
        "Unit tests have been added",
        "All Unit tests pass (even if it's outside of your changes)",
        "All Playwright tests pass (even if it's outside of your changes)"
      ],
      "priority": 34,
      "passes": false,
      "notes": ""
    },
    {
      "id": "US-516",
      "title": "Opponent model refresh for survival probabilities (PRD US-516)",
      "description": "As the tool owner, I want P(player still available at my next pick) to be calibrated. Files: optimize/opponents.py, _opponent_choice_sets.py, _opponent_eval.py, tests test_opponents.py.",
      "acceptanceCriteria": [
        "New opponent features: `position_run` (count of the last 3 league picks at the candidate's position), `roster_need` (open slots for the position on the picking seat), `adp_rank` (rank in the cheat sheet), and a sample weight of 2.0 for app-era rows with true `pick_number`; `train-opponents` reports leave-one-season-out membership accuracy and per-pick top-3 for old vs new features",
        "Survival calibration: on replayed drafts, predicted vs observed P(available at next turn) in 10 bins, pooled error within ±5 points target; written to the experiment report",
        "Adopted only if membership accuracy improves on the committed 0.287/0.341/0.287 and per-pick top-1 0.208 rows in the same report; otherwise not adopted and stated",
        "Evidence tier: this story writes `report.md` and `manifest.json` under `ml/artifacts/experiments/US-516/` (manifest has `git_sha` of the code commit, `git_dirty: false`, seed, config, per-season and pooled metrics); do NOT add it to MODEL_ARTIFACTS / BACKTEST_ARTIFACTS in tests/test_committed_model_evidence.py and do NOT touch `ml/artifacts/EVIDENCE_PASS.json`; the US-500 experiments guard test covers it",
        "Honesty (SPEC §7): the target above is attempted and then reported truthfully; a miss is written into the report with the real number and the interval, and the challenger is NOT adopted; never change baselines, splits, seeds, or held-out years to manufacture a pass",
        "CodeScene contract: every function you add or edit has cyclomatic complexity <= 8, <= 8 branches, <= 40 statements, <= 4 parameters (bundle more into a frozen dataclass request object, as cli/_request_commands.py does), nesting depth <= 3, and no condition with more than one `and`/`or` (extract a named predicate function); `uv run ruff check .` passes with the US-500 rule set",
        "CodeScene contract: new code goes into NEW modules of <= 400 lines (private helpers in `_name.py` files next to the public module); never grow an existing module past 600 lines and never make a hotspot file longer than you found it; no copy-pasted block of 5+ lines (extract a shared helper)",
        "CodeScene contract: tests use at most 3 consecutive `assert` statements per test function (use a comparison helper or `pytest.mark.parametrize`) and never repeat the same assertion block in two tests",
        "CodeScene contract: if CodeScene MCP tools are available, run `codescene-code_health_review` on every .py file you touched before committing and fix every reported issue; if they are not available, say so in progress.txt and rely on the ruff proxy rules",
        "All Typecheck passes (even if it's outside of your changes)",
        "Lint passes (even if it's outside of your changes)",
        "Unit tests have been added",
        "All Unit tests pass (even if it's outside of your changes)",
        "All Playwright tests pass (even if it's outside of your changes)"
      ],
      "priority": 35,
      "passes": false,
      "notes": ""
    },
    {
      "id": "US-517",
      "title": "Top-three projection drivers precomputed into the artifact and shown in the cheat sheet (PRD US-517)",
      "description": "As the tool owner, I want to see why a player is ranked where he is. New module ml/src/draft_oracle/models/drivers.py (build time only); artifact gains `drivers.parquet`; cheat sheet / `recommend` render short phrases; tests test_drivers.py, test_projection_artifact.py.",
      "acceptanceCriteria": [
        "`skater_drivers(model, X) -> pd.DataFrame` computes per-player contributions (SHAP via `shap` in the research extras for tree models; exact coefficient × value for linear/Bayesian models) and keeps the top 3 by absolute value as `driver_1..3` phrases like 'PP1 share +0.21' (feature -> phrase map in the module, tested)",
        "`oracle project` writes `drivers.parquet` next to the marginal tables; the cheat sheet adds ONE column 'Why' with the top driver; `recommend` shows all three; the US-208 import guard passes (`shap` added to the blocked roots in tests/test_cli.py)",
        "Committed 2026-r* fixtures are NOT regenerated in this story (the file is optional; readers tolerate its absence — test)",
        "CodeScene contract: every function you add or edit has cyclomatic complexity <= 8, <= 8 branches, <= 40 statements, <= 4 parameters (bundle more into a frozen dataclass request object, as cli/_request_commands.py does), nesting depth <= 3, and no condition with more than one `and`/`or` (extract a named predicate function); `uv run ruff check .` passes with the US-500 rule set",
        "CodeScene contract: new code goes into NEW modules of <= 400 lines (private helpers in `_name.py` files next to the public module); never grow an existing module past 600 lines and never make a hotspot file longer than you found it; no copy-pasted block of 5+ lines (extract a shared helper)",
        "CodeScene contract: tests use at most 3 consecutive `assert` statements per test function (use a comparison helper or `pytest.mark.parametrize`) and never repeat the same assertion block in two tests",
        "CodeScene contract: if CodeScene MCP tools are available, run `codescene-code_health_review` on every .py file you touched before committing and fix every reported issue; if they are not available, say so in progress.txt and rely on the ruff proxy rules",
        "All Typecheck passes (even if it's outside of your changes)",
        "Lint passes (even if it's outside of your changes)",
        "Unit tests have been added",
        "All Unit tests pass (even if it's outside of your changes)",
        "All Playwright tests pass (even if it's outside of your changes)"
      ],
      "priority": 36,
      "passes": false,
      "notes": ""
    },
    {
      "id": "US-518",
      "title": "Exploratory GRU sequence model over the last 40 games per skater (PRD US-518)",
      "description": "As the tool owner, I want to know whether a compact sequence model beats the tabular champion, expecting an honest negative result. New module ml/src/draft_oracle/models/sequence.py under the `research` extras (`torch` CPU); tests test_sequence.py that skip when torch is absent.",
      "acceptanceCriteria": [
        "`SequenceProductionModel` (GRU, hidden 32, one layer) trains on per-game sequences of the last 40 games (points, TOI, PP TOI, shots, ixG) with the US-507 features as static inputs; fixed seed; CPU training on the full data under 15 minutes (record the time in the manifest)",
        "Evaluated under the same leave-future-out protocol as US-507c; the experiment report compares MAE and rank ρ to the US-507 champion with intervals",
        "Adopted only if it beats the champion with the interval excluding zero; otherwise the report records the negative result and the model is not wired into the artifact; `torch` stays in the blocked import roots for the draft-time CLI",
        "Evidence tier: this story writes `report.md` and `manifest.json` under `ml/artifacts/experiments/US-518/` (manifest has `git_sha` of the code commit, `git_dirty: false`, seed, config, per-season and pooled metrics); do NOT add it to MODEL_ARTIFACTS / BACKTEST_ARTIFACTS in tests/test_committed_model_evidence.py and do NOT touch `ml/artifacts/EVIDENCE_PASS.json`; the US-500 experiments guard test covers it",
        "Honesty (SPEC §7): the target above is attempted and then reported truthfully; a miss is written into the report with the real number and the interval, and the challenger is NOT adopted; never change baselines, splits, seeds, or held-out years to manufacture a pass",
        "CodeScene contract: every function you add or edit has cyclomatic complexity <= 8, <= 8 branches, <= 40 statements, <= 4 parameters (bundle more into a frozen dataclass request object, as cli/_request_commands.py does), nesting depth <= 3, and no condition with more than one `and`/`or` (extract a named predicate function); `uv run ruff check .` passes with the US-500 rule set",
        "CodeScene contract: new code goes into NEW modules of <= 400 lines (private helpers in `_name.py` files next to the public module); never grow an existing module past 600 lines and never make a hotspot file longer than you found it; no copy-pasted block of 5+ lines (extract a shared helper)",
        "CodeScene contract: tests use at most 3 consecutive `assert` statements per test function (use a comparison helper or `pytest.mark.parametrize`) and never repeat the same assertion block in two tests",
        "CodeScene contract: if CodeScene MCP tools are available, run `codescene-code_health_review` on every .py file you touched before committing and fix every reported issue; if they are not available, say so in progress.txt and rely on the ruff proxy rules",
        "All Typecheck passes (even if it's outside of your changes)",
        "Lint passes (even if it's outside of your changes)",
        "Unit tests have been added",
        "All Unit tests pass (even if it's outside of your changes)",
        "All Playwright tests pass (even if it's outside of your changes)"
      ],
      "priority": 37,
      "passes": false,
      "notes": ""
    },
    {
      "id": "US-519",
      "title": "`oracle season-start` retrain-and-drift command (PRD US-519)",
      "description": "As the tool owner, I want one command that refreshes data, retrains every model, regenerates evidence and diffs metrics against the champion. New module ml/src/draft_oracle/cli/_season_start.py + `season-start` command; new ml/src/draft_oracle/drift.py; tests test_season_start.py, test_drift.py; DRAFT_NIGHT.md and README updated.",
      "acceptanceCriteria": [
        "`oracle season-start --season YYYY [--skip-fetch]` runs, in order and stopping on the first failure: the owner-machine fetch scripts (printing the exact commands and pausing for confirmation when `--skip-fetch` is absent), `normalize`, `odds`, every `train-*` command, `eval-series-sim`, `backtest --profile evidence`, and `evaluate` against the committed champion; each step's duration is printed and written to `ml/artifacts/drift/<season>/steps.json`",
        "`drift.py`: `feature_drift(current: pd.DataFrame, reference: pd.DataFrame) -> pd.DataFrame` (per feature: mean shift in reference sd units and a two-sample KS statistic) and `metric_deltas(new_manifest, champion_manifest) -> pd.DataFrame`; both tested; written to `ml/artifacts/drift/<season>/report.md`",
        "`DRAFT_NIGHT.md`'s 'once per season' section is replaced by the command; README documents it; the command is exercised end-to-end in a test with `--dry-run` that prints the plan without running steps",
        "CodeScene contract: every function you add or edit has cyclomatic complexity <= 8, <= 8 branches, <= 40 statements, <= 4 parameters (bundle more into a frozen dataclass request object, as cli/_request_commands.py does), nesting depth <= 3, and no condition with more than one `and`/`or` (extract a named predicate function); `uv run ruff check .` passes with the US-500 rule set",
        "CodeScene contract: new code goes into NEW modules of <= 400 lines (private helpers in `_name.py` files next to the public module); never grow an existing module past 600 lines and never make a hotspot file longer than you found it; no copy-pasted block of 5+ lines (extract a shared helper)",
        "CodeScene contract: tests use at most 3 consecutive `assert` statements per test function (use a comparison helper or `pytest.mark.parametrize`) and never repeat the same assertion block in two tests",
        "CodeScene contract: if CodeScene MCP tools are available, run `codescene-code_health_review` on every .py file you touched before committing and fix every reported issue; if they are not available, say so in progress.txt and rely on the ruff proxy rules",
        "All Typecheck passes (even if it's outside of your changes)",
        "Lint passes (even if it's outside of your changes)",
        "Unit tests have been added",
        "All Unit tests pass (even if it's outside of your changes)",
        "All Playwright tests pass (even if it's outside of your changes)"
      ],
      "priority": 38,
      "passes": false,
      "notes": ""
    },
    {
      "id": "US-520a",
      "title": "Final full evidence pass at a clean HEAD (PRD US-520 part 1 of 2)",
      "description": "As the tool owner, I want every committed champion artifact regenerated together after all model stories, with the comparison to the pre-v2 `main` artifacts embedded. Same procedure as US-505d; plan for many hours.",
      "acceptanceCriteria": [
        "Fold adopted experiments into the champion set: every adopted constant/default from US-503b..US-518 is in place; commit the code; from that clean commit regenerate ALL manifests in MODEL_ARTIFACTS (add the new `goals` and any adopted model directories to the tuple in tests/test_committed_model_evidence.py in the code commit), both backtests (evidence profile), the four `2026-r*` fixtures (now with `scenarios.npz`, `drivers.parquet`, conformal bands), and `EVIDENCE_PASS.json`; commit as a second commit",
        "Each regenerated `report.md` opens with the champion/challenger block versus the pre-v2 `main` artifacts (read the pre-v2 numbers from `git show <merge-base sha>:ml/artifacts/...`) with intervals; `oracle evaluate` pre-v2 champion vs v2 challenger is committed under `ml/artifacts/experiments/US-520a/`",
        "tests/test_committed_model_evidence.py and tests/test_committed_projection_artifacts.py pass; a `git clone --depth 1` into a temp directory passes the suite with 0 failures",
        "Honesty (SPEC §7): the target above is attempted and then reported truthfully; a miss is written into the report with the real number and the interval, and the challenger is NOT adopted; never change baselines, splits, seeds, or held-out years to manufacture a pass",
        "CodeScene contract: every function you add or edit has cyclomatic complexity <= 8, <= 8 branches, <= 40 statements, <= 4 parameters (bundle more into a frozen dataclass request object, as cli/_request_commands.py does), nesting depth <= 3, and no condition with more than one `and`/`or` (extract a named predicate function); `uv run ruff check .` passes with the US-500 rule set",
        "CodeScene contract: new code goes into NEW modules of <= 400 lines (private helpers in `_name.py` files next to the public module); never grow an existing module past 600 lines and never make a hotspot file longer than you found it; no copy-pasted block of 5+ lines (extract a shared helper)",
        "CodeScene contract: tests use at most 3 consecutive `assert` statements per test function (use a comparison helper or `pytest.mark.parametrize`) and never repeat the same assertion block in two tests",
        "CodeScene contract: if CodeScene MCP tools are available, run `codescene-code_health_review` on every .py file you touched before committing and fix every reported issue; if they are not available, say so in progress.txt and rely on the ruff proxy rules",
        "All Typecheck passes (even if it's outside of your changes)",
        "Lint passes (even if it's outside of your changes)",
        "Unit tests have been added",
        "All Unit tests pass (even if it's outside of your changes)",
        "All Playwright tests pass (even if it's outside of your changes)"
      ],
      "priority": 39,
      "passes": false,
      "notes": ""
    },
    {
      "id": "US-520b",
      "title": "`ml/EVALUATION.md` verdict and operator docs refresh (PRD US-520 part 2 of 2)",
      "description": "As the tool owner, I want a plain-language verdict against the PRD's success metrics and docs that match the new artifact. Files: new ml/EVALUATION.md, ml/DRAFT_NIGHT.md, ml/README.md.",
      "acceptanceCriteria": [
        "`ml/EVALUATION.md` has one table with exactly the rows of the PRD 'Success Metrics' table (champion today, target, v2 result, 95% interval, met / not met) filled from the US-520a reports, followed by one paragraph per unmet target stating the honest number and one plausible next step; no target is reworded",
        "`DRAFT_NIGHT.md` documents the new artifact contents (scenario tensor, objectives and `--standings`, injury and starter summary, drivers, conformal bands, `season-start`) and its 'Honest expectations' section is rewritten from the new numbers; README §Commands lists every new command (`evaluate`, `train-goals`, `season-start`, `--profile`, `--objective`, `--standings`, `--live-lineups`)",
        "A test asserts EVALUATION.md contains every metric name from the PRD table (parse both files) so the two cannot drift apart",
        "Honesty (SPEC §7): the target above is attempted and then reported truthfully; a miss is written into the report with the real number and the interval, and the challenger is NOT adopted; never change baselines, splits, seeds, or held-out years to manufacture a pass",
        "CodeScene contract: every function you add or edit has cyclomatic complexity <= 8, <= 8 branches, <= 40 statements, <= 4 parameters (bundle more into a frozen dataclass request object, as cli/_request_commands.py does), nesting depth <= 3, and no condition with more than one `and`/`or` (extract a named predicate function); `uv run ruff check .` passes with the US-500 rule set",
        "CodeScene contract: new code goes into NEW modules of <= 400 lines (private helpers in `_name.py` files next to the public module); never grow an existing module past 600 lines and never make a hotspot file longer than you found it; no copy-pasted block of 5+ lines (extract a shared helper)",
        "CodeScene contract: tests use at most 3 consecutive `assert` statements per test function (use a comparison helper or `pytest.mark.parametrize`) and never repeat the same assertion block in two tests",
        "CodeScene contract: if CodeScene MCP tools are available, run `codescene-code_health_review` on every .py file you touched before committing and fix every reported issue; if they are not available, say so in progress.txt and rely on the ruff proxy rules",
        "All Typecheck passes (even if it's outside of your changes)",
        "Lint passes (even if it's outside of your changes)",
        "Unit tests have been added",
        "All Unit tests pass (even if it's outside of your changes)",
        "All Playwright tests pass (even if it's outside of your changes)"
      ],
      "priority": 40,
      "passes": false,
      "notes": ""
    },
    {
      "id": "US-521",
      "title": "Final CodeScene sweep across every file changed since main",
      "description": "As the tool owner, I want the PR's CodeScene check fully green. This story only refactors; it must not change behaviour. Run after every other story has `passes: true`.",
      "acceptanceCriteria": [
        "List every `.py`/`.ts`/`.tsx` file changed versus `origin/main` (`git diff --name-only origin/main...HEAD`); for each, if CodeScene MCP tools are available run `codescene-code_health_review` (and `codescene-analyze_change_set` for the whole diff) and fix EVERY reported issue (Complex Method, Bumpy Road, Deep Nested Complexity, Complex Conditional, Excess Number of Function Arguments, Large Method, Code Duplication, Large Assertion Blocks, Duplicated Assertion Blocks, Low Cohesion/Large File); without MCP tools, apply the ruff proxy rules with tightened `max-complexity = 6` and `max-statements = 30` as a one-off local check (do not commit the tightened limits) and refactor every hit",
        "Behaviour is unchanged: `uv run pytest` passes with the same pass count as before the sweep; no committed artifact, report, or manifest changes",
        "progress.txt lists every file refactored and the CodeScene issues cleared; `.codescene-thresholds` is never lowered (raise it in this story only if the remote score improved)",
        "CodeScene contract: every function you add or edit has cyclomatic complexity <= 8, <= 8 branches, <= 40 statements, <= 4 parameters (bundle more into a frozen dataclass request object, as cli/_request_commands.py does), nesting depth <= 3, and no condition with more than one `and`/`or` (extract a named predicate function); `uv run ruff check .` passes with the US-500 rule set",
        "CodeScene contract: new code goes into NEW modules of <= 400 lines (private helpers in `_name.py` files next to the public module); never grow an existing module past 600 lines and never make a hotspot file longer than you found it; no copy-pasted block of 5+ lines (extract a shared helper)",
        "CodeScene contract: tests use at most 3 consecutive `assert` statements per test function (use a comparison helper or `pytest.mark.parametrize`) and never repeat the same assertion block in two tests",
        "CodeScene contract: if CodeScene MCP tools are available, run `codescene-code_health_review` on every .py file you touched before committing and fix every reported issue; if they are not available, say so in progress.txt and rely on the ruff proxy rules",
        "All Typecheck passes (even if it's outside of your changes)",
        "Lint passes (even if it's outside of your changes)",
        "Unit tests have been added",
        "All Unit tests pass (even if it's outside of your changes)",
        "All Playwright tests pass (even if it's outside of your changes)"
      ],
      "priority": 41,
      "passes": false,
      "notes": ""
    }
  ]
}
