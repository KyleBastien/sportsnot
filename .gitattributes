# Force LF line endings for all text files so Prettier's endOfLine: "lf"
# rule never triggers on checkout, regardless of OS core.autocrlf setting.
* text=auto eol=lf

# Preserve downloaded MoneyPuck dictionary bytes for provenance checks.
ml/data/raw/moneypuck/shots/*.csv binary
