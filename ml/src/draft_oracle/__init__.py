"""draft_oracle — ML draft optimizer for the SportsNot NHL playoff fantasy league.

The binding implementation contract is ``ml/SPEC.md``. Pipeline stages live in the
subpackages ``ingest``, ``features``, ``models``, ``optimize``, ``cli`` and
``backtest``.
"""

__version__ = "0.1.0"
