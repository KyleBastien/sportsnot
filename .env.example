# Supabase Configuration
VITE_SUPABASE_URL=your-supabase-project-url
VITE_SUPABASE_ANON_KEY=your-supabase-anon-key

# Mock Mode — set to 'true' to enable offline mock mode with fixture data
# When enabled, the app uses in-memory mock data instead of Supabase/NHL API
VITE_MOCK_MODE=false
